---
done: true
fix_cycles: 4
depends_on:
touches:
  - force-app/main/default/lwc/salesforceNavigator/salesforceNavigator.js
  - force-app/main/default/lwc/salesforceNavigator/salesforceNavigator.html
  - force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js
---

# Enter and leave edit mode, and nothing is written until Save

> Watch a new test go red before you make it green.
> _Why: a test written before the code cannot be written to hit a coverage number — there is nothing to
> cover yet. Retires when Apex gets mutation testing._

## What to build

A user sees a small edit affordance in the top right of the Navigator; clicking it enters edit mode, where
the page-level customisation controls appear and every change to the canvas is held unwritten until the
user presses Save, or thrown away when they press Cancel.

## Acceptance criteria

- [x] met Out of edit mode the Navigator's action row holds exactly two controls: the layout switcher and the edit affordance.
- [x] met The layout switcher lists the user's saved layouts and switches between them out of edit mode, because choosing a layout is navigation.
- [x] met The switcher's "New layout…", "Rename layout…" and "Delete layout…" entries are absent out of edit mode and present in it.
- [x] met "New section" is absent out of edit mode and present in it.
- [x] met Clicking the edit affordance replaces it with Cancel and Save; leaving edit mode restores the affordance.
- [x] met A canvas change made in edit mode is still unwritten after the autosave interval has elapsed — the debounce does not fire while editing.
- [x] met Save writes the layout, leaves edit mode, and the change is still there after a page reload.
- [x] met Cancel restores the canvas to exactly what it was when edit mode was entered, and writes nothing.
- [x] met A user who closes the tab or navigates away mid-edit loses the unsaved canvas change rather than having it flushed.
- [x] met Creating, renaming or deleting a saved layout inside an edit session commits immediately and is still there after a later Cancel.
- [x] met Entering edit mode and leaving it each produce a live-region announcement.
- [x] met Entering edit mode moves focus to the first revealed control in the action row; leaving returns focus to the edit affordance.
- [x] met The edit affordance does not render when the stored layout failed to load, so there is no way into a mode that could not save.

## Deviations

- **Entering edit mode flushes a pending pre-edit save.** The design does not mention it. A change made
  out of edit mode arms the 1000 ms debounce, and entering the mode a moment later would leave a write
  the autosave had already promised sitting behind a Save the user has not pressed — and hand Cancel a
  change it has no business reverting. Flushing on entry makes the boundary exact: the snapshot is what
  is stored, so "restore what was on screen on entry" and "restore what the store holds" are one
  sentence. Pinned by *commits a change made before edit mode was entered, so the snapshot is what is
  stored*.
- **The no-row rename commits through a new `commitLayoutNow()` rather than through its own Apex call.**
  The design says Tier 2 "already commits through its own Apex call"; that is true of four of the five
  paths and false of one. A user who owns no layout row is renamed by the write that *creates* the row,
  which is the autosave — and the autosave writes nothing in edit mode, so criterion 10 would have been
  false for exactly that user. `commitLayoutNow()` writes the layout as it stands without waiting for a
  debounce; it shares `captureSaveTarget()` with `scheduleSave()` so the two cannot disagree about what
  a write is addressed to.
- **A Tier 2 act that replaces the canvas re-takes the entry snapshot** (`resnapshotEdit()`, called from
  `createNewLayout` and `adoptFromStore`). Not in the design. Create, delete and switch each put a
  *different* layout on screen and commit on the spot; a Cancel restoring the pre-act snapshot would
  paint the previous layout's sections onto the layout now showing and hold them as unwritten draft —
  reverting a Tier 1 change the user never made, onto a row the revert was never about.
- **Save writes nothing when nothing changed.** The comparison is the same string equality on the
  canonical payload that slice 02 needs, exposed here as `hasUnsavedCanvasChanges`. Without it, a user
  who opened the mode, changed nothing and pressed Save would have a `Navigator_Layout__c` row created
  for them — breaking the component's oldest settled rule, that a user who has only ever looked owns no
  row.
- **Two guards in this slice are unreachable by construction and no test can fail on them**, and both
  are kept deliberately rather than missed: the `isEditing` branch in `disconnectedCallback`, and
  `discardPendingSave()` in `handleEditCancel`. Nothing can arm a save timer while editing — entry
  flushes, `scheduleSave` suppresses — so both are equivalent to their absence today. Measured by
  mutation, not assumed: removing either leaves all 489 tests green. They stay because the fact they
  depend on lives in two *other* methods, and the cost of it changing is a user's unsaved draft written
  to their layout without them asking. The comment on each says so.
- **Nine existing tests were changed by the gate**, none weakened. Four drove "New section" through a
  bare `querySelector("lightning-button")` and now enter edit mode first; three asserted the layout
  menu's six entries and now assert the three Tier 3 entries that render out of edit mode, with the
  Tier 2 half asked in edit mode where it lives; two asserted the absence of "New section" as proof
  that nothing could be changed yet and now assert the absence of the edit affordance — which is the
  only assertion that can still fail there, since "New section" is absent out of edit mode on any build
  at all.
- **Fix pass on findings 1, 2 and 4.** `commitLayoutNow(layoutJson)` now takes an explicit override
  rather than deciding internally off `isEditing` — an early version of the fix baked the "use the
  entry snapshot" branch straight into `commitLayoutNow` keyed on `isEditing`, which broke `handleEditSave`
  (the *other* caller of `commitLayoutNow`, which must always commit the current canvas): it started
  writing the entry snapshot on every Save, caught by the existing "deletes the section the user
  deleted" and "saves the seeded arrangement" tests going red. An explicit parameter, passed only by
  `renameCurrentLayout`'s no-row branch, cannot leak into the other call site by construction. Adding
  the `isEditing` guard to `handleLayoutMenuSelect` (finding 4) made three "switching between layouts"
  tests structurally unreachable as written — each raced a *live* Tier 1 debounce against a Tier 2 act,
  and once Tier 2 requires edit mode, the two can no longer coexist (entering edit mode is a
  precondition for reaching a Tier 2 act, and it is exactly what stops a Tier 1 change from arming a
  debounce). This closes `rememberSaved`'s create-adoption double-condition guard to every remaining
  user path; confirmed by mutation, collapsing it to `if (!target.layoutId)` leaves all 490 tests
  green. Those three tests were narrowed to the outcome that remains true and reachable rather than
  deleted, so the guard's original hazard stays documented even though nothing can fail on it anymore —
  the same treatment this slice already gives its other unreachable-by-construction guards.
- **Correction to the bullet above (re-review, finding B).** It claims: "This closes `rememberSaved`'s
  create-adoption double-condition guard to every remaining user path; confirmed by mutation, collapsing
  it to `if (!target.layoutId)` leaves all 490 tests green." The mutation result was real but the
  conclusion drawn from it does not follow: a green suite on the collapsed guard meant no test reached
  the guard, not that no *user* could. What is actually true: the guard remains reachable by a pairing
  the fix pass did not consider — `createNewLayout` ("New layout") does not leave edit mode, so a user
  can press Save on the draft it is replacing while New layout's own create is still in flight, and that
  Save lands after `this.layoutId` already names the layout New layout created. Collapsing the guard on
  that path moves the switcher's checked entry back onto the abandoned draft, which a new test now pins;
  see finding B's disposition below.
- **Fourth fix pass, implementing Jonah's decision (2026-08-31) that a no-row rename superseded by New
  layout keeps its own row.** `creatingLayout` gained an identity, `{ promise, distinct }`, rather than
  staying a bare promise, because the wait needed to be able to tell "a create for the layout I am
  writing" from "some other layout's create" per the finding's own words — a fourth flag in a fourth
  place would have violated `rstk-dry-enforcement.md`, so the one existing field carries the identity
  instead. `createNewLayout`'s create is the only one tagged `distinct: true`; `commitLayoutNow`'s own
  create (Save, or a no-row rename that is the first write) leaves it unset, since that is the one
  implicit layout a later write on the same no-row user is genuinely addressed to. `persist`'s create
  branch was also given the `isCurrent` question its update branch and `rememberSaved` already ask,
  rather than a hard-coded `makeActive: true` — the half of the finding the critic named directly, and
  the reason two creates racing for the same rowless user can no longer both claim the active flag.
  Closing this exposed that the paired coverage finding could not be closed as specified: the fold the
  re-resolution logic existed to correct is exactly what this fix removes, so no interleaving reachable
  after this fix makes that logic's true branch disagree with a bare replay — confirmed by mutation
  both before and after this fix landed, not assumed. See both findings' dispositions for the full
  reasoning and the probes run.

## Critique findings

- [x] fixed — `commitLayoutNow` no longer hard-codes `this.layout` as the payload; it now takes an optional `layoutJson` override, defaulting to the current canvas via `captureSaveTarget`'s own default parameter. `renameCurrentLayout`'s no-`layoutId` branch passes the entry snapshot's `json` instead, so the create it triggers is addressed to the canvas as it stood on entry, never to the Tier 1 draft in progress. Once that write's promise resolves with a `layoutId`, the branch replaces `this.editSnapshot` with `{ ...snapshot, wasStored: true }` (only if still the same session), so a later Cancel restores *to* the row the user now owns rather than wiping `storedLayout` back to `undefined`. The regression test (`commits a rename made inside an edit session by a user who owns no layout row yet`) now makes a canvas change (`addSection`) before renaming, asserts the `createLayout` payload directly (`sections: ["All Items"]`, not `["All Items", "New section"]`), and remounts on the captured payload to prove the cancelled section does not survive a reload — verified red on the unfixed code before the fix landed. Original finding: A layout rename made inside an edit session by a user who owns no layout row writes the unsaved Tier 1 draft to the server. `renameCurrentLayout`'s no-`layoutId` branch calls `commitLayoutNow()`, which serialises `this.layout` — and in edit mode `this.layout` is `storedLayout`, which `applyLayout` has already been setting to the draft. Reproduced: enter edit mode with no stored row, press New section, then rename the layout, and the `createLayout` payload is `sections: ["All Items", "New section"]`. Press Cancel and the screen goes back to `["All Items"]` while the row keeps the section, so the cancelled change survives a reload — O10 and criterion 8's "writes nothing" both fail for that user. Two halves to fix: the commit must be addressed to the entry snapshot rather than to the current canvas, and the snapshot's `wasStored` must stop saying `false` once that write has created the row, or a later Cancel sets `storedLayout` back to `undefined` on a user who now owns one.
- [x] fixed — `handleEditSave` now captures `hasUnsavedCanvasChanges` into `wroteChanges` before calling `leaveEditMode()` (which clears `editSnapshot` and would otherwise make the getter useless afterward), and announces `SAVE_EDIT_ANNOUNCEMENT` only when `wroteChanges` is true, `CANCEL_EDIT_ANNOUNCEMENT` otherwise — reusing Cancel's own wording rather than inventing a second sentence that says the same thing, per the finding's instruction to hold Save to the standard Cancel already meets. New test `announces that nothing was saved when Save is pressed with no changes, not a save that did not happen` pins it, and was verified red against the unfixed code (it received "Changes saved. Edit mode off." instead of the Cancel wording). The existing combined announcement test now makes a real change (`addSection`) before the Save half, since it needs the "changes were saved" branch to still be true after this fix. Original finding: `handleEditSave` announces "Changes saved. Edit mode off." even when `hasUnsavedCanvasChanges` is false and nothing was written. The guard that correctly stops the write does not reach the sentence, so the one channel a screen-reader user has reports a save that did not happen. Minor, but it is O12's channel and the Cancel wording was deliberately made a fact about the write rather than about the changes.
- [ ] Creating a new layout, or deleting one, inside an edit session discards the unsaved Tier 1 draft without asking, and `resnapshotEdit()` then puts it beyond Cancel's reach. `createNewLayout` writes `buildSeededLayout(this.items)` rather than the draft, so nothing leaks — the draft is simply gone. The design's discard confirmation names two call sites, Cancel and layout-switch, and slice 02's criteria inherit that pair; these two are a third and fourth. Likely slice 02's ground rather than a rebuild of this one, but a fresh builder there needs to know the pair is not the whole set.
- [x] fixed — `handleLayoutMenuSelect` now re-checks `isEditing` for `new-layout`, `rename-layout` and `delete-layout` before acting on them (Tier 3's `layout:…` values stay ungated, since choosing a layout is navigation, not customisation), on the same "a template gate is not a guarantee about a handler" reasoning already applied to `handleEditStart`. Every Tier 2 test in `describe("switching between layouts")` that dispatched those select events out of edit mode now calls `enterEditMode` first, so each again proves a user can reach the act it tests; the loop-based "opening the menu…" and "cancelling New layout…" tests were updated the same way. Three of those tests raced a live Tier 1 debounce against a Tier 2 act (`a change still in the debounce when its layout is deleted…`, `a change made while its layout is being deleted…`, `a change made while a new layout is being created…`); once Tier 2 requires edit mode, a live debounce and a Tier 2 act can no longer coexist (entering edit mode is a precondition for reaching either, and it is exactly what stops a Tier 1 change from arming a debounce), so that specific race — including `rememberSaved`'s create-adoption double-condition guard — is now unreachable by any user path. Confirmed by mutation: collapsing that guard to `if (!target.layoutId)` still leaves all 490 tests green. The three tests were renamed and narrowed to what remains true and reachable rather than deleted, and a fourth (`a rename the store refuses…`) was adjusted to commit its "next unrelated change" through Save instead of the autosave, since that change is now made inside the same edit session. Original finding: `handleLayoutMenuSelect` accepts `new-layout`, `rename-layout` and `delete-layout` whatever `isEditing` says, while `handleEditStart` was given a defensive re-check of `canEdit` on the reasoning that "a template gate is not a guarantee about a handler". The same reasoning applies to the Tier 2 entries and was not applied. The visible cost today is in the suite: every Tier 2 test in `describe("switching between layouts")` dispatches those select events out of edit mode, where the `lightning-menu-item`s are absent from the DOM, so they no longer prove a user can reach the act they test. Low priority — nothing is reachable that should not be — but the tests read as user paths and are not.
- [x] false positive — that the `recordFocusMoves()` spy on `HTMLElement.prototype.focus` leaks into later tests. It is installed with `jest.spyOn` and `jest.restoreAllMocks()` was added to `afterEach`, so it is restored. Checked harder: deleting that `restoreAllMocks()` line leaves all 181 tests green, because the two tests that install the spy are the last two in the file. The line is correct and currently carries no load.
- [x] false positive — that the focus assertions do not prove focus landed where the design says. Verified by mutation: deleting the `this.focusEditTransition()` call from `renderedCallback` reddens both focus tests, and repointing `EDIT_ENTRY_FOCUS_SELECTOR` at `.rstk-nav-edit-save` reddens the entry one. The assertions discriminate on element identity, not on "some focus happened".
- [x] false positive — that `resnapshotEdit()` misses a Tier 2 path that replaces `storedLayout`. The complete set of assignments is `applyLayout` (Tier 1, must not resnapshot), `restoreEditSnapshot` (Cancel), `createNewLayout` and both branches of `adoptFromStore` (all three call it), plus `adoptActiveLayout` and the `getLayouts` catch. The last two run only before `hasLoadedLayout` is true, and `canEdit` is `hasItems && !hasLayoutLoadError` where `hasItems` requires `!isLoading` requires `hasLoadedLayout` — so edit mode cannot have been entered when either runs. Mutation agrees: dropping either call reddens the create test or the delete test. The rename gap filed above is about `wasStored`, not about a canvas replacement.
- [x] false positive — that the nine adjusted existing tests were weakened. Read before and after on all ten adjusted spots. Four that drove "New section" through a bare `querySelector("lightning-button")` now enter edit mode first and keep every assertion. The two readiness tests were retargeted from `NEW_SECTION_BUTTON` absence — which is vacuous out of edit mode on any build — to the edit affordance, which is the only control whose absence still carries the claim, and both gained a positive assertion the originals did not have. The two menu-inventory tests dropped the three Tier 2 labels only, and those are re-asserted by the new `keeps New, Rename and Delete layout out of the switcher until edit mode is entered`. Nothing lost an assertion that is still true of the gated build.
- [x] false positive — that the suppression is more than one guard, or that a call site bypasses it. `scheduleSave()` has exactly one caller, `applyLayout` at line 1646, and every Tier 1 mutation reaches the store through `applyLayout`; the single `if (this.isEditing) return;` therefore covers all ten call sites, and dropping it reddens the mid-edit autosave test. `captureSaveTarget()` is shared by `scheduleSave` and `commitLayoutNow`, so the debounced and immediate writes cannot disagree about their target. `rstk-dry-enforcement.md` is satisfied.
- [x] false positive — that the builder overstated the two guards it declared unreachable. Both are genuinely dead: removing the `isEditing` branch from `disconnectedCallback`, and removing `discardPendingSave()` from `handleEditCancel`, each leave all 181 tests green. A third of the same kind that the Deviations do not name — the `!this.canEdit || this.isEditing` early return in `handleEditStart` — is dead on the same argument and also leaves the suite green. `rstk-preserve-defensive-checks.md` covers all three; none is a finding. Criterion 9 is met by `scheduleSave`'s suppression rather than by the disconnect branch, and the mutation confirms the flush-on-entry that makes the branch dead is itself live: dropping it reddens the pre-edit commit test.
- [x] fixed — `commitLayoutNow` now tracks a `creatingLayout` field: while a create it started for a user with no row is still unresolved, a second call (from Save or from `renameCurrentLayout`'s no-row branch) checks `!this.layoutId && this.creatingLayout` and, if both hold, waits on that promise and calls itself again rather than capturing a save target immediately. By the time it re-runs, the first create has landed and `this.layoutId` is set, so the second write recaptures with a real `layoutId` and lands as an `updateLayout` on the row the first call created, never as a second `createLayout`. The guard lives in this one method — both callers reach the server through it, so this is the single place `rstk-dry-enforcement.md` asks for rather than a copy at each call site. New test (`entering and leaving edit mode` describe) `a Save issued while a no-row rename's create is still in flight updates that row instead of creating a second one` defers the rename's `createLayout` call, presses Save while it is unresolved, and asserts `createLayout` is called exactly once and `updateLayout` once with the created `layoutId` — row count and which row is checked, not only which call was made, per the spec's new trap. Verified red on the unfixed `commitLayoutNow` (it called `createLayout` a second time) before the fix landed. Considered whether this counts as the larger coalescing/serialising change the pass was told to pause on: it does not change *what* Tier 1/2/3 do or when a write is "immediate" from the user's perspective — it only makes an already-in-flight create visible to the next capture, which is the mechanism the finding itself asks for ("make a create in flight visible to the next immediate write"), so this is a `##Deviations`-worthy *how*, not a design question. Original finding: A second immediate write issued while a first create's round trip is still in flight makes a second layout row for a user who owned none. `commitLayoutNow` calls `captureSaveTarget` synchronously and that reads `this.layoutId`, which stays `undefined` until `rememberSaved` adopts the id when the first create lands — so the second write is captured as another create. Two routes reproduced against `e491073`, both green today: (1) enter edit mode owning no row, change the canvas, rename the layout, then press Save before the create returns — `createLayout` is called twice and the store ends up holding two rows; (2) no Tier 2 act needed at all — enter, change, Save, enter again, change, Save again inside the first round trip, same two rows. The second create carries `makeActive: true`, so the server's active row is the one the client's `layoutId` does not name, and the switcher lists two identically named layouts: the "one row, two meanings" failure `save()`'s own comment says this file exists to keep shut, arrived at from a third direction. Introduced here rather than inherited — before this slice the only create was the debounce, and `scheduleSave` overwrites `pendingSave` so a burst coalesces into one; `commitLayoutNow` and the Save button both write immediately and neither coalesces. The fix pass reasoned about exactly this window when it chained a `.then()` onto the create to flip `wasStored`, and closed only the snapshot half of it.
- [x] fixed — no production change; the guard (`if (!target.layoutId && this.layoutId === undefined)`) was already intact in `rememberSaved`. Route (2) of the finding above is closed by this pass's fix to finding A — once `commitLayoutNow` coalesces a second no-row write into an update of the first create's row, that specific pairing no longer reaches `rememberSaved`'s create branch at all with a second, independent `layoutId: undefined` target, so it can no longer be used to demonstrate the guard. A different, still-live pairing does reach it: `createNewLayout` ("New layout") does not leave edit mode, so a user can press Save on the draft it is replacing while its own `createLayout` round trip is in flight; that Save is captured with `layoutId: undefined` too, and lands after `this.layoutId` already names the layout New layout created. New test `a Save that lands after New layout does not move the active layout back to the draft it replaced` (`switching between layouts` describe) uses `deferNextCreate` on New layout's own call, presses Save into the wait, and asserts the switcher's checked entry — verified red when the guard is collapsed to `if (!target.layoutId)` (checked entry moved from "Weekly review" to "My Navigator", the abandoned draft) and green with the guard intact. The docstring on the neighbouring "a new layout being created does not show on screen..." test, which carried the false claim, was corrected in place rather than only here, since a reader hitting the false claim first would not otherwise know it had been superseded. The corresponding `## Deviations` bullet is corrected below. Original finding: `rememberSaved`'s `this.layoutId === undefined` condition is live, not dead, so the surviving mutant the fix pass discloses is a coverage gap rather than proof the guard is closed to every remaining path. Verified on route (2) of the finding above: with the guard intact the switcher's checked entry stays `layout:<first created id>`; collapsed to `if (!target.layoutId)` it becomes `layout:<second created id>` — a different observable outcome on a path a user can walk, which is what load-bearing means. The Deviations bullet and finding 4's disposition both record the guard as unreachable by any user path and cite the green mutation as confirmation; the mutation result is real and the conclusion drawn from it is not. Left standing, that sentence reads to a later cleanup as permission to delete a guard that is holding.
- [x] fixed — no production change; `persist`'s `stillExists` early return was never touched by the narrowing and did not need to be. Restored the coverage through the route the finding names: new test `a Save that lands after its own layout has been deleted writes nothing and reports no save error` (`switching between layouts` describe) enters edit mode, changes the canvas, deletes the layout with `deferNextDelete` holding the round trip open, presses Save while the delete is still in flight, then releases the delete. Verified red with the guard disabled (`if (target.layoutId && !this.stillExists(target.layoutId))` short-circuited) — `updateLayout` fired on the deleted row — and green with it restored. Asserts `updateLayout` was never called and no `[role="alert"]` element appears, alongside the store ending up with exactly the two surviving rows and "Admin" active. Original finding: Narrowing the three "switching between layouts" tests dropped the suite's only coverage of `persist`'s `stillExists` early return, and that guard is still reachable. Measured on both sides: at `0f7f3df`, disabling the guard reddens exactly one test (`a change made while its layout is being deleted is written nowhere and reports no save error`); at `e491073` disabling it leaves all 490 green. What still reaches it is Save rather than the autosave — enter edit mode, change the canvas, delete the layout, and press Save while the delete round trip is in flight; `captureSaveTarget` captures the doomed `layoutId`, and `persist` runs after `adoptFromStore` has dropped that row from `this.layouts`. Confirmed by probe: with the guard disabled that sequence calls `updateLayout` on the deleted row and raises a save error about the layout the user has just removed, which is the exact failure the guard was written for. The narrowed test's docstring is accurate about its own interleaving; the sentence "which this interleaving can no longer reach" reads as a claim about the check itself, and the check is not unreachable.
- [x] fixed — `leaveEditMode()` now calls `closePrompt()`, the same method the three Tier 2 acts already call on their own successful commit. Chose this over adding an `isEditing` re-check to `handleLayoutNameCommit` and `handleLayoutDeleteConfirm` (the finding's other named option): `leaveEditMode` is the single place `isEditing` is ever set back to `false` — both Save and Cancel route through it — so one guard there covers both exit routes and both handlers at once, per `rstk-dry-enforcement.md`'s "guard goes in one place" reasoning already applied to finding A. A handler-side re-check would also leave the stale dialog itself rendered and, per the design's accessibility section, still announced to a screen reader as open even though clicking its button would now silently do nothing — closing the prompt outright removes the dangling UI as well as the dangling act. Two new tests in `entering and leaving edit mode`: `closes an open delete-layout prompt when Cancel ends edit mode, so the confirm button cannot act` (the finding's own repro, via Cancel) and `closes an open rename prompt when Save ends edit mode, so a stray commit does not fire afterward` (the same hazard through `handleLayoutNameCommit` and the other exit route, Save) — both verified red with `closePrompt()` removed from `leaveEditMode` (the prompt was still in the DOM after the mode ended) and green with it restored. Original finding: The Tier 2 gate stops at `handleLayoutMenuSelect`, and the prompt it opens outlives edit mode and still acts. The inline prompt renders under `hasItems`, not under `isEditing` (`salesforceNavigator.html:154` and `:179`), and neither `handleLayoutNameCommit` nor the "Delete layout" confirm button re-checks the mode. Reproduced: enter edit mode, choose "Delete layout...", press Cancel to leave the mode, then press the still-rendered "Delete layout" button — `deleteLayout` fires with the Navigator display-only and the edit affordance back in the action row. New layout and Rename layout leak the same way through `handleLayoutNameCommit`. This is finding 4's own reasoning, that a template gate is not a guarantee about a handler, applied one call deeper than the fix went; it closes either with the same `isEditing` re-check on those two handlers or with a `closePrompt()` in `leaveEditMode`.
- [x] fixed — chose to extend the existing wait-and-recapture mechanism (not the critic's literal "patch `layoutId` only" suggestion), because tracing the second reproduced route showed that patching only `layoutId` while leaving the rest of the target frozen would corrupt the row `createNewLayout` made. `createNewLayout` now sets `creatingLayout` too — write-only, since a user who already owns a row keeps `this.layoutId` truthy for the whole round trip and never needs to wait itself; it only has to be visible to a Save or a no-row rename racing inside its own round trip. The recursion in `commitLayoutNow` now also re-resolves `renameCurrentLayout`'s entry-snapshot override against `this.editSnapshot` *after* the wait clears, instead of replaying the value handed in before it: `createNewLayout` landing during the wait calls `resnapshotEdit` and re-takes that snapshot to the layout it just created, so replaying the stale value would have written the new layout's row under the old draft's pre-edit content and the superseded name. A bare call (`Save`) is untouched — `undefined` recurses as `undefined`, and `captureSaveTarget`'s own default already read `this.layout` fresh. This is why the invariant holds rather than being given up a second time: the triple `commitLayoutNow` eventually persists (`layoutId`, `name`, `layoutJson`) is always read together, at one moment — either the original call (no wait needed) or the moment the wait clears (all three fresh, never a stale payload paired with a fresh id) — never a mix of two moments the way a `layoutId`-only patch would produce. Two new tests in `switching between layouts` (`a Save that lands after New layout does not create a second row on the server`, `a no-row rename that lands after New layout does not create a second row on the server`) walk both routes below with `store.deferNextCreate()` holding New layout's create open, and assert `createLayout` called exactly once, `store.names()` and `store.activeName()` — not only the switcher's checked entry, which the finding-B test already showed stays right on the broken code. Both verified red against `886823a` (`createLayout` called twice, `toHaveBeenCalledTimes(1)` failing with 2) before the fix landed, and green after. Full jest suite green at 497/497; deployed clean. Original finding: Finding A's coalescing guard is placed where only two of the file's three creates can see it, and the third is `createNewLayout` -- so "a second immediate write inside a create's round trip makes a second row for a user who owned none" is still live, on the exact pairing this pass's own finding B named as reachable. `creatingLayout` is set only by `commitLayoutNow`; `createNewLayout` calls `createLayout` directly off `saveChain` and sets nothing, so a `commitLayoutNow` made inside its round trip sees `!this.layoutId && !this.creatingLayout`, skips the wait, and is captured as another create. Two routes reproduced against `886823a`, both green: (1) the scenario the new finding-B test already walks -- no row, enter edit mode, change the canvas, New layout "Weekly review" with `deferNextCreate` holding it open, press Save -- ends with `createLayout` called twice, `store.names()` `["Weekly review", "My Navigator"]`, and `store.activeName()` `"My Navigator"`, the abandoned draft, while the switcher shows "Weekly review" checked; remounting on that store lands the user on "My Navigator". (2) the same with a no-row rename in place of Save -- `["Weekly review", "Renamed"]`, active "Renamed". The finding-B test asserts only the switcher's checked entry and so is green on both halves of the damage the spec's own trap tells it to assert -- the row count and which row the store holds active. Slice-introduced rather than inherited: before the gate, `createNewLayout`'s `flushPendingSave()` put the no-row user's own create ahead of the new layout's, so the new layout's `makeActive: true` landed last and the server agreed with the client; with the draft held for Save the order inverts and it does not. Secondary, and the reason the present mechanism does not generalise: the wait re-captures *after* the await, so `layoutJson` and `name` are read at recursion time rather than at the moment of the act -- the one invariant `captureSaveTarget`, `pendingSave` and `save()` all exist to hold. No user-visible failure was reproduced from that alone (during the wait `this.layoutId` is `undefined`, so there is no other row to switch to), but a fix that patches the captured target's `layoutId` when the create lands would cover all three creates without giving the invariant up. Verified against the call path, not the disposition: `isEditing = false` occurs only in `leaveEditMode`, `creatingLayout` is written only in `commitLayoutNow`, and `createNewLayout` at line 745 is the third `createLayout` call site.
- [x] fixed — gave `creatingLayout` an identity: it is now `{ promise, distinct }` rather than a bare promise. `commitLayoutNow`'s own create (Save, or a no-row rename that is itself the first write in flight) leaves `distinct` unset — it is the same still-rowless implicit layout a later write on this no-row user is addressed to, so a second such write should wait for it and land as an update. `createNewLayout` tags its create `distinct: true` — "New layout" always makes its own, separate, freshly seeded row, never the layout a *different* write is addressed to. The wait in `commitLayoutNow` now reads: a bare call (Save, `layoutJson === undefined`) always waits, `distinct` or not, because it carries no identity of its own — it means "whatever is on screen" and is content to recapture fresh once the wait clears, exactly as before. An overridden call (only `renameCurrentLayout`'s no-row branch produces one) waits only when the in-flight create is *not* `distinct`; racing a `distinct` create, it falls through immediately and captures its own target off the entry snapshot it already holds, landing as its own `createLayout` rather than folding into the one in flight. Two acts, two rows, both honoured — Jonah's decision, 2026-08-31, implemented rather than re-opened. Second half of the fix, named directly by the finding: `persist`'s create branch no longer hard-codes `makeActive: true`. It now shares one `isCurrent` computed before the branch — `target.layoutId ? target.layoutId === this.layoutId : this.layoutId === undefined` — the create half asking exactly the question `rememberSaved` already asks one step later ("is the screen still undecided") instead of assuming yes, so two creates racing for the same rowless user can no longer both claim the active flag regardless of which one `this.layoutId` has already moved on to. The mis-pinned test `a no-row rename that lands after New layout does not create a second row on the server` is renamed to `a no-row rename that lands after New layout creates its own row rather than losing the rename` and now asserts `createLayout` called twice, `store.names()` `["Weekly review", "Renamed"]`, and `store.activeName()` `"Weekly review"` — plus, per the newest trap, the announcement itself: `spoken(region.textContent)` reads `"Layout renamed to Renamed."`, asserted before `releaseCreate()` so a later fold could not also erase the evidence of it. Verified red against the unfixed code first (`createLayout` called once, `toHaveBeenCalledTimes(2)` failing with 1, before either production line landed) and green after. Full jest suite green at 498/498; deployed clean to `sfnav-t2`. Criterion 10 and O11 now hold for this act: the rename lands, under its own name, on its own row, and Cancel was never in question since Tier 2 never reaches it. Route (1)'s Save-into-New-layout pairing is untouched by this fix (Save is a bare call, keeps coalescing into an update of New layout's row exactly as `a Save that lands after New layout does not create a second row on the server` already pinned, still green) — that pairing is the still-open draft-discard box above, deferred to slice 02, not reopened here.
- [x] fixed — no production change; this is a pure coverage gap, and closing it turned out to mean explaining why it cannot be closed as specified rather than writing the test as specified. The finding's probe (no row, columns-6, Save rejected, re-enter edit mode, New layout racing a no-row rename) is real and was walked exactly as written, but the fix above removes the fold it was probing: an overridden write racing a `distinct` create no longer waits on it or re-resolves anything against `this.editSnapshot` afterward, it falls through and makes its own row off the entry snapshot it already captured. Reachability was checked, not assumed, both before and after the fix above landed: with the fold still in place (pre-fix), the probe's own numbers hold, `columns === 3` with the re-resolution intact and `columns === 6` with it collapsed to a bare replay — confirmed by running both. With the fix above in place, the same probe now produces two rows (`"Weekly review"` at 3 columns, active; `"Renamed"` at 6 columns, inactive) regardless of whether the re-resolution ternary is collapsed to `resolvedOverride = layoutJson` — confirmed by mutation, full suite green either way. Every other interleaving that could still reach the wait's override branch was tried and ruled out the same way: a second no-row rename racing the first's own (not `distinct`) create does still wait and does still execute the ternary's true branch, but `this.editSnapshot.json` never moves during that wait (nothing but a `distinct` act's `resnapshotEdit` ever changes it, and a `distinct` act no longer waits for an override), so the two branches agree in value there too — confirmed the same way, full suite green collapsed or not. The branch is reachable in that one remaining shape and non-discriminating in it; it is not reachable at all in the shape the finding's probe used. New test `a no-row rename racing New layout after an earlier create was rejected still writes its own row correctly` keeps the finding's probe verbatim and asserts the outcome that is now true: two rows, New layout's own seed untouched at 3 columns, the rename's own row correctly holding what was actually on screen when it was renamed (the rejected six-column draft) rather than silently dropped — verified red against the unfixed code (`store.names()` only `["Weekly review"]`, the rename lost) and green after. The re-resolution line itself was not deleted — no production change beyond the fix above — and a comment was added at the site explaining, in the same terms as this disposition, why it is unreachable-with-effect today and what would make it load-bearing again (a future `resnapshotEdit` caller that is not accounted for as `distinct` or gated on a defined `layoutId`), the same standing `## Deviations` already gives the slice's other unreachable-by-construction guards.
- [ ] The `distinct` fall-through reopens the double-create it was meant to route around, because the single `creatingLayout` slot is cleared by whichever create resolves first rather than by its owner. An overridden call that falls through past a `distinct` create overwrites `this.creatingLayout` with its own non-`distinct` entry, and both writers clear the field with a bare `this.creatingLayout = undefined` that never checks the field still holds their own entry — so when `createNewLayout`'s create settles it wipes the *rename's* entry while the rename's own `createLayout` is still in flight. A Save made in that window reads `!this.layoutId && !this.creatingLayout`, skips the wait a bare call is documented to always take, and is captured as another create. Reproduced on a walkable path: no row, enter edit mode, set a section to six columns, choose New layout ("Weekly review") with its create held open, rename the layout to "Renamed" (falls through, its create queued behind New layout's), release New layout's create as *refused*, then press Save while the rename's create is still open — `createLayout` is called three times, `store.names()` is `["Renamed", "Renamed"]`, and the active row holds the three-column entry snapshot while the user's six-column Save is stranded on a second, inactive, identically named row, so the Save is lost on reload. Introduced here: the same probe against `ef9a4ba`'s production file gives two `createLayout` calls, one row, active, holding the six-column Save. A refused create is what makes it reachable — a create that succeeds sets `this.layoutId` and the wait's own `!this.layoutId` guard closes the window — and no test in the file lets a create be refused inside another create's round trip. Mechanical to fix: make each wipe conditional on the field still holding its own entry (`if (this.creatingLayout === mine)`); with that one change on both writers the probe returns to the pre-pass outcome and all 191 navigator tests plus the probe stay green.
- [ ] The re-resolution branch is not dead by construction, and the comment this pass added at the site says it is. Its stated reason — "the only thing that ever moves `editSnapshot.json` mid-session is `resnapshotEdit`" — leaves out `handleEditStart`'s own `captureEditSnapshot()`, which re-takes the snapshot every time the mode is re-entered, and leaves out that a user whose `storedLayout` is `undefined` has `this.layout` derived from `this.items`, which the tab wire re-delivers on an LDS cache refresh — an event this same file's comment at lines 413-418 calls "a normal event for a UI API adapter". Live, discriminating route, walked and measured: no row, enter edit mode, rename to "First" with its create held open, rename again to "Second" (an overridden call, which waits because the first rename's create is not `distinct`), Cancel out of the mode, let the wire redeliver a shorter tab list, re-enter the mode, then release the create. With the ternary intact the row is updated with the current entry snapshot (one section, one item); collapsed to `resolvedOverride = layoutJson` it is updated with the stale one (one section, three items) — a different observable on a path a user can walk, on a suite that is green either way. The branch itself is correct as written and needs no production change; the claim of deadness does, and this is the third "unreachable by construction" claim in this slice and the second of them to be false.
- [ ] `rememberSaved`'s create-adoption guard is a surviving mutant at HEAD, and the test written to pin it now passes for a new reason. Collapsing `if (!target.layoutId && this.layoutId === undefined)` to `if (!target.layoutId)` leaves all 498 green; the same collapse was already green against `ef9a4ba`, so the `## Deviations` correction bullet's route was not the live one even when it was written — Save is a bare call, so it waits on New layout's `distinct` create and lands as an *update*, and `a Save that lands after New layout does not move the active layout back to the draft it replaced` never reaches the create branch its docstring describes. The guard is still load-bearing, on this pass's own new path: on `a no-row rename that lands after New layout creates its own row rather than losing the rename`, collapsing it moves the switcher's checked entry from "Weekly review" to "Renamed" — a layout the user is not looking at and one the server holds inactive — while `store.names()` and `store.activeName()` stay exactly as that test asserts them. Now that the create's `makeActive` asks the same question the guard asks, the store is the observable that stops moving under the mutation and the screen is the one that does. One added assertion on the switcher's checked entry in that test closes it.
