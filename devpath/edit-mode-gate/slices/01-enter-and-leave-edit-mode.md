---
done: true
fix_cycles: 7
depends_on:
touches:
  - force-app/main/default/lwc/salesforceNavigator/salesforceNavigator.js
  - force-app/main/default/lwc/salesforceNavigator/salesforceNavigator.html
  - force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js
---

# Enter and leave edit mode, and nothing is written until Save

> Watch a new test go red before you make it green.
> _Why: a test written before the code cannot be written to hit a coverage number — there is nothing to
> cover yet. Retires when Apex gets mutation testing._

## What to build

A user sees a small edit affordance in the top right of the Navigator; clicking it enters edit mode, where
the page-level customisation controls appear and every change to the canvas is held unwritten until the
user presses Save, or thrown away when they press Cancel.

## Acceptance criteria

- [x] met Out of edit mode the Navigator's action row holds exactly two controls: the layout switcher and the edit affordance.
- [x] met The layout switcher lists the user's saved layouts and switches between them out of edit mode, because choosing a layout is navigation.
- [x] met The switcher's "New layout…", "Rename layout…" and "Delete layout…" entries are absent out of edit mode and present in it.
- [x] met "New section" is absent out of edit mode and present in it.
- [x] met Clicking the edit affordance replaces it with Cancel and Save; leaving edit mode restores the affordance.
- [x] met A canvas change made in edit mode is still unwritten after the autosave interval has elapsed — the debounce does not fire while editing.
- [x] met Save writes the layout, leaves edit mode, and the change is still there after a page reload.
- [x] met Cancel restores the canvas to exactly what it was when edit mode was entered, and writes nothing.
- [x] met A user who closes the tab or navigates away mid-edit loses the unsaved canvas change rather than having it flushed.
- [x] met Creating, renaming or deleting a saved layout inside an edit session commits immediately and is still there after a later Cancel.
- [x] met Entering edit mode and leaving it each produce a live-region announcement.
- [x] met Entering edit mode moves focus to the first revealed control in the action row; leaving returns focus to the edit affordance.
- [x] met The edit affordance does not render when the stored layout failed to load, so there is no way into a mode that could not save.

## Deviations

- **Entering edit mode flushes a pending pre-edit save.** The design does not mention it. A change made
  out of edit mode arms the 1000 ms debounce, and entering the mode a moment later would leave a write
  the autosave had already promised sitting behind a Save the user has not pressed — and hand Cancel a
  change it has no business reverting. Flushing on entry makes the boundary exact: the snapshot is what
  is stored, so "restore what was on screen on entry" and "restore what the store holds" are one
  sentence. Pinned by *commits a change made before edit mode was entered, so the snapshot is what is
  stored*.
- **The no-row rename commits through a new `commitLayoutNow()` rather than through its own Apex call.**
  The design says Tier 2 "already commits through its own Apex call"; that is true of four of the five
  paths and false of one. A user who owns no layout row is renamed by the write that *creates* the row,
  which is the autosave — and the autosave writes nothing in edit mode, so criterion 10 would have been
  false for exactly that user. `commitLayoutNow()` writes the layout as it stands without waiting for a
  debounce; it shares `captureSaveTarget()` with `scheduleSave()` so the two cannot disagree about what
  a write is addressed to.
- **A Tier 2 act that replaces the canvas re-takes the entry snapshot** (`resnapshotEdit()`, called from
  `createNewLayout` and `adoptFromStore`). Not in the design. Create, delete and switch each put a
  *different* layout on screen and commit on the spot; a Cancel restoring the pre-act snapshot would
  paint the previous layout's sections onto the layout now showing and hold them as unwritten draft —
  reverting a Tier 1 change the user never made, onto a row the revert was never about.
- **Save writes nothing when nothing changed.** The comparison is the same string equality on the
  canonical payload that slice 02 needs, exposed here as `hasUnsavedCanvasChanges`. Without it, a user
  who opened the mode, changed nothing and pressed Save would have a `Navigator_Layout__c` row created
  for them — breaking the component's oldest settled rule, that a user who has only ever looked owns no
  row.
- **Two guards in this slice are unreachable by construction and no test can fail on them**, and both
  are kept deliberately rather than missed: the `isEditing` branch in `disconnectedCallback`, and
  `discardPendingSave()` in `handleEditCancel`. Nothing can arm a save timer while editing — entry
  flushes, `scheduleSave` suppresses — so both are equivalent to their absence today. Measured by
  mutation, not assumed: removing either leaves all 489 tests green. They stay because the fact they
  depend on lives in two *other* methods, and the cost of it changing is a user's unsaved draft written
  to their layout without them asking. The comment on each says so.
- **Nine existing tests were changed by the gate**, none weakened. Four drove "New section" through a
  bare `querySelector("lightning-button")` and now enter edit mode first; three asserted the layout
  menu's six entries and now assert the three Tier 3 entries that render out of edit mode, with the
  Tier 2 half asked in edit mode where it lives; two asserted the absence of "New section" as proof
  that nothing could be changed yet and now assert the absence of the edit affordance — which is the
  only assertion that can still fail there, since "New section" is absent out of edit mode on any build
  at all.
- **Fix pass on findings 1, 2 and 4.** `commitLayoutNow(layoutJson)` now takes an explicit override
  rather than deciding internally off `isEditing` — an early version of the fix baked the "use the
  entry snapshot" branch straight into `commitLayoutNow` keyed on `isEditing`, which broke `handleEditSave`
  (the *other* caller of `commitLayoutNow`, which must always commit the current canvas): it started
  writing the entry snapshot on every Save, caught by the existing "deletes the section the user
  deleted" and "saves the seeded arrangement" tests going red. An explicit parameter, passed only by
  `renameCurrentLayout`'s no-row branch, cannot leak into the other call site by construction. Adding
  the `isEditing` guard to `handleLayoutMenuSelect` (finding 4) made three "switching between layouts"
  tests structurally unreachable as written — each raced a *live* Tier 1 debounce against a Tier 2 act,
  and once Tier 2 requires edit mode, the two can no longer coexist (entering edit mode is a
  precondition for reaching a Tier 2 act, and it is exactly what stops a Tier 1 change from arming a
  debounce). This closes `rememberSaved`'s create-adoption double-condition guard to every remaining
  user path; confirmed by mutation, collapsing it to `if (!target.layoutId)` leaves all 490 tests
  green. Those three tests were narrowed to the outcome that remains true and reachable rather than
  deleted, so the guard's original hazard stays documented even though nothing can fail on it anymore —
  the same treatment this slice already gives its other unreachable-by-construction guards.
- **Correction to the bullet above (re-review, finding B).** It claims: "This closes `rememberSaved`'s
  create-adoption double-condition guard to every remaining user path; confirmed by mutation, collapsing
  it to `if (!target.layoutId)` leaves all 490 tests green." The mutation result was real but the
  conclusion drawn from it does not follow: a green suite on the collapsed guard meant no test reached
  the guard, not that no *user* could. What is actually true: the guard remains reachable by a pairing
  the fix pass did not consider — `createNewLayout` ("New layout") does not leave edit mode, so a user
  can press Save on the draft it is replacing while New layout's own create is still in flight, and that
  Save lands after `this.layoutId` already names the layout New layout created. Collapsing the guard on
  that path moves the switcher's checked entry back onto the abandoned draft, which a new test now pins;
  see finding B's disposition below.
- **Fourth fix pass, implementing Jonah's decision (2026-08-31) that a no-row rename superseded by New
  layout keeps its own row.** `creatingLayout` gained an identity, `{ promise, distinct }`, rather than
  staying a bare promise, because the wait needed to be able to tell "a create for the layout I am
  writing" from "some other layout's create" per the finding's own words — a fourth flag in a fourth
  place would have violated `rstk-dry-enforcement.md`, so the one existing field carries the identity
  instead. `createNewLayout`'s create is the only one tagged `distinct: true`; `commitLayoutNow`'s own
  create (Save, or a no-row rename that is the first write) leaves it unset, since that is the one
  implicit layout a later write on the same no-row user is genuinely addressed to. `persist`'s create
  branch was also given the `isCurrent` question its update branch and `rememberSaved` already ask,
  rather than a hard-coded `makeActive: true` — the half of the finding the critic named directly, and
  the reason two creates racing for the same rowless user can no longer both claim the active flag.
  Closing this exposed that the paired coverage finding could not be closed as specified: the fold the
  re-resolution logic existed to correct is exactly what this fix removes, so no interleaving reachable
  after this fix makes that logic's true branch disagree with a bare replay — confirmed by mutation
  both before and after this fix landed, not assumed. See both findings' dispositions for the full
  reasoning and the probes run.
- **Fifth fix pass, closing findings 1, 2 and 3 of the fourth pass's re-review.** Finding 1's fix is
  exactly what the critic verified: both writers of `creatingLayout` (`createNewLayout`'s and
  `commitLayoutNow`'s own-create branch) now capture the object they are about to assign in a local
  `mine`, and their clear callback only sets the field back to `undefined` if `this.creatingLayout ===
  mine` still holds. Nothing else about the mechanism changed — no new field, no new bookkeeping site,
  per `rstk-dry-enforcement.md`'s existing shape for this field. Checked, not just applied: reverting
  `createNewLayout`'s ownership check alone reproduces the finding's own three-`createLayout` outcome on
  the new test; reverting `commitLayoutNow`'s own check alone leaves the full suite green, which means
  that half of the fix is currently correct-but-unproven rather than currently load-bearing — the
  reachable pairing that would discriminate it runs the other direction (a plain rename's own create
  outliving a later New layout that unconditionally overwrites the field on assignment, which no test
  walks and this pass did not add one for, since the finding named only the clear side). Finding 2 was a
  documentation-only fix: the comment was wrong, not the code, so nothing in `commitLayoutNow` changed
  for it. Finding 3 was a test-only fix: one assertion added to an existing test, no production line
  touched. All three new tests were watched red before green, against the mutation or the unfixed code
  each finding specified, and the three double-create tests named in the fix-pass instructions were
  re-run against a targeted mutation each and still kill it. Deployed clean to `sfnav-t2`; full jest
  suite green at 500/500 (498 carried over, plus the two new tests finding 1 and finding 2 each added;
  finding 3 added an assertion to an existing test rather than a new one).
- [x] fixed — the pause is resolved and the lockout is built. Jonah resolved it in session on 2026-08-31 by directing that slices 03, 04 and 05 be built first; those three landed and were critiqued, every Tier 1 control is now behind `lwc:if={editing}`, all four drag and drop surfaces are gated, and an independent scan found no route to the save path out of edit mode. The seventh pass then re-ran this box's own verification step, found the premise true, and built the lockout in `f0f1a72`; its critique is in `2e83f6d`. Closed by the orchestrator rather than by a worker, because the pause was answered by the engineer and this is the seat holding that answer. Original pause text follows. **Sixth pass, Jonah's decision (2026-08-31) to disable the four writing controls while a write is
  outstanding: paused at its own mandated Step 1.** The decision assumes every path that can issue a
  write to the server funnels through Save, New layout, Rename layout and Delete layout. It does not.
  `navigatorSection.html`/`.js` and `navigatorItem.html`/`.js` carry no `@api editing` and no reference
  to `isEditing` anywhere — checked by grep across all four files, zero matches — so the section's "Add
  items" button, its overflow menu (Rename section…/column count/Delete section), the item's overflow
  menu (Rename…/Remove/Move to…), and `draggable="true"` on both the section card and the item anchor
  render and operate exactly as `spec.md`'s `## Current state` describes the pre-slice codebase:
  unconditionally, whatever `isEditing` is. `salesforceNavigator.js:251`'s own comment says threading
  `@api editing` down is the job of "the later slices," and `devpath/edit-mode-gate/slices/
  03-gate-the-section-header-controls.md`, `04-gate-the-item-menu.md` and `05-gate-the-drag-surfaces.md`
  are exactly that work — none carries a `done` field, and this slice's own `touches:` frontmatter has
  never listed `navigatorSection.*` or `navigatorItem.*`. The section list itself renders under
  `hasItems` (`salesforceNavigator.html:119`), not under `isEditing`, and every one of `<c-navigator-
  section>`'s events is wired in the parent unconditionally (`:219-234`) — nothing about reaching these
  controls depends on the mode.
  Each of those controls calls `applyLayout()` → `scheduleSave()` (`salesforceNavigator.js:1778-1780`,
  `:1923`), whose only suppression is `if (this.isEditing) return;` (`:1942`) — a guard against writing
  *while editing*, not a gate on reaching these controls at all. Made before edit mode is ever entered,
  that guard does not fire: the 1000 ms debounce arms normally and, left alone, calls `save()` →
  `persist()` → `createLayout`/`updateLayout` (`:1962`, `:2006-2104`) — a genuine write touching none of
  the four controls, so a lockout scoped to them leaves this route open for the whole of its outstanding
  window.
  This is not only a gap in coverage; it reopens the exact "two rows for a rowless user" race this
  slice's five fix passes have spent closing, through an entry point `creatingLayout` was never built to
  see. A user with no stored row renames a section from its own overflow menu before ever clicking the
  pencil — live today, since the menu is ungated — arming the debounce. They then enter edit mode:
  `handleEditStart` (`:1147-1166`) calls `flushPendingSave()` (`:1972-1979`), which calls `this.save()`
  directly, **not** through `commitLayoutNow`, so it never sets `creatingLayout`. If that flush's
  `createLayout` round trip is still open when the user presses Save or performs a no-row rename — both
  of which do go through `commitLayoutNow` — `commitLayoutNow`'s own in-flight check, `!this.layoutId &&
  this.creatingLayout` (`:1868`), sees nothing in flight (the flush's create is invisible to that
  field), takes the immediate-capture branch, and issues a second `createLayout` — two rows for a user
  who owned none, the same failure findings 1, A and the sixth-gap box above all describe, reached
  through a path none of them audited because it never touches `commitLayoutNow` on its first leg. Not
  walked end-to-end against a running org or jest — this is a static trace, named precisely so a builder
  who picks this box up can walk it rather than re-deriving it.
  Per the task's own mandate this is a PAUSE, not a finding to patch inline: gating `navigatorSection`/
  `navigatorItem` behind `isEditing` is slices 03-05's stated scope, not this slice's `touches:` list,
  and reaching into those files here would be scope creep across a boundary the spec itself drew, not a
  fix to this slice. No lockout code was written this pass. Disabling the four controls while a write is
  outstanding is very likely still correct and worth doing once slices 03-05 land — it would close every
  race among Save/New/Rename/Delete themselves — but "two layout operations cannot be issued inside one
  server round trip" is not true in general yet, only true for that one subset of round trips, and a
  lockout built now would look like it closed the class while leaving this route standing.

- **Seventh pass, resuming the sixth: the premise re-verified, then the lockout built.** Step 1's own
  mandate was to enumerate every path into `createLayout`/`updateLayout`/`deleteLayout`/`renameLayout`
  and into `commitLayoutNow`/`save`/`persist`/`scheduleSave`, and name the user gesture for each. Slices
  03, 04 and 05 have since landed: `navigatorSection` and `navigatorItem` both carry `@api editing`,
  every Tier 1 control in both (the overflow menus, "Add items", both drag sources, both drop targets,
  both keyboard-grab paths) is gated on it — checked by reading both components' current `.js`/`.html`
  files directly, not by trusting the slices' own `done: true` — and `salesforceNavigator.js`'s own
  `handleSectionAddItems`/`handleSectionDrop`/`handleItemDragStart`/`handleSectionDragStart` are reachable
  only through events those now-gated children dispatch. With that confirmed, every route to the four Apex
  calls resolves to exactly one of the four writing controls: `createNewLayout` (New layout),
  `renameCurrentLayout`'s two branches (Rename layout — the no-row branch through `commitLayoutNow`, the
  with-row branch through `renameLayout` directly), `deleteCurrentLayout` (Delete layout), and
  `commitLayoutNow`'s bare call from `handleEditSave` (Save). `scheduleSave`'s debounce — the only path
  that could reach a write without going through one of the four — can no longer arm at all: it returns
  early while `isEditing`, and every caller of `applyLayout` (the only caller of `scheduleSave`) is now
  itself reachable only from a control gated on `editing`/`isEditing`, so a Tier 1 mutation can no longer
  happen at all while `isEditing` is false. The suite's own `describe("autosave")` tests confirm this from
  the inside: their own comment (`renameFirstItem`, added by slice 04) says they dispatch the `itemrename`
  event directly on the item element specifically because the menu that used to fire it is now edit-mode-
  only, "to keep pinning the debounce as the backstop `## Design` calls it — against a *future* ungated
  write — rather than as a route a user can still reach today." The premise holds; built on it.
- **The lockout: `isWriteLocked` (a `writeInFlight` counter), disabled rather than absent.** `disabled`
  on the existing `lightning-button` (Save) and the three `lightning-menu-item`s (New layout, Rename
  layout, Delete layout) — deliberately the opposite of the `lwc:if` absence rule the rest of the gate
  uses, because this is a transient busy state, not a mode: the control is still the same control, other
  controls' state does not change shape around it, and a screen reader should be told "not right now"
  rather than told the option no longer exists. Recorded here as the how, per the task's own instruction.
  `beginWrite()`/`endWrite()` are called at each of the four controls' own external entry point —
  `handleEditSave`, `createNewLayout`, both branches of `renameCurrentLayout`, `deleteCurrentLayout` —
  never inside `commitLayoutNow` itself, which is what let this land as a genuinely small diff: whatever
  `commitLayoutNow` returns does not settle until its own internal wait-and-retry has (if it takes that
  branch at all), so wrapping only the outer call already covers the whole window. Each of the three
  `saveChain`-based writers (`createNewLayout`, `deleteCurrentLayout`, `renameCurrentLayout`'s with-row
  branch) attaches `endWrite`'s `.finally()` to a *second, unreferenced* subscriber on the promise that
  gets assigned to `this.saveChain`, rather than to the promise chain `this.saveChain` itself becomes —
  a second `.then()`/`.finally()` subscriber on the same promise runs on its own schedule and cannot
  change when the *other* subscriber (`this.saveChain`'s own downstream) settles, so this pass changes
  nothing about the timing every other write already chained onto `saveChain` depends on. Handler-side
  re-checks in `handleEditSave` and `handleLayoutMenuSelect` (`this.isWriteLocked`) are what actually
  close the race, not the `disabled` attribute — the same "a template gate is not a guarantee about a
  handler" reasoning this file already applies to `handleEditStart` and to Tier 2's own gate — and
  mutation confirms both halves are independently load-bearing: removing either the handler guards or the
  template bindings alone reddens tests the other cannot cover.
- **Focus on disabling into a still-editing state: the layout switcher, a new `EDIT_FOCUS_LOCK` target.**
  Save is the one of the four that always calls `leaveEditMode()` in the same handler, so its own press is
  covered by the existing pencil hand-off (`EDIT_FOCUS_LEAVE`, assigned *after* `beginWrite`'s
  `EDIT_FOCUS_LOCK` in the same call, so the later write wins) — unchanged from before this pass. New
  layout, Rename layout and Delete layout are different: none of the three leaves edit mode, and
  committing any of them closes the inline prompt it was pressed from (`closePrompt()`) without restoring
  focus anywhere, which is Trap 3's hazard one control over from the pencil. `beginWrite` sets
  `editFocusTarget = EDIT_FOCUS_LOCK`, and `focusEditTransition`'s selector table sends it to
  `.rstk-nav-layout-menu` — the control that opened the prompt in the first place, and the one still
  usefully focusable once the other three are disabled. Mutation-confirmed: dropping the assignment
  reddens `moves focus to the layout switcher when New layout's own create leaves it disabled without
  leaving edit mode`.
- **Announced on engagement only, not on clearing — a deliberate asymmetry, not an oversight.**
  `beginWrite` announces `WRITE_LOCK_ANNOUNCEMENT` on the 0→1 transition, through the existing `announce()`
  helper per `.claude/rules/lwc-accessible-interactions.md`. `endWrite` announces nothing. Traced rather
  than assumed: all four controls already announce their own outcome on settling (`"<name> created and now
  showing."`, `"Layout renamed to <name>."`, `"Layout deleted. Now showing <name>."`, Save's
  `SAVE_EDIT_ANNOUNCEMENT`/`CANCEL_EDIT_ANNOUNCEMENT`), each set inside the `.then()`/`.catch()` on the
  *same* chain `endWrite`'s own `.finally()` is attached to — so a second, generic "available again"
  message fired from `endWrite` would render as a *later* microtask on that same chain and silently
  overwrite the more informative message several existing tests assert exact wording of (one pins
  `spoken(region.textContent)` to `"Layout renamed to Renamed."` directly). The busy announcement is safe
  on the *other* side of that ordering by construction: it always fires synchronously, strictly before the
  async outcome it will be superseded by, for all four controls — confirmed by the new announcement test,
  which asserts the busy text mid-flight and the outcome text after release, in that order. On failure,
  the existing `role="alert"` save-error region is the accessible channel already in place; this pass adds
  no second one, on the same reasoning.
- **A necessary, broader consequence of closing findings 1 and 2: the entire `creatingLayout` wait-and-
  recapture branch is now a surviving mutant across the whole suite, not only on the two pairings those
  findings named.** Measured, not assumed: with the lockout in place, short-circuiting `commitLayoutNow`'s
  `if (!this.layoutId && this.creatingLayout)` guard to `if (false)` — disabling the wait branch
  entirely — leaves all 535 tests green, as does collapsing its `resolvedOverride` ternary to a bare
  `layoutJson`, and as does collapsing `rememberSaved`'s create-adoption guard
  (`!target.layoutId && this.layoutId === undefined`) to `if (!target.layoutId)`. All three protect
  against the same precondition — two immediate creates racing for the same rowless user — which the
  lockout now forecloses at the handler level for the same reason findings 1 and 2 are closed: a second of
  the four writing controls can no longer be *attempted* while a first has a round trip outstanding, so
  `creatingLayout` is never asked to arbitrate between two of them. None of these guards was removed —
  they stay as defence in depth, per `.claude/rules/rstk-preserve-defensive-checks.md` and this slice's
  own standing treatment of its other unreachable-by-construction guards, and none of this is claimed dead
  *by construction* in the sense that phrase has been wrong twice on this slice before: it is measured, by
  mutation, against the full current suite, and reasoned from the two handler-side re-checks
  (`handleEditSave`, `handleLayoutMenuSelect`) that are what actually prevent a second write from being
  issued — not from an enumeration of callers that could later prove incomplete the way the "editSnapshot
  only moves via `resnapshotEdit`" claim did.
- **One test's discriminating power for `rememberSaved`'s create-adoption guard was lost, not preserved
  under another name, and that is recorded here rather than silently absorbed into the paragraph above.**
  Finding 3's disposition (a prior pass) added the guard's only assertion — the switcher's checked entry —
  to `a no-row rename that lands after New layout creates its own row rather than losing the rename`,
  which this pass retargeted to `cannot open Rename layout while New layout's create is still open, and
  renames the layout it created once it lands` because its own overlapping setup is exactly what the
  lockout now forecloses. The retargeted test no longer walks two creates racing for the same user at all
  — it walks one create, then a with-row rename of it — so it cannot carry that assertion forward, and no
  other test in the file was found to. Checked, not assumed: collapsing the guard to `if (!target.layoutId)`
  is part of the mutation sweep in the bullet above and the full suite stays green. This is the same
  closure as the rest of this bullet's family, reached through the one test that used to be the guard's
  sole coverage rather than through a fresh probe — recorded separately because it is a coverage loss with
  no replacement, not a redundant instance of one that already has three.

## Critique findings

- [x] fixed — `commitLayoutNow` no longer hard-codes `this.layout` as the payload; it now takes an optional `layoutJson` override, defaulting to the current canvas via `captureSaveTarget`'s own default parameter. `renameCurrentLayout`'s no-`layoutId` branch passes the entry snapshot's `json` instead, so the create it triggers is addressed to the canvas as it stood on entry, never to the Tier 1 draft in progress. Once that write's promise resolves with a `layoutId`, the branch replaces `this.editSnapshot` with `{ ...snapshot, wasStored: true }` (only if still the same session), so a later Cancel restores *to* the row the user now owns rather than wiping `storedLayout` back to `undefined`. The regression test (`commits a rename made inside an edit session by a user who owns no layout row yet`) now makes a canvas change (`addSection`) before renaming, asserts the `createLayout` payload directly (`sections: ["All Items"]`, not `["All Items", "New section"]`), and remounts on the captured payload to prove the cancelled section does not survive a reload — verified red on the unfixed code before the fix landed. Original finding: A layout rename made inside an edit session by a user who owns no layout row writes the unsaved Tier 1 draft to the server. `renameCurrentLayout`'s no-`layoutId` branch calls `commitLayoutNow()`, which serialises `this.layout` — and in edit mode `this.layout` is `storedLayout`, which `applyLayout` has already been setting to the draft. Reproduced: enter edit mode with no stored row, press New section, then rename the layout, and the `createLayout` payload is `sections: ["All Items", "New section"]`. Press Cancel and the screen goes back to `["All Items"]` while the row keeps the section, so the cancelled change survives a reload — O10 and criterion 8's "writes nothing" both fail for that user. Two halves to fix: the commit must be addressed to the entry snapshot rather than to the current canvas, and the snapshot's `wasStored` must stop saying `false` once that write has created the row, or a later Cancel sets `storedLayout` back to `undefined` on a user who now owns one.
- [x] fixed — `handleEditSave` now captures `hasUnsavedCanvasChanges` into `wroteChanges` before calling `leaveEditMode()` (which clears `editSnapshot` and would otherwise make the getter useless afterward), and announces `SAVE_EDIT_ANNOUNCEMENT` only when `wroteChanges` is true, `CANCEL_EDIT_ANNOUNCEMENT` otherwise — reusing Cancel's own wording rather than inventing a second sentence that says the same thing, per the finding's instruction to hold Save to the standard Cancel already meets. New test `announces that nothing was saved when Save is pressed with no changes, not a save that did not happen` pins it, and was verified red against the unfixed code (it received "Changes saved. Edit mode off." instead of the Cancel wording). The existing combined announcement test now makes a real change (`addSection`) before the Save half, since it needs the "changes were saved" branch to still be true after this fix. Original finding: `handleEditSave` announces "Changes saved. Edit mode off." even when `hasUnsavedCanvasChanges` is false and nothing was written. The guard that correctly stops the write does not reach the sentence, so the one channel a screen-reader user has reports a save that did not happen. Minor, but it is O12's channel and the Cancel wording was deliberately made a fact about the write rather than about the changes.
- [x] fixed — slice 02 built the whole set. `handleLayoutNameCommit`'s `PROMPT_NEW` branch and `handleLayoutDeleteConfirm` both now check `hasUnsavedCanvasChanges` before proceeding and, if it holds, open the same shared discard-confirmation prompt Cancel and layout-switch use (`PROMPT_DISCARD`, `openDiscardPrompt`/`handleDiscardConfirm`) rather than letting `createNewLayout`/`deleteCurrentLayout` run straight through and quietly discard the draft via their own `resnapshotEdit()`. Declining leaves the draft in place and creates or deletes nothing; confirming discards and proceeds. Proved by four new tests in `salesforceNavigator.test.js` (`entering and leaving edit mode`): `asks before discarding when New layout is chosen with unsaved canvas changes, and declining leaves the draft in place`, `confirming the discard prompt on New layout throws the draft away and creates the named layout`, and the Delete-layout pair with the same shapes — each guard mutation-verified both directions (forced open, forced closed) against the exact test it names. Original finding: `createNewLayout` writes `buildSeededLayout(this.items)` rather than the draft, so nothing leaks — the draft is simply gone. The design's discard confirmation names two call sites, Cancel and layout-switch, and slice 02's criteria inherit that pair; these two are a third and fourth. Likely slice 02's ground rather than a rebuild of this one, but a fresh builder there needs to know the pair is not the whole set.
- [x] fixed — `handleLayoutMenuSelect` now re-checks `isEditing` for `new-layout`, `rename-layout` and `delete-layout` before acting on them (Tier 3's `layout:…` values stay ungated, since choosing a layout is navigation, not customisation), on the same "a template gate is not a guarantee about a handler" reasoning already applied to `handleEditStart`. Every Tier 2 test in `describe("switching between layouts")` that dispatched those select events out of edit mode now calls `enterEditMode` first, so each again proves a user can reach the act it tests; the loop-based "opening the menu…" and "cancelling New layout…" tests were updated the same way. Three of those tests raced a live Tier 1 debounce against a Tier 2 act (`a change still in the debounce when its layout is deleted…`, `a change made while its layout is being deleted…`, `a change made while a new layout is being created…`); once Tier 2 requires edit mode, a live debounce and a Tier 2 act can no longer coexist (entering edit mode is a precondition for reaching either, and it is exactly what stops a Tier 1 change from arming a debounce), so that specific race — including `rememberSaved`'s create-adoption double-condition guard — is now unreachable by any user path. Confirmed by mutation: collapsing that guard to `if (!target.layoutId)` still leaves all 490 tests green. The three tests were renamed and narrowed to what remains true and reachable rather than deleted, and a fourth (`a rename the store refuses…`) was adjusted to commit its "next unrelated change" through Save instead of the autosave, since that change is now made inside the same edit session. Original finding: `handleLayoutMenuSelect` accepts `new-layout`, `rename-layout` and `delete-layout` whatever `isEditing` says, while `handleEditStart` was given a defensive re-check of `canEdit` on the reasoning that "a template gate is not a guarantee about a handler". The same reasoning applies to the Tier 2 entries and was not applied. The visible cost today is in the suite: every Tier 2 test in `describe("switching between layouts")` dispatches those select events out of edit mode, where the `lightning-menu-item`s are absent from the DOM, so they no longer prove a user can reach the act they test. Low priority — nothing is reachable that should not be — but the tests read as user paths and are not.
- [x] false positive — that the `recordFocusMoves()` spy on `HTMLElement.prototype.focus` leaks into later tests. It is installed with `jest.spyOn` and `jest.restoreAllMocks()` was added to `afterEach`, so it is restored. Checked harder: deleting that `restoreAllMocks()` line leaves all 181 tests green, because the two tests that install the spy are the last two in the file. The line is correct and currently carries no load.
- [x] false positive — that the focus assertions do not prove focus landed where the design says. Verified by mutation: deleting the `this.focusEditTransition()` call from `renderedCallback` reddens both focus tests, and repointing `EDIT_ENTRY_FOCUS_SELECTOR` at `.rstk-nav-edit-save` reddens the entry one. The assertions discriminate on element identity, not on "some focus happened".
- [x] false positive — that `resnapshotEdit()` misses a Tier 2 path that replaces `storedLayout`. The complete set of assignments is `applyLayout` (Tier 1, must not resnapshot), `restoreEditSnapshot` (Cancel), `createNewLayout` and both branches of `adoptFromStore` (all three call it), plus `adoptActiveLayout` and the `getLayouts` catch. The last two run only before `hasLoadedLayout` is true, and `canEdit` is `hasItems && !hasLayoutLoadError` where `hasItems` requires `!isLoading` requires `hasLoadedLayout` — so edit mode cannot have been entered when either runs. Mutation agrees: dropping either call reddens the create test or the delete test. The rename gap filed above is about `wasStored`, not about a canvas replacement.
- [x] false positive — that the nine adjusted existing tests were weakened. Read before and after on all ten adjusted spots. Four that drove "New section" through a bare `querySelector("lightning-button")` now enter edit mode first and keep every assertion. The two readiness tests were retargeted from `NEW_SECTION_BUTTON` absence — which is vacuous out of edit mode on any build — to the edit affordance, which is the only control whose absence still carries the claim, and both gained a positive assertion the originals did not have. The two menu-inventory tests dropped the three Tier 2 labels only, and those are re-asserted by the new `keeps New, Rename and Delete layout out of the switcher until edit mode is entered`. Nothing lost an assertion that is still true of the gated build.
- [x] false positive — that the suppression is more than one guard, or that a call site bypasses it. `scheduleSave()` has exactly one caller, `applyLayout` at line 1646, and every Tier 1 mutation reaches the store through `applyLayout`; the single `if (this.isEditing) return;` therefore covers all ten call sites, and dropping it reddens the mid-edit autosave test. `captureSaveTarget()` is shared by `scheduleSave` and `commitLayoutNow`, so the debounced and immediate writes cannot disagree about their target. `rstk-dry-enforcement.md` is satisfied.
- [x] false positive — that the builder overstated the two guards it declared unreachable. Both are genuinely dead: removing the `isEditing` branch from `disconnectedCallback`, and removing `discardPendingSave()` from `handleEditCancel`, each leave all 181 tests green. A third of the same kind that the Deviations do not name — the `!this.canEdit || this.isEditing` early return in `handleEditStart` — is dead on the same argument and also leaves the suite green. `rstk-preserve-defensive-checks.md` covers all three; none is a finding. Criterion 9 is met by `scheduleSave`'s suppression rather than by the disconnect branch, and the mutation confirms the flush-on-entry that makes the branch dead is itself live: dropping it reddens the pre-edit commit test.
- [x] fixed — `commitLayoutNow` now tracks a `creatingLayout` field: while a create it started for a user with no row is still unresolved, a second call (from Save or from `renameCurrentLayout`'s no-row branch) checks `!this.layoutId && this.creatingLayout` and, if both hold, waits on that promise and calls itself again rather than capturing a save target immediately. By the time it re-runs, the first create has landed and `this.layoutId` is set, so the second write recaptures with a real `layoutId` and lands as an `updateLayout` on the row the first call created, never as a second `createLayout`. The guard lives in this one method — both callers reach the server through it, so this is the single place `rstk-dry-enforcement.md` asks for rather than a copy at each call site. New test (`entering and leaving edit mode` describe) `a Save issued while a no-row rename's create is still in flight updates that row instead of creating a second one` defers the rename's `createLayout` call, presses Save while it is unresolved, and asserts `createLayout` is called exactly once and `updateLayout` once with the created `layoutId` — row count and which row is checked, not only which call was made, per the spec's new trap. Verified red on the unfixed `commitLayoutNow` (it called `createLayout` a second time) before the fix landed. Considered whether this counts as the larger coalescing/serialising change the pass was told to pause on: it does not change *what* Tier 1/2/3 do or when a write is "immediate" from the user's perspective — it only makes an already-in-flight create visible to the next capture, which is the mechanism the finding itself asks for ("make a create in flight visible to the next immediate write"), so this is a `##Deviations`-worthy *how*, not a design question. Original finding: A second immediate write issued while a first create's round trip is still in flight makes a second layout row for a user who owned none. `commitLayoutNow` calls `captureSaveTarget` synchronously and that reads `this.layoutId`, which stays `undefined` until `rememberSaved` adopts the id when the first create lands — so the second write is captured as another create. Two routes reproduced against `e491073`, both green today: (1) enter edit mode owning no row, change the canvas, rename the layout, then press Save before the create returns — `createLayout` is called twice and the store ends up holding two rows; (2) no Tier 2 act needed at all — enter, change, Save, enter again, change, Save again inside the first round trip, same two rows. The second create carries `makeActive: true`, so the server's active row is the one the client's `layoutId` does not name, and the switcher lists two identically named layouts: the "one row, two meanings" failure `save()`'s own comment says this file exists to keep shut, arrived at from a third direction. Introduced here rather than inherited — before this slice the only create was the debounce, and `scheduleSave` overwrites `pendingSave` so a burst coalesces into one; `commitLayoutNow` and the Save button both write immediately and neither coalesces. The fix pass reasoned about exactly this window when it chained a `.then()` onto the create to flip `wasStored`, and closed only the snapshot half of it.
- [x] fixed — no production change; the guard (`if (!target.layoutId && this.layoutId === undefined)`) was already intact in `rememberSaved`. Route (2) of the finding above is closed by this pass's fix to finding A — once `commitLayoutNow` coalesces a second no-row write into an update of the first create's row, that specific pairing no longer reaches `rememberSaved`'s create branch at all with a second, independent `layoutId: undefined` target, so it can no longer be used to demonstrate the guard. A different, still-live pairing does reach it: `createNewLayout` ("New layout") does not leave edit mode, so a user can press Save on the draft it is replacing while its own `createLayout` round trip is in flight; that Save is captured with `layoutId: undefined` too, and lands after `this.layoutId` already names the layout New layout created. New test `a Save that lands after New layout does not move the active layout back to the draft it replaced` (`switching between layouts` describe) uses `deferNextCreate` on New layout's own call, presses Save into the wait, and asserts the switcher's checked entry — verified red when the guard is collapsed to `if (!target.layoutId)` (checked entry moved from "Weekly review" to "My Navigator", the abandoned draft) and green with the guard intact. The docstring on the neighbouring "a new layout being created does not show on screen..." test, which carried the false claim, was corrected in place rather than only here, since a reader hitting the false claim first would not otherwise know it had been superseded. The corresponding `## Deviations` bullet is corrected below. Original finding: `rememberSaved`'s `this.layoutId === undefined` condition is live, not dead, so the surviving mutant the fix pass discloses is a coverage gap rather than proof the guard is closed to every remaining path. Verified on route (2) of the finding above: with the guard intact the switcher's checked entry stays `layout:<first created id>`; collapsed to `if (!target.layoutId)` it becomes `layout:<second created id>` — a different observable outcome on a path a user can walk, which is what load-bearing means. The Deviations bullet and finding 4's disposition both record the guard as unreachable by any user path and cite the green mutation as confirmation; the mutation result is real and the conclusion drawn from it is not. Left standing, that sentence reads to a later cleanup as permission to delete a guard that is holding.
- [x] fixed — no production change; `persist`'s `stillExists` early return was never touched by the narrowing and did not need to be. Restored the coverage through the route the finding names: new test `a Save that lands after its own layout has been deleted writes nothing and reports no save error` (`switching between layouts` describe) enters edit mode, changes the canvas, deletes the layout with `deferNextDelete` holding the round trip open, presses Save while the delete is still in flight, then releases the delete. Verified red with the guard disabled (`if (target.layoutId && !this.stillExists(target.layoutId))` short-circuited) — `updateLayout` fired on the deleted row — and green with it restored. Asserts `updateLayout` was never called and no `[role="alert"]` element appears, alongside the store ending up with exactly the two surviving rows and "Admin" active. Original finding: Narrowing the three "switching between layouts" tests dropped the suite's only coverage of `persist`'s `stillExists` early return, and that guard is still reachable. Measured on both sides: at `0f7f3df`, disabling the guard reddens exactly one test (`a change made while its layout is being deleted is written nowhere and reports no save error`); at `e491073` disabling it leaves all 490 green. What still reaches it is Save rather than the autosave — enter edit mode, change the canvas, delete the layout, and press Save while the delete round trip is in flight; `captureSaveTarget` captures the doomed `layoutId`, and `persist` runs after `adoptFromStore` has dropped that row from `this.layouts`. Confirmed by probe: with the guard disabled that sequence calls `updateLayout` on the deleted row and raises a save error about the layout the user has just removed, which is the exact failure the guard was written for. The narrowed test's docstring is accurate about its own interleaving; the sentence "which this interleaving can no longer reach" reads as a claim about the check itself, and the check is not unreachable.
- [x] fixed — `leaveEditMode()` now calls `closePrompt()`, the same method the three Tier 2 acts already call on their own successful commit. Chose this over adding an `isEditing` re-check to `handleLayoutNameCommit` and `handleLayoutDeleteConfirm` (the finding's other named option): `leaveEditMode` is the single place `isEditing` is ever set back to `false` — both Save and Cancel route through it — so one guard there covers both exit routes and both handlers at once, per `rstk-dry-enforcement.md`'s "guard goes in one place" reasoning already applied to finding A. A handler-side re-check would also leave the stale dialog itself rendered and, per the design's accessibility section, still announced to a screen reader as open even though clicking its button would now silently do nothing — closing the prompt outright removes the dangling UI as well as the dangling act. Two new tests in `entering and leaving edit mode`: `closes an open delete-layout prompt when Cancel ends edit mode, so the confirm button cannot act` (the finding's own repro, via Cancel) and `closes an open rename prompt when Save ends edit mode, so a stray commit does not fire afterward` (the same hazard through `handleLayoutNameCommit` and the other exit route, Save) — both verified red with `closePrompt()` removed from `leaveEditMode` (the prompt was still in the DOM after the mode ended) and green with it restored. Original finding: The Tier 2 gate stops at `handleLayoutMenuSelect`, and the prompt it opens outlives edit mode and still acts. The inline prompt renders under `hasItems`, not under `isEditing` (`salesforceNavigator.html:154` and `:179`), and neither `handleLayoutNameCommit` nor the "Delete layout" confirm button re-checks the mode. Reproduced: enter edit mode, choose "Delete layout...", press Cancel to leave the mode, then press the still-rendered "Delete layout" button — `deleteLayout` fires with the Navigator display-only and the edit affordance back in the action row. New layout and Rename layout leak the same way through `handleLayoutNameCommit`. This is finding 4's own reasoning, that a template gate is not a guarantee about a handler, applied one call deeper than the fix went; it closes either with the same `isEditing` re-check on those two handlers or with a `closePrompt()` in `leaveEditMode`.
- [x] fixed — chose to extend the existing wait-and-recapture mechanism (not the critic's literal "patch `layoutId` only" suggestion), because tracing the second reproduced route showed that patching only `layoutId` while leaving the rest of the target frozen would corrupt the row `createNewLayout` made. `createNewLayout` now sets `creatingLayout` too — write-only, since a user who already owns a row keeps `this.layoutId` truthy for the whole round trip and never needs to wait itself; it only has to be visible to a Save or a no-row rename racing inside its own round trip. The recursion in `commitLayoutNow` now also re-resolves `renameCurrentLayout`'s entry-snapshot override against `this.editSnapshot` *after* the wait clears, instead of replaying the value handed in before it: `createNewLayout` landing during the wait calls `resnapshotEdit` and re-takes that snapshot to the layout it just created, so replaying the stale value would have written the new layout's row under the old draft's pre-edit content and the superseded name. A bare call (`Save`) is untouched — `undefined` recurses as `undefined`, and `captureSaveTarget`'s own default already read `this.layout` fresh. This is why the invariant holds rather than being given up a second time: the triple `commitLayoutNow` eventually persists (`layoutId`, `name`, `layoutJson`) is always read together, at one moment — either the original call (no wait needed) or the moment the wait clears (all three fresh, never a stale payload paired with a fresh id) — never a mix of two moments the way a `layoutId`-only patch would produce. Two new tests in `switching between layouts` (`a Save that lands after New layout does not create a second row on the server`, `a no-row rename that lands after New layout does not create a second row on the server`) walk both routes below with `store.deferNextCreate()` holding New layout's create open, and assert `createLayout` called exactly once, `store.names()` and `store.activeName()` — not only the switcher's checked entry, which the finding-B test already showed stays right on the broken code. Both verified red against `886823a` (`createLayout` called twice, `toHaveBeenCalledTimes(1)` failing with 2) before the fix landed, and green after. Full jest suite green at 497/497; deployed clean. Original finding: Finding A's coalescing guard is placed where only two of the file's three creates can see it, and the third is `createNewLayout` -- so "a second immediate write inside a create's round trip makes a second row for a user who owned none" is still live, on the exact pairing this pass's own finding B named as reachable. `creatingLayout` is set only by `commitLayoutNow`; `createNewLayout` calls `createLayout` directly off `saveChain` and sets nothing, so a `commitLayoutNow` made inside its round trip sees `!this.layoutId && !this.creatingLayout`, skips the wait, and is captured as another create. Two routes reproduced against `886823a`, both green: (1) the scenario the new finding-B test already walks -- no row, enter edit mode, change the canvas, New layout "Weekly review" with `deferNextCreate` holding it open, press Save -- ends with `createLayout` called twice, `store.names()` `["Weekly review", "My Navigator"]`, and `store.activeName()` `"My Navigator"`, the abandoned draft, while the switcher shows "Weekly review" checked; remounting on that store lands the user on "My Navigator". (2) the same with a no-row rename in place of Save -- `["Weekly review", "Renamed"]`, active "Renamed". The finding-B test asserts only the switcher's checked entry and so is green on both halves of the damage the spec's own trap tells it to assert -- the row count and which row the store holds active. Slice-introduced rather than inherited: before the gate, `createNewLayout`'s `flushPendingSave()` put the no-row user's own create ahead of the new layout's, so the new layout's `makeActive: true` landed last and the server agreed with the client; with the draft held for Save the order inverts and it does not. Secondary, and the reason the present mechanism does not generalise: the wait re-captures *after* the await, so `layoutJson` and `name` are read at recursion time rather than at the moment of the act -- the one invariant `captureSaveTarget`, `pendingSave` and `save()` all exist to hold. No user-visible failure was reproduced from that alone (during the wait `this.layoutId` is `undefined`, so there is no other row to switch to), but a fix that patches the captured target's `layoutId` when the create lands would cover all three creates without giving the invariant up. Verified against the call path, not the disposition: `isEditing = false` occurs only in `leaveEditMode`, `creatingLayout` is written only in `commitLayoutNow`, and `createNewLayout` at line 745 is the third `createLayout` call site.
- [x] fixed — gave `creatingLayout` an identity: it is now `{ promise, distinct }` rather than a bare promise. `commitLayoutNow`'s own create (Save, or a no-row rename that is itself the first write in flight) leaves `distinct` unset — it is the same still-rowless implicit layout a later write on this no-row user is addressed to, so a second such write should wait for it and land as an update. `createNewLayout` tags its create `distinct: true` — "New layout" always makes its own, separate, freshly seeded row, never the layout a *different* write is addressed to. The wait in `commitLayoutNow` now reads: a bare call (Save, `layoutJson === undefined`) always waits, `distinct` or not, because it carries no identity of its own — it means "whatever is on screen" and is content to recapture fresh once the wait clears, exactly as before. An overridden call (only `renameCurrentLayout`'s no-row branch produces one) waits only when the in-flight create is *not* `distinct`; racing a `distinct` create, it falls through immediately and captures its own target off the entry snapshot it already holds, landing as its own `createLayout` rather than folding into the one in flight. Two acts, two rows, both honoured — Jonah's decision, 2026-08-31, implemented rather than re-opened. Second half of the fix, named directly by the finding: `persist`'s create branch no longer hard-codes `makeActive: true`. It now shares one `isCurrent` computed before the branch — `target.layoutId ? target.layoutId === this.layoutId : this.layoutId === undefined` — the create half asking exactly the question `rememberSaved` already asks one step later ("is the screen still undecided") instead of assuming yes, so two creates racing for the same rowless user can no longer both claim the active flag regardless of which one `this.layoutId` has already moved on to. The mis-pinned test `a no-row rename that lands after New layout does not create a second row on the server` is renamed to `a no-row rename that lands after New layout creates its own row rather than losing the rename` and now asserts `createLayout` called twice, `store.names()` `["Weekly review", "Renamed"]`, and `store.activeName()` `"Weekly review"` — plus, per the newest trap, the announcement itself: `spoken(region.textContent)` reads `"Layout renamed to Renamed."`, asserted before `releaseCreate()` so a later fold could not also erase the evidence of it. Verified red against the unfixed code first (`createLayout` called once, `toHaveBeenCalledTimes(2)` failing with 1, before either production line landed) and green after. Full jest suite green at 498/498; deployed clean to `sfnav-t2`. Criterion 10 and O11 now hold for this act: the rename lands, under its own name, on its own row, and Cancel was never in question since Tier 2 never reaches it. Route (1)'s Save-into-New-layout pairing is untouched by this fix (Save is a bare call, keeps coalescing into an update of New layout's row exactly as `a Save that lands after New layout does not create a second row on the server` already pinned, still green) — that pairing is the still-open draft-discard box above, deferred to slice 02, not reopened here.
- [x] fixed — no production change; this is a pure coverage gap, and closing it turned out to mean explaining why it cannot be closed as specified rather than writing the test as specified. The finding's probe (no row, columns-6, Save rejected, re-enter edit mode, New layout racing a no-row rename) is real and was walked exactly as written, but the fix above removes the fold it was probing: an overridden write racing a `distinct` create no longer waits on it or re-resolves anything against `this.editSnapshot` afterward, it falls through and makes its own row off the entry snapshot it already captured. Reachability was checked, not assumed, both before and after the fix above landed: with the fold still in place (pre-fix), the probe's own numbers hold, `columns === 3` with the re-resolution intact and `columns === 6` with it collapsed to a bare replay — confirmed by running both. With the fix above in place, the same probe now produces two rows (`"Weekly review"` at 3 columns, active; `"Renamed"` at 6 columns, inactive) regardless of whether the re-resolution ternary is collapsed to `resolvedOverride = layoutJson` — confirmed by mutation, full suite green either way. Every other interleaving that could still reach the wait's override branch was tried and ruled out the same way: a second no-row rename racing the first's own (not `distinct`) create does still wait and does still execute the ternary's true branch, but `this.editSnapshot.json` never moves during that wait (nothing but a `distinct` act's `resnapshotEdit` ever changes it, and a `distinct` act no longer waits for an override), so the two branches agree in value there too — confirmed the same way, full suite green collapsed or not. The branch is reachable in that one remaining shape and non-discriminating in it; it is not reachable at all in the shape the finding's probe used. New test `a no-row rename racing New layout after an earlier create was rejected still writes its own row correctly` keeps the finding's probe verbatim and asserts the outcome that is now true: two rows, New layout's own seed untouched at 3 columns, the rename's own row correctly holding what was actually on screen when it was renamed (the rejected six-column draft) rather than silently dropped — verified red against the unfixed code (`store.names()` only `["Weekly review"]`, the rename lost) and green after. The re-resolution line itself was not deleted — no production change beyond the fix above — and a comment was added at the site explaining, in the same terms as this disposition, why it is unreachable-with-effect today and what would make it load-bearing again (a future `resnapshotEdit` caller that is not accounted for as `distinct` or gated on a defined `layoutId`), the same standing `## Deviations` already gives the slice's other unreachable-by-construction guards.
- [x] fixed — both writers' clears now check `if (this.creatingLayout === mine)` before wiping the field, exactly as the critic verified. `createNewLayout` captures its own entry in a local `mine` before assigning it to `this.creatingLayout`, and its clear callback only sets the field back to `undefined` if it still holds that same object; `commitLayoutNow`'s own-create branch does the same. New test `a Save made while a no-row rename's create is open still updates that row after a distinct create it raced was refused` (`switching between layouts` describe) walks the finding's own probe verbatim — New layout's create refused rather than merely delayed, the rename's own create held open through it, Save pressed inside that window — and asserts `createLayout` called twice (not three), `updateLayout` once, `store.names()` `["Renamed"]`, `store.activeName()` `"Renamed"`, and the surviving row's payload holding the six-column Save rather than a second, inactive, identically named row losing it. Verified red against the unfixed code first: `createLayout` called three times, `toHaveBeenCalledTimes(2)` failing with 3 — the finding's own numbers, exactly. Re-verified by mutation after the fix landed: reverting `createNewLayout`'s ownership check alone (leaving `commitLayoutNow`'s fixed) reproduces the same three-call failure on this new test. Reverting `commitLayoutNow`'s own check alone leaves the full suite green — that check is not discriminated by any test in the file today, this one included; the reachable pairing for it runs the other way (a plain rename's own create outliving a later New layout that overwrites the field), which no test walks, so it stands on the same footing as the `## Deviations` guards this slice already keeps unproven-but-correct. The three existing double-create tests the task named — `a Save issued while a no-row rename's create is still in flight…`, `a Save that lands after New layout does not create a second row…`, and the two-row rename test (`a no-row rename that lands after New layout creates its own row rather than losing the rename`) — were each re-run against a targeted mutation (disabling the `!this.layoutId && this.creatingLayout` wait entirely; un-tagging `createNewLayout`'s create as `distinct`) and each still kills its mutant. Full jest suite green at 500/500; deployed clean to `sfnav-t2`. Original finding: the `distinct` fall-through reopens the double-create it was meant to route around, because the single `creatingLayout` slot is cleared by whichever create resolves first rather than by its owner. An overridden call that falls through past a `distinct` create overwrites `this.creatingLayout` with its own non-`distinct` entry, and both writers clear the field with a bare `this.creatingLayout = undefined` that never checks the field still holds their own entry — so when `createNewLayout`'s create settles it wipes the *rename's* entry while the rename's own `createLayout` is still in flight. A Save made in that window reads `!this.layoutId && !this.creatingLayout`, skips the wait a bare call is documented to always take, and is captured as another create. Reproduced on a walkable path: no row, enter edit mode, set a section to six columns, choose New layout ("Weekly review") with its create held open, rename the layout to "Renamed" (falls through, its create queued behind New layout's), release New layout's create as *refused*, then press Save while the rename's create is still open — `createLayout` is called three times, `store.names()` is `["Renamed", "Renamed"]`, and the active row holds the three-column entry snapshot while the user's six-column Save is stranded on a second, inactive, identically named row, so the Save is lost on reload. Introduced here: the same probe against `ef9a4ba`'s production file gives two `createLayout` calls, one row, active, holding the six-column Save. A refused create is what makes it reachable — a create that succeeds sets `this.layoutId` and the wait's own `!this.layoutId` guard closes the window — and no test in the file lets a create be refused inside another create's round trip. Mechanical to fix: make each wipe conditional on the field still holding its own entry (`if (this.creatingLayout === mine)`); with that one change on both writers the probe returns to the pre-pass outcome and all 191 navigator tests plus the probe stay green.
- [x] fixed — no production change; the branch was already correct, so this closes as a comment correction, per `rstk-preserve-documentation.md` corrected in place rather than deleted. The comment at `commitLayoutNow`'s wait now says the branch is live and names the two routes the "unreachable with effect" claim missed: re-entering edit mode re-takes the snapshot on every entry (`handleEditStart`'s `captureEditSnapshot()`), and a no-row user's `this.layout` is derived from `this.items`, which the tab wire can redeliver on an LDS cache refresh — the same event this file's own comment on `wiredNavItems` already calls normal. Pinned with a new test, `a no-row rename's overridden write resolves against the entry snapshot current when its wait clears, not the one handed in when it started` (`switching between layouts` describe), which walks the critic's own route — two no-row renames back to back (the second an overridden call that waits on the first's still-open, non-`distinct` create), Cancel, a wire redelivery to a shorter one-item tab list, re-entry, then release of the first's create — and asserts the resulting row holds the fresh one-item snapshot rather than the stale three-item one the second write started with. Verified red under the exact mutation the finding named (collapsing the ternary to `resolvedOverride = layoutJson`): the row held three items instead of one. The test passes unchanged on both the pre-fix and post-fix production file, since no code changed here — only the named mutation discriminates it, matching the finding's own description that this branch needs no production change. Original finding: the re-resolution branch is not dead by construction, and the comment this pass added at the site says it is. Its stated reason — "the only thing that ever moves `editSnapshot.json` mid-session is `resnapshotEdit`" — leaves out `handleEditStart`'s own `captureEditSnapshot()`, which re-takes the snapshot every time the mode is re-entered, and leaves out that a user whose `storedLayout` is `undefined` has `this.layout` derived from `this.items`, which the tab wire re-delivers on an LDS cache refresh — an event this same file's comment at lines 413-418 calls "a normal event for a UI API adapter". Live, discriminating route, walked and measured: no row, enter edit mode, rename to "First" with its create held open, rename again to "Second" (an overridden call, which waits because the first rename's create is not `distinct`), Cancel out of the mode, let the wire redeliver a shorter tab list, re-enter the mode, then release the create. With the ternary intact the row is updated with the current entry snapshot (one section, one item); collapsed to `resolvedOverride = layoutJson` it is updated with the stale one (one section, three items) — a different observable on a path a user can walk, on a suite that is green either way. The branch itself is correct as written and needs no production change; the claim of deadness does, and this is the third "unreachable by construction" claim in this slice and the second of them to be false.
- [x] fixed — no production change; the guard (`if (!target.layoutId && this.layoutId === undefined)`) was already intact. One assertion added to `a no-row rename that lands after New layout creates its own row rather than losing the rename`: after the existing `createLayout`/`store.names()`/`store.activeName()` checks, it now also asserts the switcher's checked entry reads `["Weekly review"]`. Verified red under the exact mutation the finding named (collapsing the guard to `if (!target.layoutId)`): the checked entry moved to `["Renamed"]` while `store.names()` and `store.activeName()` stayed exactly as already asserted — the inversion the finding describes, where the store stops moving under the mutation and the screen is what moves. Green with the guard intact. Full jest suite green at 500/500; deployed clean to `sfnav-t2`. Original finding: `rememberSaved`'s create-adoption guard is a surviving mutant at HEAD, and the test written to pin it now passes for a new reason. Collapsing `if (!target.layoutId && this.layoutId === undefined)` to `if (!target.layoutId)` leaves all 498 green; the same collapse was already green against `ef9a4ba`, so the `## Deviations` correction bullet's route was not the live one even when it was written — Save is a bare call, so it waits on New layout's `distinct` create and lands as an *update*, and `a Save that lands after New layout does not move the active layout back to the draft it replaced` never reaches the create branch its docstring describes. The guard is still load-bearing, on this pass's own new path: on `a no-row rename that lands after New layout creates its own row rather than losing the rename`, collapsing it moves the switcher's checked entry from "Weekly review" to "Renamed" — a layout the user is not looking at and one the server holds inactive — while `store.names()` and `store.activeName()` stay exactly as that test asserts them. Now that the create's `makeActive` asks the same question the guard asks, the store is the observable that stops moving under the mutation and the screen is the one that does. One added assertion on the switcher's checked entry in that test closes it.
- [x] fixed — unreachable rather than corrected, and the lockout is what closes it. The probe needs three acts made in succession while a no-row user owns no row: rename ("First") with its create held open, New layout ("Weekly review") with *its* create *also* held open while the first is still outstanding, then a second rename ("Second"). `isWriteLocked` forecloses it at the second act: New layout is one of the four writing controls the sixth pass's lockout disables while any of them — including the first rename — has a round trip outstanding, both via the template's `disabled={isWriteLocked}` and via `handleLayoutMenuSelect`'s own handler-side re-check (`(!this.isEditing || this.isWriteLocked)`), so New layout can no longer even be *opened* while the first rename's create is in flight. There is no walkable sequence left in which two of `creatingLayout`'s writers ever hold an entry at the same time, which is the precondition this finding's whole probe depends on.

  Demonstrated three ways, not merely reasoned: (1) every test in the suite that used to walk an overlapping pair of these acts — four in `switching between layouts`, one in `entering and leaving edit mode` — now fails at the "attempt the second act" step against the gated build (`promptInput(element)` returns `null`, or the disabled Save button's click is a no-op), and had to be retargeted to what remains true and reachable; each retargeted test asserts the block directly (`menuItemDisabled`/`.disabled`) before proceeding. (2) Mutation, run after the retargeting: short-circuiting the wait's own guard to `if (false)` in `commitLayoutNow`, collapsing its `resolvedOverride` ternary to a bare `layoutJson`, and collapsing `createNewLayout`'s and `commitLayoutNow`'s own ownership checks to unconditional wipes were each tried in isolation against the full, now-535-test suite — every one leaves it green, confirming no test any longer reaches a state where two writers hold `creatingLayout` at once, which is what this finding's own probe needs to discriminate anything. (3) The handler-side re-check is not only a template attribute a mutation could route around by construction: `handleLayoutMenuSelect` and `handleEditSave` both gate on `this.isWriteLocked` in the handler itself, the same "a template gate is not a guarantee about a handler" reasoning already applied to `handleEditStart` and to Tier 2's own gate — so even a `select` event dispatched directly on the switcher (bypassing whatever `disabled` does in a real browser, exactly as this file's own test helpers do) is refused.

  The mechanism itself — `creatingLayout`, `distinct`, both writers' ownership checks on the clear, and `commitLayoutNow`'s wait-and-recapture — is **not removed**, per `.claude/rules/rstk-preserve-defensive-checks.md` and this slice's own standing treatment of its other unreachable-by-construction guards: it stays as defence in depth behind the lockout, not superseded by it. See the `## Deviations` bullet on the sixth pass for the fuller accounting, including that this closure is not scoped to this one finding — `rememberSaved`'s create-adoption guard and the wait's own re-resolution ternary are surviving mutants across the whole suite now too, for the identical reason.

  Original finding: The `creatingLayout` slot is lossy on the **assignment** as well as on the clear, and a third act inside the window gives a user who owned no row two rows for the one layout they renamed twice. Finding 1 gave both writers an ownership check on the *clear*; neither writer checks anything on the *write*. `createNewLayout` (line 827) and `commitLayoutNow` (line 1917) each overwrite the field unconditionally, so once a `distinct` create has displaced a still-open non-`distinct` one, the create a later overridden write is genuinely addressed to is invisible to it — it reads the `distinct` entry, takes the fall-through, and makes its own row instead of folding. The fifth pass reported the asymmetry and hand-traced two acts, correctly: rename to "Renamed" with its create held open, then New layout "Weekly review", really does end clean — measured, not re-traced: `createLayout` twice, `store.names()` `["Renamed", "Weekly review"]`, `store.activeName()` and the checked entry and a remount all "Weekly review". A **third** act is what breaks it, and no pass has traced one. Measured on a walkable path: no row, enter edit mode, rename to "First" with its create held open, New layout "Weekly review" with its create held open, rename again to "Second" — `createLayout` three times, `store.names()` `["First", "Weekly review", "Second"]`, `store.activeName()` "Weekly review", and the switcher lists all three, so the user owns a row under the very name they renamed away from. The control says what it should have been: the same two renames with New layout taken out from between them give `createLayout` once, `updateLayout` once, `store.names()` `["Second"]` — the fold `creatingLayout`'s own docstring promises, that a non-`distinct` create is "the same implicit, still-rowless layout a later write on this same no-row user is addressed to, so a second such write should wait for it and land as an update of the row it creates". Causality measured rather than reasoned: making `createNewLayout`'s assignment non-clobbering (`if (!this.creatingLayout)`) returns the probe to `["Second", "Weekly review"]` with one update — and leaves all 500 tests green, so nothing in the suite discriminates the assignment in either direction. Worse with a refusal in the middle position: rename "First" (create lands), New layout **refused**, rename "Second" — `createLayout` three times, `store.names()` `["First", "Second"]`, active "First", and the client diverges from itself, the switcher's button label reading "Second" while its checked entry reads "First"; a remount lands on "First", so the user's most recent rename is stranded on an inactive orphan row and criterion 10 is false for them. Slice-introduced, not inherited: `git show ed5e213^` has no `creatingLayout` and no `commitLayoutNow` at all, and before the gate a no-row rename went through the debounce, which coalesces two renames into one write because `scheduleSave` overwrites `pendingSave`. This is the sixth gap in this one mechanism and the fifth to be a neighbour of the last fix, which is the tripwire the engineer set: the slot is a single cell being asked to hold the *set* of creates in flight, and each pass closes the one interleaving it was shown while leaving the next one open. Recommend redesigning the slot rather than guarding the assignment too — `if (!this.creatingLayout)` above is a measurement of causality, not a proposed fix, and it is wrong as one (it would stop a `distinct` create from ever becoming visible to the overridden write that must fall through past it, which is the behaviour Jonah's 2026-08-31 decision asked for).
- [x] fixed — unreachable rather than corrected, and the lockout is what closes it. The probe this finding names needs: a no-row rename's create held open and then refused, New layout's own create started (and held open) *while the rename's is still outstanding*, then Save pressed inside New layout's window. The second step is exactly what `isWriteLocked` forecloses — New layout cannot be attempted at all while the rename's own create has a round trip open, refused or not, because the lockout disables all four writing controls the instant any one of them is outstanding, and re-checks that in the handler (`handleLayoutMenuSelect`) as well as in the template. There is no sequence left in which `commitLayoutNow`'s own-create branch and `createNewLayout`'s create are ever both in flight for the same rowless user, which is the precondition this finding's ownership-check race needs.

  The test this finding's own probe named — `a Save made while a no-row rename's create is open still updates that row after a distinct create it raced was refused` — was walked exactly as written against the gated build and fails at the "select new-layout while the rename's create is open" step (`promptInput(element)` is `null`); it has been retargeted to `New layout and Save are both blocked while a no-row rename's create is open, closing the race finding 2 asked for coverage of`, which asserts the block directly (both controls' `disabled` state) before walking the one write left reachable — Save, pressed again once the lock clears, updating the rename's own row. Re-verified by mutation, run after the retarget: reverting `commitLayoutNow`'s own ownership check to an unconditional wipe (the exact half this finding named) leaves the full 535-test suite green, alongside `createNewLayout`'s check, the wait's own guard, and its `resolvedOverride` ternary — none of the four is discriminated by anything in the suite anymore, because none of the pairings any of them exists to arbitrate can be walked through the UI once a second write cannot be issued while a first is outstanding.

  No production line in `commitLayoutNow` changed for this disposition. The check itself is not removed — it stands exactly as finding 1 left it, as defence in depth behind the lockout per `.claude/rules/rstk-preserve-defensive-checks.md`, on the same footing as this slice's other guards that are correct but currently unproven. See the `## Deviations` bullet on the sixth pass for the fuller accounting: this is one instance of a broader closure, not a fix specific to this one race.

  Original finding: `commitLayoutNow`'s half of finding 1's fix is load-bearing on a walkable path and nothing reaches it, so the pass's "correct-but-unproven" is right about the coverage and understates the guard. Both halves measured: reverting `createNewLayout`'s ownership check alone (`if (true)` for `if (this.creatingLayout === mine)`) reddens the new refused-create test with exactly the finding's numbers — `createLayout` called 3 where 2 is asserted; reverting `commitLayoutNow`'s alone leaves all 500 green, as the pass discloses. The pairing that discriminates it runs the direction the pass named and did not walk, and it does discriminate. Measured: no row, enter edit mode, set a section to six columns, rename to "Renamed" with its own create held open and then **refused**, New layout "Weekly review" with its create held open, release the rename's create as refused, then press Save inside New layout's still-open window. With the check intact — `createLayout` twice, `updateLayout` once, `store.names()` `["Weekly review"]`, active "Weekly review". With it collapsed to an unconditional wipe — `createLayout` three times, `store.names()` `["Weekly review", "Renamed"]`, and the user's six-column Save stranded on a second, inactive row named "Renamed": the same damage finding 1 named, reached through the other writer. Test-only as it stands; no production change follows from this box on its own, and the probe above is the test the fifth pass declined to add. If the box above is taken as a redesign rather than a sixth patch, this probe belongs to that work rather than being written against the current shape.
- [x] false positive — that `endWrite`'s `.finally()` on a second, unreferenced subscriber changes another write's timing or can escape as an unhandled rejection. Both halves checked rather than reasoned. Timing: `this.saveChain` is assigned the *bare* promise (`created`, `renamed`, `deleted`), never the `.finally()` result, so `saveChain`'s identity and settle time are byte-identical to the pre-pass code and every write already chained onto it is untouched; the `.finally()` subscriber is attached first, so `endWrite` runs before any later chainer's callback, and each control's own outcome message is set *inside* the chain (in its `.then()`/`.catch()`), strictly before `endWrite` — which announces nothing, so nothing can be overwritten from that side. Rejection: all three `saveChain` writers terminate in `.catch()`, and `commitLayoutNow`'s two return shapes bottom out in `persist`'s `.catch()` or in `Promise.resolve()`, so the promise the discarded `.finally()` produces can never reject; measured on all four controls with the Apex call refused (Save, no-row rename, with-row rename, delete) and no unhandled rejection is produced. Also measured: routing the `.finally()` *into* `this.saveChain` instead (`this.saveChain = created.finally(...)`, and the same for `deleted`) leaves all 535 green, so the distinction the deviation argues for is currently undiscriminated by the suite — but the argument itself is correct, which is what this box is about.
- [x] false positive — that `writeInFlight` can leak and leave Save permanently disabled. Every one of the five `endWrite()` sites is independently load-bearing under mutation, so no path can silently lose its decrement: deleting `created.finally` reddens 4 tests, `renamed.finally` 1, `deleted.finally` 1, `handleEditSave`'s 1, and the no-row rename's 3. `beginWrite()` sits after every early return in all four acts (`renameCurrentLayout`'s `!name || name === this.layoutName`, `deleteCurrentLayout`'s `!layoutId`, `handleEditSave`'s `!wroteChanges`), so no gesture increments without a matching decrement. Walked, not reasoned, on five settle shapes with the lock's state read back afterwards: Save refused, with-row rename refused, delete refused, no-row rename's create refused, and a Save whose `persist` takes the `stillExists` early return and never calls Apex at all — the lock clears in all five. `Math.max(0, …)` is a surviving mutant, as disclosed, and is genuinely unreachable given the pairing above.
- [x] fixed — no production change; `beginWrite()` was already correct, so this closes as a coverage gap. New test `does not let New layout open while Save's own no-row create is still outstanding, even after re-entering edit mode` (`entering and leaving edit mode` describe) walks exactly the sequence the finding names: no stored row, enter edit mode, add a section, press Save with `createLayout` held open, re-enter edit mode inside that window, then attempt New layout — asserting `menuItemDisabled(element, "new-layout")` is `true`, that the prompt input stays absent, and `createLayout` is still called once. Verified red against the finding's own mutation (deleting `this.beginWrite();` from `handleEditSave`, leaving the handler's own `isWriteLocked` re-check and all three menu bindings in place): `menuItemDisabled` returned `false` where `true` was expected — `Expected: true / Received: false` — the exact accounting the finding gives, `writeInFlight` never leaving zero for Save's own write, so re-entry finds nothing locked. Restored `beginWrite()` and reran; green. Full jest suite green at 536/536 (535 carried over plus this one new test); deployed clean to `sfnav-t2`. Original finding: `beginWrite()` in `handleEditSave` is a surviving mutant, and it is the one line of the lockout with a walkable race behind it. Deleting it — leaving the handler's own `isWriteLocked` re-check and all three menu bindings in place — leaves the full 535 green. Measured on the sequence it protects: a user with no stored row enters edit mode, sets six columns, presses Save (its `createLayout` held open), presses the pencil again — nothing disables the pencil, and Save's own write does not leave edit mode's controls locked in the *new* session — then presses New layout. With `beginWrite()` present, New layout is `disabled`, the handler refuses it, the prompt does not open, and `createLayout` is called once. With it deleted, the prompt opens, the name commits, and `createLayout` is called twice — `["My Navigator", "Weekly review"]` — two rows for a user who owned none, through `createNewLayout`, which never consults `creatingLayout` on the way in. So the pairing this pass's dispositions call foreclosed is foreclosed by exactly this line, and nothing in the suite would notice it going. The test that closes it is the sequence above, asserted at `menuItemDisabled(element, "new-layout")` after the re-entry and at `createLayout.mock.calls.length`.
- [x] fixed — no production change; `EDIT_FOCUS_LEAVE` was already assigned after `EDIT_FOCUS_LOCK` in `handleEditSave`, correctly. One assertion added to the existing focus test (`moves focus to the first revealed control on entry, and back to the edit affordance on the way out`): after its existing entry and Cancel assertions, it re-enters edit mode, makes a canvas change, presses Save, and asserts the last focus move landed on `.rstk-nav-edit`. Verified red under the exact mutation the finding names — hoisting `leaveEditMode()` above the `if (wroteChanges)` block in `handleEditSave`, nothing else changed: the rest of the 202-test file suite stayed green and only this new assertion failed, focus having moved to `.rstk-nav-layout-menu` instead of `.rstk-nav-edit` (the jsdom stub-swap diff in the failure output named the layout menu directly). Restored the original order and reran; green. Full jest suite green at 536/536; deployed clean to `sfnav-t2`. Original finding: Save's focus hand-off is order-dependent, correct today, and unpinned. The deviation names the ordering as load-bearing — `EDIT_FOCUS_LEAVE` assigned after `EDIT_FOCUS_LOCK` in the same handler — and it is: measured, on the real build a Save with changes produces exactly one focus move, to `.rstk-nav-edit`; with the two statements reordered (`leaveEditMode()` hoisted above `beginWrite()`, nothing else changed) it produces exactly one focus move, to `.rstk-nav-layout-menu`, and all 535 tests stay green. No test drives focus through Save at all: `moves focus to the first revealed control on entry, and back to the edit affordance on the way out` leaves through `cancelEdits`, and `ends any in-flight keyboard grab when edit mode ends` does too. The switcher itself is a sane target when the lock does own the transition — it renders under `canEdit`, which edit mode requires, and it is never one of the disabled four — so this is a coverage finding, not a behaviour one. One assertion on `saveEdits` in the existing focus test closes it.
- [ ] Save's `WRITE_LOCK_ANNOUNCEMENT` never reaches the live region, and the deviation's stated reason why the announcement is safe is wrong for Save specifically. `announce()` sets one reactive property, so two calls in one tick render only the last; `handleEditSave` calls `beginWrite()` (which announces the busy message) and then, *synchronously in the same handler*, `this.announce(SAVE_EDIT_ANNOUNCEMENT)`. Measured with the `updateLayout` held open: mid-flight the region reads `"Changes saved. Edit mode off."`, never the busy sentence. The deviation's claim — "it always fires synchronously, strictly before the async outcome it will be superseded by, for all four controls" — holds for New/Rename/Delete, whose outcomes are set inside a `.then()`, and is false for Save, whose outcome is not async at all. The behaviour is arguably the right one (Save leaves edit mode, so none of the four named controls is even rendered to be described as unavailable) but it is reached by accident, and it means the deviation's justification cannot be relied on by whoever reads it next. Not a nonce hazard: measured across two consecutive Saves-with-changes, the rendered strings still differ, because `handleEditStart`'s own announcement between them restores the parity `beginWrite` disturbs.
- [ ] The announce-on-engage-only decision costs a screen-reader user the one case where the lock is actually confusing, and that case is not the one the reasoning considers. The reasoning against announcing on *clear* is sound and I verified its premise: each control's outcome message is set inside the same chain `endWrite`'s `.finally()` is attached to, and `endWrite` runs first, so a generic "available again" fired from there would be superseded rather than superseding — it would be inaudible, not destructive, but adding it buys nothing. What the asymmetry does not cover: Save is the only one of the four that leaves edit mode while its own write is outstanding, and the pencil is not disabled, so the user can re-enter edit mode inside that window. Measured: on re-entry Save and all three menu entries are `disabled`, and the live region holds `"Edit mode on. Customise your Navigator, then press Save."` — the busy sentence was erased in the previous tick (finding above) and nothing announces it again on entry, so a screen-reader user is in a fresh edit session in which the primary action is disabled and has been told nothing about it at any point. This is the one place the missing clear-announcement would not have helped either; what would is announcing on *entry into a locked mode*, or not letting Save's own lock outlive its session. Recording it as the accessibility question the test-wording collision hid rather than settled.
- [x] fixed — `handleSectionAddItems` now captures `this.editSnapshot` at the moment it opens the picker (`const session = this.editSnapshot;`) and passes it through to `addChosenItem`, which refuses unless `this.isEditing && this.editSnapshot === session` — a session-identity check, not the bare `!this.isEditing` return this box itself flagged as insufficient. Chose this shape over closing the picker from `leaveEditMode()` (the `closePrompt()` precedent the design points to): `lightning/modal`'s `.open()` returns only a promise, never a component handle the caller could call `.close()` on, so closing it from the parent would mean adding a new externally-triggered close path to `NavigatorItemPicker` itself — outside this slice's `touches:` — or reaching across the shadow boundary with `document.querySelector`, which `rstk-lwc-standards.md` forbids. The bare-check gap this box named is real and distinct from the Cancel-while-open route it walked: Cancel ends a session, but re-entering starts a *new* one with a *new* `editSnapshot` object while `isEditing` reads `true` throughout, so a bare mode check would let a stale, already-discarded choice land on a session the user never made it in. Mutation-confirmed both halves discriminate: dropping the whole guard reddens two new tests (`does not write when a chosen item resolves after Cancel has ended the session that opened the picker`, `does not let a stale picker's choice land on a different, later edit session`); keeping `!this.isEditing` alone and dropping only the session comparison leaves the first green and reddens only the second — proving the session check closes a gap the bare mode check does not. Original finding: `handleSectionAddItems` is correctly gated — the section's "Add items" button renders under `lwc:if={editing}` — but the write it leads to is applied when `NavigatorItemPicker.open()` *resolves*, and `addChosenItem` guarded `isAttached` and `tabId` and not the mode. Walked end to end: enter edit mode, press "Add items", press Cancel while the picker is still open, then choose an entry — `applyLayout` ran with `isEditing` now false, `scheduleSave` armed the debounce, and one `updateLayout` landed carrying the added item. Reachability rests on `lightning/modal` being a real modal in a running org, a platform guarantee this repo asserts nowhere; this suite's own mock mounts the real picker component, so the choice really does land in the test the way the finding describes.
- [ ] `handleEditSave` and `handleLayoutMenuSelect` are not the only handler-side entry points into the four writing acts, only the only two that are guarded. `handleLayoutNameCommit` (bound twice, `oncommit` on the input and `onclick` on the prompt's own button) calls `createNewLayout` and `renameCurrentLayout` directly, and `handleLayoutDeleteConfirm` calls `deleteCurrentLayout` directly; neither re-checks `isWriteLocked`. Verified unreachable while locked rather than taken on trust: the prompt can only be opened by the already-guarded `handleLayoutMenuSelect`, both commit handlers call `closePrompt()` before dispatching so a double-commit cannot fire, and the only act that can engage the lock while a prompt stands open is Save — which reaches `leaveEditMode()` → `closePrompt()` in the same handler, measured (`promptInput(element)` is `null` immediately after). So the guard is genuinely belt-and-braces today. But the fact that makes these two handlers safe lives in a third method, which is the exact shape this slice keeps `disconnectedCallback`'s `isEditing` branch and `handleEditCancel`'s `discardPendingSave()` for, and here there is no guard and no comment saying why one is not needed. Also worth recording against `isWriteLocked`'s docstring, which claims "a *second* layout operation cannot be issued inside the first's round trip": Tier 3 is deliberately ungated on the lock, and `switchToLayout`'s `activateLayout` is a server-side layout mutation that *can* be issued inside any of the four's round trips — measured, and harmless because it chains behind them on `saveChain`, but the sentence overstates what is true.
- [x] fixed — the verdict's own minimum-fix list is closed: (1) the `beginWrite()` test above; (2) the Save-focus assertion above; (3) one sentence added *in the code*, at each of the three preserved expressions the deviation names — `commitLayoutNow`'s wait-and-recapture guard (`if (!this.layoutId && this.creatingLayout)`), its `resolvedOverride` ternary, and `rememberSaved`'s create-adoption guard (`if (!target.layoutId && this.layoutId === undefined)`) — saying, in the code rather than only in this file, that no test in the suite discriminates it, that `isWriteLocked` is what stands in front of the race it protects against, and that it is preserved per `.claude/rules/rstk-preserve-defensive-checks.md`. None of the three is claimed dead by construction — each comment says only that it is unproven by the current suite, per the task's own instruction and this slice's history of two false "unreachable by construction" claims. Re-verified by mutation after each comment was written, so the claim the comment makes is the one actually checked: short-circuiting the wait guard to `if (false)`, collapsing `resolvedOverride` to a bare `layoutJson`, and collapsing `rememberSaved`'s guard to `if (!target.layoutId)` each still leave the full 536-test suite green (535 carried over plus the `beginWrite()` test), restored after each check. `Math.max(0, …)` in `endWrite` — the verdict's fourth disclosed survivor — is not one of the three the task named for a code comment and is untouched here; it already has its own disposition two boxes above. Full jest suite green at 536/536; deployed clean to `sfnav-t2`. Original finding: Verdict on the untested backstop, since the pass asks for one: it is not a backstop in the state it is in, and the fix is not to re-test the races. Independently reproduced all four disclosed survivors against the full suite — `if (false && …)` on `commitLayoutNow`'s wait, `resolvedOverride` collapsed to a bare `layoutJson`, `rememberSaved`'s guard collapsed to `if (!target.layoutId)`, and `Math.max(0, …)` — and all four leave 535 green, so the disclosure is accurate. The problem is not that they are unpinned; it is that the *reason* they are unpinned is an argument, and this pass's own version of that argument is falsified twice above: the primary guard that forecloses the race is itself a surviving mutant on its most important line (`beginWrite()` in `handleEditSave`), and the foreclosure is not total, because `addChosenItem` can still arm a debounce out of edit mode and `handleEditStart`'s flush reaches `createLayout` without ever touching `creatingLayout`. An untested guard whose justification is a *measured* fact about the code survives a mutation sweep, because the sweeper can re-measure it; an untested guard whose justification is an enumeration in a slice document does not, because the sweeper reads the file. Minimum that would fix this without re-testing anything the lockout forecloses: (1) the `beginWrite()` test in the finding above, which pins the live guard on a walkable path and costs one test; (2) the Save-focus assertion, one line in an existing test; (3) at each of the three preserved expressions, one sentence *in the code* saying that no test in the suite discriminates it, that the lockout is what stands in front of it, and that it is preserved under `rstk-preserve-defensive-checks.md` — the comments today say the third and imply the second and say nothing about the first, which is the one a mutation sweep needs to read. The general lesson — that "preserved defensive check" and "surviving mutant" are the same observation seen from two ends, and only the code can carry the distinction — belongs in `.claude/rules` at Learn, not here.
- [ ] All three new code comments state the wrong suite size, and that number is the one claim in them a mutation sweeper checks first. Each says the mutation it names "leaves all 535 tests green"; the suite this same commit ships is 536, because it also adds the `beginWrite()` test. Re-ran all three against the committed tree, restoring the file after each: `if (false)` on `commitLayoutNow`'s `if (!this.layoutId && this.creatingLayout)` — 536 green; `const resolvedOverride = layoutJson;` collapsing the ternary — 536 green; `if (!target.layoutId)` on `rememberSaved`'s guard — 536 green. Everything else about the three comments holds and was checked independently, not taken on the pass's word: the "no test discriminates this" claim is true in all three cases, and each stated reason is accurate too — `creatingLayout` has exactly two writers (`createNewLayout`, and `commitLayoutNow`'s own create branch), `commitLayoutNow` has exactly two external callers (`handleEditSave`, `renameCurrentLayout`'s no-row branch), all four sit behind `beginWrite`/`endWrite` spanning the whole create round trip and behind the two handler-side `isWriteLocked` re-checks, and the only other assignments to `this.layoutId` that could reach `rememberSaved`'s guard (`adoptFromStore` via `switchToLayout`, `adoptActiveLayout` via the load path) cannot land inside a create's window — both queue on `saveChain` behind that create's own `persist`, or run before anything is interactive. So the reasoning is sound and only the arithmetic is wrong, which is the worst place for it: a sweeper who runs the stated mutation and reads 536 has no way to tell a stale comment from a wrong one. The slice's own disposition three boxes above already says 536 ("535 carried over plus the `beginWrite()` test"), so the comments disagree with the file describing them rather than with an open question. One digit in each of three comments; nothing else about them should change.
