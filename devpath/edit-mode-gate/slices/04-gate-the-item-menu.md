---
depends_on:
  - devpath/edit-mode-gate/slices/03-gate-the-section-header-controls.md
done: true
fix_cycles: 1
touches:
  - force-app/main/default/lwc/navigatorSection/navigatorSection.html
  - force-app/main/default/lwc/navigatorItem/navigatorItem.js
  - force-app/main/default/lwc/navigatorItem/navigatorItem.html
  - force-app/main/default/lwc/navigatorItem/__tests__/navigatorItem.test.js
---

# Out of edit mode an item is a link and nothing else

> Watch a new test go red before you make it green.
> _Why: a test written before the code cannot be written to hit a coverage number — there is nothing to
> cover yet. Retires when Apex gets mutation testing._

## What to build

Out of edit mode a navigation item shows only its label and icon as a working link; the per-item overflow
menu — rename, remove, move to another section — appears only once the user enters edit mode.

## Acceptance criteria

- [x] met Out of edit mode an item renders no overflow menu, so renaming it, removing it and moving it to another section are all unreachable.
- [x] met In edit mode the menu is present and its entries behave exactly as they do today, including "Move to…" appearing only when there is somewhere to move to.
- [x] met The menu is absent from the DOM rather than hidden by CSS.
- [x] met The item's link still renders, is still clickable and still navigates to the right place out of edit mode.
- [x] met An item takes the mode as an `@api editing` property passed down by its section, and renders correctly for both values of it when mounted on its own.

## Deviations
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js, +1726 -185 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. This slice's fix pass touched the file again, so this is the current whole-branch figure and it supersedes the +1704 -188 on the earlier box for the same path; both stay open and both are closed at review. The fix pass's own share is finding 1's restored pin at the org-tab-label test and three corrected comments.
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/navigatorSection/navigatorSection.js, +48 -3 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. Almost all of that figure is slice 03, which owns this file; this slice's own share is one doc comment recording that the section passes `editing` through to each item.
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js, +1704 -188 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. Cumulative across slices 01, 03 and 04 — nine prior commits have rewritten this suite. This slice's own share is the fourteen save-chain tests re-vehicled onto a direct `itemrename` dispatch, plus explicit `enterEditMode` calls added to 26 tests across two behavioural describes that drove the item menu without it.

- The fourteen `renameFirstItem`/`burstItemRename`-driven tests in `salesforceNavigator.test.js` (their real subject is the save chain — debounce, leading-edge capture, coalescing, create/update id handling, layout-switch races) keep their vehicle but change how it fires: `renameFirstItem` now dispatches the `itemrename` `CustomEvent` directly on the `c-navigator-item` element instead of opening its (now edit-mode-only) menu and typing into the revealed input. This works unchanged in every fixture the fourteen use, including the two `canEdit`-false ones, because it never touches the item's own rendered UI — only the event the section's and the page's listeners already respond to regardless of what produced it — and it keeps every one of the fourteen running *out* of edit mode, which is the only state `scheduleSave` arms in at all. Verified by mutation: deleting `scheduleSave`'s `hasLayoutLoadError` early return reddened both `canEdit`-false tests with the new helper in place.
- Gating the item's menu broke far more than the fourteen: two whole behavioural describes in `salesforceNavigator.test.js` — "moving an item into another section" (11 of its 18 tests, the ones driven through the item's own Move to… menu rather than drag) and "renaming an item" (12 of its 15 tests) — plus 3 tests in "removing an item and adding it back", all drove the item's menu directly and had no `enterEditMode` call. Fixed by adding `await enterEditMode(element);` at each affected call site (or, for "renaming an item", baking it into that describe's own local `navigatorOn` mount helper, since every test in that describe needs the menu at some point and none of them cares about being out of edit mode). Left the "moving" describe's shared `navigatorWithTwoSections`/`navigatorWithAWithdrawnTab` helpers alone rather than baking `enterEditMode` into them, because they are also the mount helpers for that describe's drag tests, which must stay out of edit mode (drag surfaces are slice 05's).
- Everywhere one of the above tests also asserted a persisted payload (`updateLayout`/`createLayout` calls, `savedIds`/`savedItems`/`lastSavedLayout`, or a remount on a written payload), `settleAutosave()` was replaced with `saveEdits(element)` — autosave is suppressed by `scheduleSave`'s `isEditing` guard, so the debounce cannot write in edit mode; an explicit Save is the only route left to persist a real change made there. One exception: "creates no layout row when an empty box is committed on an item with no rename" in "renaming an item" was *not* moved into edit mode like its neighbours, and was rewritten to dispatch `itemrename` directly (the same technique as `renameFirstItem`) and to keep asserting via `settleAutosave()` out of edit mode. The guard it pins (`handleItemRename`'s payload-equality check, `salesforceNavigator.js:1425`) leaves the serialised layout byte-identical for a no-op, so `handleEditSave`'s own `hasUnsavedCanvasChanges` check would report "nothing to write" whether or not that guard exists — pressing Save there would have been exactly the vacuous-pass shape trap 273 warns about, proven by mutation (see below).
- **Correcting the record inherited from slice 03's finding 5, on a fix pass over the critique below.** What can still write out of edit mode after this slice is four surfaces, not one. The section card's pointer drag and keyboard reorder are live — that half of the record was already correct — but so are the item's own: `navigatorItem`'s anchor is still `draggable="true"` out of edit mode and its `handleDragKeydown` is still ungated, and each of the four is a route to a full `updateLayout` (or, for a user who owns no row, a `createLayout`) with no Save pressed. The suite already proves all four green, out of edit mode, against the payload actually written: the item's pointer drag in "still shows the moved item in its new position after a reload" (`salesforceNavigator.test.js:1574`, remounts on `createLayout`/`updateLayout`'s captured `layoutJson`); the item's keyboard reorder in "still shows a keyboard-moved item in its new position after a reload" (`:1604`, Space-arrow-Space, same remount-on-payload technique) and in "reorders the item the user picked up when an earlier one is out of reach" (`:2285`, asserts `savedIds(updateLayout, 0)`); and the section card's pointer drag and keyboard reorder in "still shows the reordered sections after a reload" (`:1766`) and "still shows keyboard-reordered sections after a reload" (`:1790`). None of the four is this slice's to gate — `draggable`, `tabindex`, `handleDragKeydown` and `handleCardKeydown` on both components are slice 05's, per `## Design`'s drag-surfaces exception — but slice 05 needs the count right before it starts: it is gating four routes across two components, not one route on one.

- Fixing finding 3 below turned up a third instance of the same inaccurate comment beyond the two the finding names: "a change still in the debounce is written to the layout it was made on, not to the one switched to" (`salesforceNavigator.test.js:4342`, in "switching between layouts") carries the identical "an item rename is not [gated]" sentence, for the identical reason (`renameFirstItem`'s direct dispatch, and this test never enters edit mode). Corrected it alongside the two the finding cites rather than leaving a known-stale copy in place, per `rstk-preserve-documentation.md`.

## Critique findings

- [x] false positive — the direct `itemrename` dispatch is faithful, and the detail it synthesises is exactly what `navigatorItem` sends. The child dispatches `{ index: this.index, rename }` from `handleRenameCommit` (`navigatorItem.js:363`) on a plain `new CustomEvent(name, { detail })` — non-bubbling, non-composed, two primitives; the helper dispatches `{ index: 0, rename: value }` on the same host with the same constructor, and it always targets the *first* `c-navigator-item` in the section, so the hard-coded `0` is that item's real `index`. Both sides are primitives-only per `rstk-lwc-standards.md`. The event lands on the section's `onitemrename` listener exactly as the real one does, so `navigatorSection.handleItemRename`'s `sectionIndex` relay (`navigatorSection.js:606-612`) is still in the path and still pinned — hard-coding `sectionIndex: 0` there reddens "renames an item in the second section as readily as in the first", and hard-coding `index: 0` reddens four more. Nor does any of the 24 call sites synthesise an event the child would have swallowed: `handleRenameCommit` refuses a rename equal to the current label, and every value passed is distinct from it. The suite already had the precedent one level up — "writes nothing when a removal names no item on screen" dispatches `itemremove` straight at the section. What the fourteen no longer prove is any user path, and that is correct rather than a loss: out of edit mode no user path exists, which is the whole point of the slice, and the helper's own doc comment says so in as many words ("pinning the debounce as the backstop `## Design` calls it ... rather than as a route a user can still reach today"). The two `hasLayoutLoadError` fixtures are labelled at their call sites too. No change wanted.
- [x] false positive — the special-cased test and the masking claim behind it both hold, checked by construction rather than by reading. Deleting `handleItemRename`'s payload-equality guard (`salesforceNavigator.js:1425-1427`) reddens exactly one test in 511: "creates no layout row when an empty box is committed on an item with no rename". Then the masking claim, tested properly: rewriting that test to the shape its neighbours took — `navigatorOn(undefined, [ACCOUNT_ITEM])` plus `renameTo` plus `saveEdits`, with the announcement assertion relaxed to allow the "Edit mode off" sentence — passes with the guard present *and* passes with the guard deleted. So folding it in with the rest would have left the only pin on that guard vacuous, exactly as the slice says, and dispatching directly out of edit mode is the right and necessary exception. The empty-string rename it synthesises is one the real child does send: `startRename` seeds the draft with the label, clearing the box makes `rename` differ from the label, and the commit fires.
- [x] false positive — trap 273 was run, not inspected, and both mutations the slice records reproduce exactly. Deleting `scheduleSave`'s `hasLayoutLoadError` early return (`salesforceNavigator.js:1929-1931`) reddens precisely the two `canEdit`-false tests and nothing else, with the new helper in place — so the mutation the retarget dropped was kept beside the new control-absence assertion rather than in place of it, which is what the trap asks for. Six more run against the guards this slice moves: `scheduleSave`'s `isEditing` return reddens 4; `handleEditSave`'s `hasUnsavedCanvasChanges` conditional reddens 3; `handleItemRemove`'s payload-equality guard still reddens its one test (slice 03's finding 2 fix survives the fold); `handleItemRename`'s label-equality guard in the child reddens 1; `renameItem`'s platform-label clear reddens 2; the section's two relay fields redden 1 and 5. No discrepancy with anything the slice claims.
- [x] false positive — the substitute for a red-first history on the presence assertions is adequate, and no presence assertion passes on a broken gate. Four ways of breaking it, all caught. Ungating the menu outright (`lwc:if` on an always-true getter) reddens three of the five new tests plus both `canEdit`-false page tests. Gating it on an always-false getter reddens the other two, including "renders the full overflow menu ... in edit mode". Deleting `editing={editing}` from `navigatorSection.html` reddens 30. Passing an always-true value through it instead reddens 2 — and those 2 are the page-level assertions this slice itself added at `:650` and `:787`, so they are load-bearing rather than decoration. Deleting `if (!value) { this.isRenaming = false; }` from the setter reddens the transition test. The gate is asserted as DOM absence (`querySelector("lightning-button-menu")` is null), which is the only shape `lwc-jest-ceilings.md` allows and satisfies criterion 3 on its own terms; a half-gate that kept the button and dropped the entries would fail the same query.
- [x] false positive — scope, criterion 4 and the drag preconditions all check out. `git diff 441bf37..d379977` contains not one added or removed line matching `draggable`, `tabindex`, `handleDragKeydown` or `handleCardKeydown`, and every hunk in `navigatorItem.test.js` starts at line 774 or later, so `:296-300` (`anchor.draggable === true`) and `:640-659` (draggable regardless of `grabbed`) are untouched and green inside the 511. `handleClick` is byte-identical — the only production change to `navigatorItem.js` is the `editing` accessor pair and its doc comment. The link is still pinned out of edit mode in three places: the new gate test asserts the anchor, its `href` and a real navigation with the menu absent; the pre-existing `it.each` over `ctrlKey`/`metaKey`/`shiftKey` mounts with `editing` unset and asserts `defaultPrevented === false` with no `Navigate` call, which is what `lwc-navigation-links.md` binds; and `salesforceNavigator.test.js:368-383` clicks an item anchor on an ungated mount and asserts the page reference end to end. Lint and prettier are clean and the tree is clean at `d379977`.
- [x] false positive — the census of all three describes comes back clean apart from the one finding below, and every folded test still fails when the thing it names breaks. "Moving an item into another section": 18 tests, 11 now in edit mode and 7 left out of it, and the split is exactly right — the 7 are the pointer-drag and keyboard-grab tests plus the two `itemDragActive` plumbing tests, none of which touch the menu. Of the 11, hard-coding `handleItemMoveTo`'s `toSection` to 0 reddens 8; the 3 it does not touch are the two menu-content tests, which redden when a section is offered on its own items' menus, and "moves an item out of the second section as readily as out of the first", whose destination already is 0. "Renaming an item": 15 tests, 14 folded into edit mode by the local `navigatorOn` and 1 held out deliberately; the four relay- and model-level mutations above distribute across 9 of them, and the rest are announcement or label assertions that redden under the menu mutations. "Removing an item and adding it back": exactly 3 gained `enterEditMode` — the three that drive the item's own Remove entry — and "writes nothing when a removal names no item on screen" correctly stayed out of edit mode and still reddens on its own guard. Nothing vacuous of slice 03's shape was found in any of the three.
- [x] fixed — restored `:2950` ("picks up a change to the org's own tab label, with no write at all") to its pre-slice-04 shape: mounted inline (`getLayouts.mockResolvedValue`, `createNavigator`, `getNavItems.emit`, `flush`, with no `enterEditMode` call, since this describe's shared `navigatorOn` now enters edit mode as part of mounting) and asserted via `settleAutosave()` in place of `saveEdits(element)` — the same exception `:2894` already carries. Verified by mutation, not inspection: added `if (isRedelivery) { this.scheduleSave(); }` to `wiredNavItems` in `salesforceNavigator.js`, guarded on `this.items` already being non-empty so only a *redelivery* arms it, and only this one test reddened, with "Expected number of calls: 0, Received number of calls: 1" on `updateLayout` — exactly the shape the finding describes. Reverted the mutation; the test is green again and the production diff is clean. Re-ran the `saveEdits` + `not.toHaveBeenCalled` scan myself rather than trusting the critic's count: a text-level scan turns up six, not five — `:2950` (just restored) plus `:4571`, `:5651`, `:5806`, `:5950` and `:5993`, all five of the latter inside "switching between layouts" or "entering and leaving edit mode" (slice 01's own Save/Cancel machinery, never touched by this slice's edit-mode retrofit). Read each: `:4571` and `:5651` are genuine "Save writes nothing" outcomes (`stillExists`, `hasUnsavedCanvasChanges`); `:5806` asserts an intermediate non-call inside a coalescing-create race, not a final "nothing written" claim; `:5950` asserts `renameLayout` (a Tier 2 act) was not called, not a canvas-write claim; `:5993` is a genuine "Save writes nothing, and says so" outcome. None is vacuous or broken by this slice's changes.
- [x] fixed — corrected the record rather than the code, per the finding's own instruction: no production change, since all four surfaces (`navigatorSection`'s and `navigatorItem`'s `draggable`, `tabindex`, `handleDragKeydown` and `handleCardKeydown`) are slice 05's to gate. Added the `## Deviations` bullet above ("Correcting the record inherited from slice 03's finding 5...") naming all four surfaces and the test proving each, so slice 05 starts from an accurate inventory instead of the one-surface count slice 03's finding 5 handed down.
- [x] fixed — updated rather than deleted, per `rstk-preserve-documentation.md`. At `:5605-5608` (now shifted; the test is "commits a change made before edit mode was entered, so the snapshot is what is stored"), rewrote the comment to state that item rename *is* gated as of this slice, name `renameFirstItem`'s direct-dispatch technique as the reason the first (out-of-edit-mode) call still stands in for the scenario, and cover the second (in-edit-mode) call as the faithful stand-in for a real gesture that it now is — so the one comment covers both uses instead of neither. Fixed the "related and smaller" issue too: the `navigatorOn` header comment in "renaming an item" (`:2702-2709`) no longer claims "every test in this describe drives that menu at some point" — it now says "most", and names the three that do not (`:2764`, `:2950`, `:2975`) plus the redelivery exception's own inline mount. Also fixed a third instance of the same stale sentence the finding did not cite by number but that carries the identical defect: "a change still in the debounce is written to the layout it was made on, not to the one switched to" (`:4342` in "switching between layouts") — see the `## Deviations` bullet on this.
- [ ] The corrected `navigatorOn` header comment is still wrong, and restoring the redelivery test falsified a neighbour the pass did not revisit. "Renaming an item" holds 15 tests and **four** of them never touch the menu, not three: `:2768` (navigation/rename-target), `:2898` (the empty-box exception), `:2954` (the redelivery exception) and `:2992` (no-other-layout). The comment at `:2702-2713` names three, omits `:2898` — the one this slice's own `## Deviations` bullet singles out as the deliberate out-of-edit-mode exception — and then contradicts itself inside one sentence: it lists "the org-tab-relabel test" among those "not disturbed by mounting this way" and immediately names "the redelivery test" as "the one exception [that] mounts inline instead", and those are the same test, `:2954`. Two tests in the describe mount inline out of edit mode, not one. Restoring `:2954` also made a *different* comment false: `:2906`, the empty-box test's own header, still reads "Runs out of edit mode, unlike every other test in this describe", which stopped being true the moment `:2954` went back out of edit mode. Documentation only — nothing here is vacuous and no criterion turns on it, and every gate mutation reddens as the pass claims — but this is the second comment in this describe left carrying a false uniqueness claim, and it is the same defect class finding 3 was raised for and reported closed. Name all four non-menu tests, drop "the one exception", and re-word `:2906` from "every other test" to "most".
- [x] false positive — no longer true, verified on current code: `navigatorItem.html:29` binds `draggable={editing}`, and both `handleDragOver` (`navigatorItem.js:450`) and `handleDrop` (`:473`) carry an `editing` guard, so an item cannot be dragged onto another section out of edit mode; slice 05's keyboard gate closes the other route. Criterion 1's wording is accurate as of slice 05. It did overclaim when this box was written, against the build that then existed. Closed by Jonah, 2026-09-01. Original finding: Criterion 1 is still ticked and still overclaims — the half of the previous finding 2 the fix pass did not close. "Out of edit mode an item renders no overflow menu, so renaming it, removing it and moving it to another section are all unreachable" is true of renaming and removing, since the menu is the only route to either, and false of moving to another section: an item dragged onto another section's item still crosses the boundary out of edit mode, green today in "moves a dragged item into the section it was dropped on, not the card it was dropped on" (`salesforceNavigator.test.js:2096`, mounted through `navigatorWithSections` at `:1730`, which never enters edit mode), and the same route reaches a written payload out of edit mode at `:1574`. The fix pass's four-surface `## Deviations` bullet documents that route and is right about it; it just never came back to the criterion the previous critic named in the same breath ("criterion 1 as written ... is true of the menu's three acts only"). The gate itself is sound — ungating the menu reddens 5 tests, gating it always-false reddens 56 including "offers nowhere to move to when there is nowhere to move to" — so this is the criterion's wording rather than the code: qualify it to the menu's own three acts and hand the drag route to slice 05, rather than un-tick it. Left open because a ticked criterion's wording is a human's to settle at review.
