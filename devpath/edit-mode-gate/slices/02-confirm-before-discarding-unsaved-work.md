---
done: true
fix_cycles: 3
depends_on:
  - devpath/edit-mode-gate/slices/01-enter-and-leave-edit-mode.md
touches:
  - force-app/main/default/lwc/salesforceNavigator/salesforceNavigator.js
  - force-app/main/default/lwc/salesforceNavigator/salesforceNavigator.html
  - force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js
---

# Unsaved work is not thrown away without asking

> Watch a new test go red before you make it green.
> _Why: a test written before the code cannot be written to hit a coverage number — there is nothing to
> cover yet. Retires when Apex gets mutation testing._

## What to build

A user who cancels edit mode, or switches to a different saved layout while editing, is asked to confirm
first whenever there is unsaved work to lose — and is not asked when there is nothing to lose.

## Acceptance criteria

- [x] met Cancel on an edit session in which nothing was changed closes edit mode without asking anything. Proved by `closes edit mode without asking when Cancel is pressed on an untouched session` (`salesforceNavigator.test.js`, `entering and leaving edit mode`). Guard: `handleEditCancel`'s `if (this.hasUnsavedCanvasChanges)`, forced to `if (true)` — reddened (the prompt appeared where it should not have); restored, green.
- [x] met Cancel on an edit session with unsaved canvas changes asks the user to confirm before discarding. Proved by `asks before discarding when Cancel is pressed with unsaved canvas changes, and declining leaves the user editing with them intact`. Same guard, forced to `if (false)` — reddened (no `[role="alertdialog"]` appeared); restored, green.
- [x] met Confirming discards the changes and leaves edit mode; declining leaves the user in edit mode with the changes still on screen. Confirming proved by `confirming the discard prompt on Cancel throws the unsaved changes away and leaves edit mode` — guard: `handleDiscardConfirm`'s `action.type === DISCARD_CANCEL` branch, forced to `if (false)` — reddened (the section added before Cancel survived); restored, green. Declining proved by the same test as the criterion above, which presses "Keep editing" and asserts the change and the Save button are both still there.
- [x] met Selecting a different saved layout while editing with unsaved changes asks the same question, by the same route. Proved by `asks before discarding when switching layouts while editing with unsaved changes, and declining stays on the layout being edited` and `confirming the discard prompt on a layout switch throws the draft away and switches, by the same route Cancel uses` (`switching between layouts` describe). Guard: the Tier 3 branch's `if (this.isEditing && this.hasUnsavedCanvasChanges)`, forced both `false` (reddened: no dialog, immediate switch) and `true` (reddened: `switches immediately, without asking, when editing with nothing unsaved` broke); restored, green both times. Widened to New layout and Delete layout too — see `## Deviations`.
- [x] met Whether there is anything to lose is decided by comparing the serialised current layout against the serialised snapshot taken on entry, so the question is asked exactly when a write would differ. This is `hasUnsavedCanvasChanges`, unchanged from slice 01 and reused rather than reimplemented: `serializeLayout(this.layout) !== this.editSnapshot.json`. Proved by `closes without asking when a change is undone back to exactly its entry value` (columns set to 6 then back to 2, Cancel closes silently) — mutating the getter to `return true;` (a stand-in for a dirty-flag protocol that never un-sets on an exact revert) reddened that test *and* `closes edit mode without asking when Cancel is pressed on an untouched session` *and* `switches immediately, without asking, when editing with nothing unsaved`; restored, all green.
- [x] met The confirmation reuses the component's existing inline prompt rather than adding a third dialog mechanism beside the prompt and the item picker's modal. `PROMPT_DISCARD` is one more value of the same `layoutPrompt` field `PROMPT_NEW`/`PROMPT_RENAME`/`PROMPT_DELETE` already use, opened via the existing `openPrompt`/closed via the existing `closePrompt`, rendered in the same `lwc:if`/`lwc:elseif` chain inside the same `.rstk-nav-layout-prompt` container `PROMPT_DELETE` already renders in (input-less, `role="alertdialog"`, borrowed verbatim). No `LightningConfirm` or any other dialog import exists anywhere in `salesforceNavigator.js` — verified by grep, checked as part of this pass.

## Deviations

- **Widened from two call sites to four, per the orchestrator's instruction.** The design's own text and this slice's original criteria name only Cancel and switching layouts. Slice 01's critique left an open box (`- [ ] Creating a new layout, or deleting one, inside an edit session discards the unsaved Tier 1 draft without asking...`) naming New layout and Delete layout as a third and fourth call site for the identical hazard — each replaces the canvas wholesale (`createNewLayout` via `buildSeededLayout`, `deleteCurrentLayout` via `adoptFromStore`) and re-takes the entry snapshot (`resnapshotEdit`), so an unsaved Tier 1 draft is simply gone with nothing left to Cancel back to. Built the whole set: `handleLayoutNameCommit`'s `PROMPT_NEW` branch and `handleLayoutDeleteConfirm` both now check `hasUnsavedCanvasChanges` and open the same shared prompt before proceeding. One path (`openDiscardPrompt`/`handleDiscardConfirm`/`PROMPT_DISCARD`), four call sites (`DISCARD_CANCEL`, `DISCARD_SWITCH`, and `NEW_LAYOUT`/`DELETE_LAYOUT` reused as the action tag rather than a second pair of near-identical constants).
- **`pendingDiscardAction` is a small tagged object (`{ type, ... }`), not four booleans or four prompt values.** One field, matching the shape `layoutPrompt` itself already uses ("one field rather than four booleans so two cannot be open at once") and `creatingLayout`'s own precedent for carrying identity (`{ promise, distinct }`) rather than adding a parallel field per call site. `DISCARD_SWITCH` carries `layoutId`; `NEW_LAYOUT` carries the `name` the user had already typed in the naming prompt before the discard prompt intervened, so confirming does not lose it.
- **The four call sites check `hasUnsavedCanvasChanges` at their own handler, not inside a shared entry-point wrapper.** A 2-3 line `if (hasUnsavedCanvasChanges) { openDiscardPrompt(...); return; }` is repeated at `handleEditCancel`, the Tier 3 branch of `handleLayoutMenuSelect`, `handleLayoutNameCommit`, and `handleLayoutDeleteConfirm`. This mirrors the codebase's own established pattern for "one thing done at each of the four writing entry points" (`beginWrite()`/`endWrite()`, called the same way at the same four kinds of site) rather than a DRY violation — the actual shared logic (`openDiscardPrompt`, `handleDiscardConfirm`, the prompt markup) lives in exactly one place each.
- **One shared message (`DISCARD_PROMPT_MESSAGE`), not four call-site-specific sentences.** `layoutDeleteMessage` names the layout being deleted because that is the one detail a bare "Delete?" would leave out; the discard prompt does not need call-site-specific wording to be equally informative, because each call site's own control (Cancel, the layout switcher, New layout, Delete layout) already says what happens once the draft is gone. Kept the surface small rather than branching on `pendingDiscardAction.type` for a sentence, per `rstk-complexity-guard.md`.
- **The picker fix (inherited item 2): `addChosenItem` refuses when the session has changed, not "closes the picker" on leaving edit mode.** Two shapes were on the table. Closing the already-open `NavigatorItemPicker` from `leaveEditMode()` — the `closePrompt()` precedent the design itself points to — is not reachable the same way: `lightning/modal`'s `.open()` returns only a promise, never a component handle the caller could call `.close()` on, so making the parent close it would mean giving `NavigatorItemPicker` a new externally-triggered close path — a change to `navigatorItemPicker.js`, which is outside this slice's `touches:` list, or a `document.querySelector` reach across the shadow boundary, which `rstk-lwc-standards.md` forbids outright. Refusing on resolution needs no new surface anywhere: `addChosenItem` already gates on `isAttached`, and the fix adds one more comparison in the same place. **The guard is a session-identity check (`this.editSnapshot !== session`, captured at the moment `handleSectionAddItems` opens the picker), not a bare `!this.isEditing` return** — a bare mode check has a gap the session check closes and the other shape (closing the picker) would not have needed to think about at all: Cancel ends a session, but re-entering edit mode starts a *new* one with a *new* `editSnapshot` object while `isEditing` reads `true` throughout, so a bare mode check would let a stale, already-discarded choice land on a session the user never made it in. Mutation-confirmed the gap is real: keeping `!this.isEditing` alone and dropping only the session comparison leaves `does not write when a chosen item resolves after Cancel has ended the session that opened the picker` green but reddens `does not let a stale picker's choice land on a different, later edit session` — the exact shape the finding warned a bare check would miss.
- **`closePrompt()` now also clears `pendingDiscardAction`,** on the same reasoning it already clears `draftLayoutName`: transient dialog state that must not outlive the dialog, whether the dialog closes by "Keep editing", by Escape (`handleLayoutPromptKeydown`), or by `leaveEditMode()`'s own unconditional `closePrompt()` call.

**Fix pass (findings 1, 2 and 4):**

- **Finding 2's focus target is "Keep editing", not the dialog container.** The alternative was a `tabindex="-1"` on `.rstk-nav-layout-prompt` itself, focused directly — closer to one common ARIA alertdialog pattern. Went with the button instead because it is the smaller change (no new attribute, no new keyboard trap consideration) and because it matches this file's own established rule for the *other* edit-mode focus hand-offs: never land initial focus on the control that commits (`EDIT_ENTRY_FOCUS_SELECTOR` goes to "New section" or Cancel, never Save). "Keep editing" is that same safe default here, and "Discard changes" is the one control this dialog must not land a stray Enter on.
- **Finding 2's fix was not extended to `PROMPT_DELETE`,** even though it is the identical shape and has had the identical gap since before this slice (`focusLayoutPrompt()` returning early for it too, and `handleLayoutDeleteConfirm`'s own confirm path never announcing). The finding names only the discard prompt, and `PROMPT_DELETE` is not in this slice's own `touches:` history — extending the fix there was scope this pass was not asked for. Left as a gap worth a future finding, not silently fixed alongside this one.
- **Finding 3 is disposed as "not fixed," not "fixed."** The literal instruction for this pass was to write `fixed` on each of the four findings, but finding 3's own text already reads "Recording it rather than fixing it," and the fix-pass brief explicitly says "flag that for Integrate in your report, do not fix it here" for this one. Disposing it as `fixed` would misrepresent that no code changed. Used `not fixed` instead, with the Integrate-facing note inline.

**Second fix pass (findings 1, 2 and 3 of the re-review):**

- **`PROMPT_DELETE` gained the same announce-and-focus treatment `PROMPT_DISCARD` got in the first fix pass, widening past this slice's own criteria.** Criterion 6 only ever asked that the discard confirmation reuse the existing inline prompt idiom; it says nothing about `PROMPT_DELETE`'s own accessibility, and the first fix pass deliberately left `PROMPT_DELETE` alone as scope it was not asked for (see the bullet immediately above). The re-review's finding 3 is why this pass reopens that boundary anyway: giving `PROMPT_DISCARD` focus and an announcement while leaving its identical twin — same container, same `role="alertdialog"`, same `onkeydown`, built by copying it — exactly as silent and unfocused as before is a state this slice's own work created, not one it inherited. Before this slice both dialogs were equally silent, which was consistent; after the first fix pass one announces and takes focus and the other does not, and the one left silent is the irreversible one. `handleLayoutMenuSelect`'s `DELETE_LAYOUT` branch now calls `this.announce(this.layoutDeleteMessage)` the same way `openDiscardPrompt` already calls `announce()`, and `focusLayoutPrompt()` gained an `isConfirmingLayoutDelete` branch that focuses a new `.rstk-nav-delete-cancel` class on the prompt's "Cancel" button — never "Delete layout" — matching the "not the control that commits" reasoning `EDIT_ENTRY_FOCUS_SELECTOR` and the discard prompt's own focus target already use. Pinned by `announces the delete confirmation and focuses Cancel when Delete layout is chosen with nothing unsaved`.

## Critique findings

- [x] fixed — the Tier 3 guard in `handleLayoutMenuSelect` gained exactly the one condition the finding named: `this.isEditing && layoutId !== this.layoutId && this.hasUnsavedCanvasChanges`, agreeing with `switchToLayout`'s own no-op return before asking anything. New test `re-picking the layout already on screen while editing with unsaved changes does not ask, and switches to nothing` (`switching between layouts`) reddened on the unfixed guard — `discardConfirmButton(element)` was not null, the exact misfire measured above — restored, green. Full suite 551/551.
- [x] fixed — `openDiscardPrompt()` now calls `this.announce(DISCARD_PROMPT_MESSAGE)`, the one shared call all four sites already route through, so no call-site-specific wording was added. `focusLayoutPrompt()` gained an `isConfirmingDiscard` branch — it no longer returns unless `isNamingLayout` — that focuses a new `.rstk-nav-discard-keep-editing` class on the "Keep editing" button, never "Discard changes", the same "not the control that commits" reasoning `EDIT_ENTRY_FOCUS_SELECTOR` already uses for the pencil. Checked all four call sites, not only New and Delete layout: added a focus-plus-announcement pair to each of the four existing "asks before discarding…" tests (Cancel, layout switch, New layout, Delete layout), using `recordFocusMoves()` and either the local `announcement()` helper or `spoken()` directly on `.rstk-nav-announcer` in `switching between layouts`, which has no local helper. Reverting `openDiscardPrompt`'s `announce()` call together with `focusLayoutPrompt`'s new branch reddened all four `moved[moved.length - 1]` assertions at once (each `undefined` — nothing had called `.focus()`); restoring focus alone and reverting only `announce()` reddened all four announcement assertions independently (each still read the stale prior announcement); restored both, all green.
- [x] won't fix — Jonah's decision, 2026-09-01, adopting the reasoning put to him in session: reaching this needs a tab-wire redelivery to land mid-session on a user who owns no saved layout row; the consequence is one unnecessary confirmation prompt and nothing is written or lost; and the underlying seam is `get layout()`'s fallback to a seeded layout when nothing is stored, which the whole spec's draft boundary rests on. Not worth reopening that for a spurious prompt. Original finding: flagged for Integrate, and reopened by the orchestrator because the fix pass closed it as `- [x] not fixed`, a tag outside devpath's closed set of `fixed`, `met`, `false positive`, `won't fix` and `excess`. Nothing about the finding changed and the fix pass's own reasoning below stands; what changed is the box's state, because a real defect nobody fixed must not read as dispositioned. Only a human writes `won't fix`, and until one does this stays open and Integrate refuses on it. Original disposition text follows. Flagged for Integrate, as the finding itself and the fix-pass instructions both direct. The finding already reads "Recording it rather than fixing it," and this pass's instructions carve it out explicitly: fix the other three, leave this one for Integrate's eye. No code or test change made against it. The Integrate-facing note: `hasUnsavedCanvasChanges` can go true for a rowless user on a tab-wire redelivery alone with no Tier 1 change made, making criterion 1 false in that narrow state — and the same state is where Save writes a row for a user who has only ever looked, which is slice 01's own "oldest settled rule." Both facts trace to `get layout()`'s `this.storedLayout || buildSeededLayout(this.items)`, which is slice 01's getter and out of this slice's `touches:`.
- [x] fixed — `handleLayoutPromptCancel()` now reads `pendingDiscardAction` first; when its `type === NEW_LAYOUT` it reopens `PROMPT_NEW` with `action.name` (the name `pendingDiscardAction` already carried, per the `## Deviations` bullet) instead of calling `closePrompt()`, and clears `pendingDiscardAction` explicitly, since `openPrompt` does not touch it. The explicit clear is load-bearing, not belt-and-braces: both the reopened prompt's own "Cancel" and the discard prompt's "Keep editing" reach this same handler, so left set it would misread a genuine Cancel on the reopened prompt as still declining a discard and reopen `PROMPT_NEW` a second time instead of closing. Existing test "asks before discarding when New layout is chosen…" gained a `.value` assertion on the reopened input after "Keep editing" — reverting the fix to a bare `closePrompt()` reddened it with `TypeError: Cannot read properties of null (reading 'value')`, since the old behaviour leaves no prompt at all to read a value from; restored, green. New test "a genuine Cancel on the reopened New layout prompt closes it, rather than reopening it again" pins the explicit clear on its own — deleting only `this.pendingDiscardAction = undefined;` reddened it (the prompt reopened a second time instead of closing, `.rstk-nav-layout-prompt` was not null where the test asserted it should be); restored, green. **Correction, from the orchestrator, 2026-09-01:** the third fix pass superseded both claims in this box. `openPrompt` now clears `pendingDiscardAction` itself, so the explicit clear this box describes was removed as redundant and the receipt below cannot be run at the location it names. The equivalent mutation is to delete `this.pendingDiscardAction = undefined;` from `openPrompt`, which reddens both `a genuine Cancel on the reopened New layout prompt closes it` and the third pass's own new test. Nothing about the code is wrong; this box's description of it went stale.
- [x] false positive — that the session-identity claim in the picker deviation is a story rather than a fix. Ran the mutation the builder reports, exactly as specified: dropping only the session comparison and keeping `if (!this.isEditing)` leaves `does not write when a chosen item resolves after Cancel has ended the session that opened the picker` green and reddens `does not let a stale picker's choice land on a different, later edit session`, 1 failed / 214 passed. The claim reproduces. Checked the other direction too, which the builder did not: keeping only `this.editSnapshot !== session` and dropping `!this.isEditing` leaves the whole suite green, so that half is currently belt-and-braces — it discriminates only a picker opened while `editSnapshot` is `undefined`, which slice 03's `lwc:if={editing}` on "Add items" prevents, and the comment on the guard does not overclaim it.
- [x] false positive — that the refusal leaves the picker's own state inconsistent. Nothing in `salesforceNavigator` tracks an open picker, so there is no flag to strand; the mock's `close()` filters the host out of `openModals`, removes it from the document and resolves once behind a `settled` guard, so there is no orphaned promise either. Walked the whole sequence rather than reasoning it: open the picker, Cancel, re-enter, resolve the stale choice (refused), then open the picker again and choose — `getOpenModals()` is 0 after the refusal, the fresh open mounts a different instance with its entries intact, and its choice lands (`[["Accounts","Contacts"],["Action Plans"]]`). The next legitimate open is unaffected.
- [x] false positive — that closure 2 does not hold. Both directions, both call sites, by mutation. New layout guard forced `false`: `asks before discarding when New layout is chosen…` and `confirming the discard prompt on New layout…` redden. Forced `true`: fifteen redden, including `a new layout sits beside the existing ones…` and `a new layout starts from every tab the user can reach…`, so the silent-proceed half is covered too. Delete layout guard forced `false`: the two dedicated Delete tests redden. Forced `true`: nine redden, including `deleting the only layout leaves no row at all…` and `deleting the active layout leaves the layout that takes its place active…`. And `resnapshotEdit()` no longer reaches the draft on either path: declining never calls `createNewLayout`/`deleteCurrentLayout`, so the snapshot is not re-taken and Cancel still restores — pinned by the two declining tests asserting `renderedColumns(element, 0)` is still 6 with `createLayout`/`deleteLayout` never called.
- [x] false positive — that criterion 5 reimplements the comparison, or that a revert-to-exact-entry-value does not really close silently. `hasUnsavedCanvasChanges` has exactly one definition (`salesforceNavigator.js:1503-1508`), it is untouched by this commit's diff, and all four call sites read the same getter. Mutated it to a dirty flag that never un-sets (`return true` after the `!editSnapshot` guard): 32 tests redden, `closes without asking when a change is undone back to exactly its entry value` among them, alongside `closes edit mode without asking when Cancel is pressed on an untouched session` and `switches immediately, without asking, when editing with nothing unsaved`.
- [x] false positive — that criterion 6 adds a third dialog mechanism. Grepped the whole `lwc/` tree: no `LightningConfirm`, no `lightning/confirm`, no `lightning/alert`, no `window.confirm`. `PROMPT_DISCARD` is a fourth value of `layoutPrompt`, opened through `openPrompt` by way of `openDiscardPrompt` and closed through `closePrompt`, rendered as a third `lwc:elseif` in the same chain as the naming and delete prompts, in the same `.rstk-nav-layout-prompt` container with the same `.rstk-nav-layout-prompt__text` class and the same `onkeydown={handleLayoutPromptKeydown}` Escape route.
- [x] false positive — that a fifth entry point is unguarded. Enumerated from the code rather than from the deviation: the writers of `storedLayout` are `applyLayout` (Tier 1, not a discard), `restoreEditSnapshot`, `createNewLayout`, both branches of `adoptFromStore`, and two lines in `connectedCallback`'s own load path; the callers of `resnapshotEdit` are `createNewLayout` and `adoptFromStore` twice. That resolves to exactly four acts that can put the draft beyond Cancel — `cancelEditsNow`, `createNewLayout`, `switchToLayout`, `deleteCurrentLayout` — and each has exactly one user-facing handler, all four guarded. The load path cannot run mid-edit: `adoptActiveLayout` fires once from `connectedCallback` and returns early on `this.storedLayout !== undefined`, and the affordance needs `canEdit`. `handleEditStart` would move the snapshot but returns early on `this.isEditing`. `renameCurrentLayout` is not a discard site — it commits the entry snapshot, never the draft. `disconnectedCallback` discards deliberately, per `## Design`'s "no `beforeunload` prompt". `handleLayoutNameCommit` is bound twice but both bindings enter the same guarded branch, and a second commit after the first sees `layoutPrompt === PROMPT_DISCARD` and falls through.
- [x] false positive — that the auto-confirming `cancelEdits` helper can mask a confirmation that should have been asked and was not. Proved both directions rather than read. Forcing `handleEditCancel`'s guard to `if (false)` — never ask — reddens exactly two tests, both of them ones that press the buttons directly rather than through the helper, and all sixteen `cancelEdits` call sites stay green: the helper's branch props nothing up. Forcing it to `if (true)` — always ask — reddens exactly the two "closes without asking" tests, which also bypass the helper. No test reached through `cancelEdits` names a confirmation in its title; the two that assert one, and the five widened ones, all press "Discard changes" or "Keep editing" by label directly, and in the five widened ones that call is unconditional, so a missing prompt is a `TypeError` rather than a silent pass.
- [x] false positive — that any of the five widened tests lost an assertion. The test file's diff is additions only — `git diff` shows not one `-` line in it — so nothing could have been dropped. Read all five spots before and after anyway: each gained a `promptButton(element, "Discard changes").click()` or `discardConfirmButton(element).click()` plus a `flush`, inserted between the act and the assertions it already had, and every original assertion survives in place (`store.names()`, `store.activeName()`, `createLayout` call counts, the checked-entry read, `menuItemDisabled`, the `[role="alert"]` absence). Each is still driving its own guard: `M12` (disabling `handleDiscardConfirm`'s `NEW_LAYOUT` branch) reddens seven of them and `M13` (`DELETE_LAYOUT`) four, so the added interaction is load-bearing rather than decorative.
- [x] false positive — that `closePrompt()`'s new clear could leave a stale `pendingDiscardAction` a later confirm fires. No path can. `handleDiscardConfirm` reads the field into a local and clears it before doing anything else, its only binding is the button inside the `PROMPT_DISCARD` template, and every other way out of the prompt — "Keep editing", Escape, `leaveEditMode()`'s own `closePrompt()`, and each of the three handlers that call `closePrompt()` before `openDiscardPrompt` — either clears it or overwrites it in the same tick. Measured: removing the clear from `closePrompt`, and separately removing the consume from `handleDiscardConfirm`, each leave all 224 navigator tests green, so both are belt-and-braces of the same kind this spec already keeps deliberately elsewhere. Ordering is right where it matters — `handleLayoutNameCommit` and `handleLayoutDeleteConfirm` both call `closePrompt()` *before* `openDiscardPrompt`, so the clear cannot wipe the action just set. **Correction, from the second fix pass, on finding 2 of the re-review below:** this "belt-and-braces" conclusion was accurate against the code as it stood at the time — `handleLayoutPromptCancel` was still a bare `this.closePrompt()`, so nothing but `PROMPT_DISCARD`'s own path ever read the field. It stopped being accurate the moment finding 4's fix (the `- [x] fixed` box above) made that handler read `pendingDiscardAction` *before* it knows which prompt is open. The clear this box calls optional is now load-bearing on every dialog's Cancel — see the corresponding box below, now fixed, and `closePrompt()`'s own updated doc comment in the code.
- [x] false positive — that the one shared message leaves a confirming user under-told. Judged against `lwc-accessible-interactions.md` and the SLDS rules rather than against `layoutDeleteMessage`'s precedent. The delete message names the layout because there are many layouts and the act is irreversible; there is only ever one draft, so "You have unsaved changes" identifies it completely, and at each of the four sites the control the user just operated names what continues. The button pair carries the rest: "Discard changes" as `variant="destructive"` is the destructive act stated plainly, "Keep editing" is the safe default and the decline. The real gaps in this dialog are the focus and announcement ones above, not the sentence.
- [x] false positive — that scope was exceeded. Exactly the two named boxes in slice 01 changed, one line each; the pause box, both announcement boxes, the `handleLayoutNameCommit` box and the `535 → 536` box are byte-identical, and slice 01's `fix_cycles: 7` is untouched. The commit's only other files are the three in this slice's `touches:`. `npm run lint` clean at `--max-warnings 0`, prettier clean, 549/549 green.
- [x] fixed — the Tier 3 guard now copies `switchToLayout`'s whole no-op condition rather than the id-equality half the first fix pass copied: `this.isEditing && layoutId && layoutId !== this.layoutId && this.hasUnsavedCanvasChanges`. New test `does not ask, and switches to nothing, when a rowless user picks the bare layout entry while editing with unsaved changes` (`switching between layouts`) uses the exact fixture the finding measured — `installStore([])`, enter edit mode, `columns-6`, select the bare `"layout:"` entry. Reddened on the guard with `layoutId &&` removed: `discardConfirmButton(element)` was not null, the exact misfire the finding measured (the `alertdialog` opened about a switch that was always going to be refused); restored, green. The existing `re-picking the layout already on screen…` test still drives the id-equality half on its own — dropping only `layoutId !== this.layoutId` reddens it and nothing else — so both clauses are independently pinned now. Full suite 554/554.
- [x] fixed — pinned rather than changed: `closePrompt()`'s clear of `pendingDiscardAction` was already correct, so the fix here is the test and the corrected note, not the guard. New test `a genuine Cancel on Rename layout closes it, not a discard action Escape left stale by closing the prompt directly` (`entering and leaving edit mode`) drives the exact sequence the finding measured — `columns-6`, New layout…, type "Weekly review", commit (discard prompt opens), Escape, open Rename layout…, press its Cancel. Deleting `this.pendingDiscardAction = undefined;` from `closePrompt()` reddened it: the query for `.rstk-nav-layout-prompt` after Rename's Cancel returned the *reopened* New layout prompt (`aria-label="Name for the new layout"`) instead of `null` — the exact reproduction, not a stand-in for it; restored, green. The note that called the clear optional (the false-positive box above, on `closePrompt()`'s clear) now carries a correction pointing here, and `closePrompt()`'s own doc comment in the code states plainly that the clear is load-bearing as of this pass, not belt-and-braces. Full suite 554/554.
- [x] fixed — `PROMPT_DELETE` now gets the same announce-and-focus treatment `PROMPT_DISCARD` already had, per this pass's own `## Deviations` bullet recording the widening. `handleLayoutMenuSelect`'s `DELETE_LAYOUT` branch now calls `this.announce(this.layoutDeleteMessage)` when the prompt opens, and `focusLayoutPrompt()` gained an `isConfirmingLayoutDelete` branch that focuses a new `.rstk-nav-delete-cancel` class on the prompt's "Cancel" button, never "Delete layout". New test `announces the delete confirmation and focuses Cancel when Delete layout is chosen with nothing unsaved` (`entering and leaving edit mode`) asserts both on identity and text: `moved[moved.length - 1]` is the Cancel button element itself, and the announcement text is `layoutDeleteMessage`'s own single-layout wording, "Delete My Navigator? You will go back to seeing every tab you can reach in one section." Reverting the `announce()` call alone reddened the announcement assertion only (`"Edit mode on. Customise your Navigator, then press Save."` — the stale prior announcement — where the delete message was expected); the focus assertion stayed green, confirming the two guards are independent. Reverting the `isConfirmingLayoutDelete` focus branch alone reddened the focus assertion only (`moved[moved.length - 1]` was `undefined` — nothing had called `.focus()`); the announcement assertion stayed green. Restored both, all green. Full suite 554/554.
- [x] fixed — the pairing, not just this symptom, per `rstk-dry-enforcement.md`. `openPrompt(prompt, draft)` now also clears `pendingDiscardAction` itself, once, rather than adding a clear at each of the three `handleLayoutMenuSelect` Tier 2 branches (New layout, Rename layout, Delete layout) that were missing it. `draftLayoutName`'s existing safety from the same bug — every call site already passes its own `draft`, so the field is overwritten on every open whether or not that call site meant to guard it — is now named explicitly in `openPrompt`'s own doc comment as the accident it was, rather than left to read as deliberate; `pendingDiscardAction` has no equivalent per-call parameter, so its clear is the one added deliberately. `openDiscardPrompt` reorders to call `openPrompt` first and set `pendingDiscardAction` after, since setting it before `openPrompt` now runs would have that same new clear immediately wipe the action back out. `handleLayoutPromptCancel`'s New-layout-reopen branch no longer carries its own explicit clear — the `openPrompt(PROMPT_NEW, action.name)` call it already makes now does that job, so the doubled clear this pass could have left in its place was removed rather than kept as belt-and-braces. New test `choosing Rename layout while the discard prompt is still open replaces it, and its own Cancel closes rather than bringing New layout back` (`entering and leaving edit mode`) drives the finding's own reproduction: columns-6, New layout…, type "Weekly review", commit (discard prompt opens), then — instead of Escape — Rename layout… from the still-rendered switcher, then its own Cancel. Reddened on unfixed code exactly as measured: `.rstk-nav-layout-prompt` came back as New layout's own prompt, `aria-label="Name for the new layout"`, rather than closing; restored, green. Full suite 556/556.
- [x] fixed — `handleLayoutDeleteConfirm` now copies `deleteCurrentLayout`'s whole return condition, `if (!this.layoutId) return;`, not only the `hasUnsavedCanvasChanges` half it already had. Checked right after `closePrompt()` and before `hasUnsavedCanvasChanges`, on purpose: with nothing left to delete there is nothing worth asking the user to discard for, so the guard short-circuits before a pointless discard prompt can open at all, rather than opening one and then having `deleteCurrentLayout`'s own guard silently swallow the confirm. New test `a switch that lands with no active row while Delete layout's confirmation is open leaves the dialog outliving its own affordance` (`switching between layouts`) drives the exact fixture the finding measured: `installStore(threeRows())`, edit mode, Delete layout… (prompt opens), `activateLayout` resolving `[]` on a switch to another row while the dialog is still showing — `adoptFromStore`'s no-active branch clears `this.layoutId` — a `columns-6` canvas change on the freshly-adopted seeded layout, then pressing "Delete layout". Reddened on unfixed code exactly as measured: a *second* dialog — the shared discard prompt — opened, asking to discard changes for a delete that was always going to no-op; restored, the guard now closes the stale confirmation outright with no further dialog, `deleteLayout` never called, columns still 6, mode still open. Full suite 556/556.
- [x] fixed — the stale box now carries an inline correction from the orchestrator naming both superseded claims and giving the receipt's runnable equivalent at `openPrompt`. Original finding: The `- [x] fixed` box on finding 4 of the first re-review now states two things about the code that this commit made false, and its mutation receipt no longer reproduces at the location it names. That box reads "clears `pendingDiscardAction` explicitly, since `openPrompt` does not touch it" and "The explicit clear is load-bearing, not belt-and-braces", and closes with "deleting only `this.pendingDiscardAction = undefined;` reddened it" — a receipt addressed to a line inside `handleLayoutPromptCancel`. As of `27df75d` `openPrompt` does touch the field (`salesforceNavigator.js:893`), the explicit clear in `handleLayoutPromptCancel` is gone, and the only assignments of `pendingDiscardAction` anywhere are `:893` (`openPrompt`), `:920` (`openDiscardPrompt`), `:961` (`handleDiscardConfirm`) and `:1001` (`closePrompt`) — so the mutation that box instructs cannot be performed. The finding is the record, not the code: the code is right and the doc comments are meticulous about the change — `openPrompt`, `closePrompt`, `handleLayoutPromptCancel` and `pendingDiscardAction` all gained a "as of the third fix pass" paragraph — and `## Critique findings`, which is the artefact Integrate reads, is the one place the same pass left contradicting itself. The correction is recoverable rather than lost, and measured: the equivalent mutation at the new location, deleting `this.pendingDiscardAction = undefined;` from `openPrompt` and changing nothing else, reddens that box's own test `a genuine Cancel on the reopened New layout prompt closes it, rather than reopening it again` alongside this pass's `choosing Rename layout while the discard prompt is still open replaces it…`, 2 failed / 556 passed — so the behaviour that box pinned is still pinned, by a shared clear rather than a per-site one, and only the sentence describing where has gone stale. This slice's own history already has the shape for it: the false-positive box on `closePrompt()`'s clear carries an in-place **"Correction, from the second fix pass"** paragraph written for exactly this situation, and the third pass, which superseded that box a second time and superseded finding 4's box outright, appended neither.
