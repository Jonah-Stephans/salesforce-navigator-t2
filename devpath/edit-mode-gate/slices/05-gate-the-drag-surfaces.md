---
depends_on:
  - devpath/edit-mode-gate/slices/03-gate-the-section-header-controls.md
  - devpath/edit-mode-gate/slices/04-gate-the-item-menu.md
done: true
fix_cycles: 4
touches:
  - force-app/main/default/lwc/salesforceNavigator/salesforceNavigator.js
  - force-app/main/default/lwc/navigatorSection/navigatorSection.js
  - force-app/main/default/lwc/navigatorSection/navigatorSection.html
  - force-app/main/default/lwc/navigatorSection/__tests__/navigatorSection.test.js
  - force-app/main/default/lwc/navigatorItem/navigatorItem.js
  - force-app/main/default/lwc/navigatorItem/navigatorItem.html
  - force-app/main/default/lwc/navigatorItem/navigatorItem.css
  - force-app/main/default/lwc/navigatorItem/__tests__/navigatorItem.test.js
---

# Out of edit mode nothing can be dragged or grabbed

> Watch a new test go red before you make it green.
> _Why: a test written before the code cannot be written to hit a coverage number — there is nothing to
> cover yet. Retires when Apex gets mutation testing._

## What to build

Out of edit mode a user cannot rearrange the Navigator by any route — sections and items are not
draggable by pointer and cannot be grabbed from the keyboard — while clicking a link goes on working
exactly as it does today.

## Acceptance criteria

- [x] met Out of edit mode a section card and an item anchor both report `draggable="false"`.
- [x] met Out of edit mode pressing Space on a section or an item does not grab it, and no announcement is made.
- [x] met Out of edit mode a section card is not a tab stop, so a keyboard user moving through the panel stops only on links.
- [x] met In edit mode pointer dragging and the full keyboard grab-move-drop-cancel path both work exactly as they do today, announcements included.
- [x] met Clicking a link out of edit mode navigates, and a Ctrl-, Cmd- or Shift-click still reaches the browser instead of being intercepted.
- [x] met Leaving edit mode clears any keyboard grab that was in flight, so focus restoration cannot chase a card that is no longer grabbable.
- [x] met The comment in `navigatorItem.css` asserting that there is no edit mode is replaced with one recording what superseded it, not deleted.

## Deviations
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/navigatorSection/navigatorSection.css, +15 -2 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`, which names `navigatorItem.css` but not this one. Swept in by the second fix pass: the superseded comment claiming the card is permanently draggable and permanently focusable was replaced rather than deleted, and `cursor: grab` moved off the base rule onto an edit-mode-conditional modifier class. Criterion 7 named only the item's stylesheet, so this file's identical stale comment was outside the slice as written.
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js, +1851 -160 against `main` as this branch found it; outside this slice's `touches`. This third fix pass touched the file again, so this is the current whole-branch figure and it supersedes the +1817 -172 on the box immediately below for the same path; all four boxes stay open and all four are closed at review. This pass's own share is 46 lines, all additions: the inline `await enterEditMode(element);` and its accompanying comment added to "drags the item the user picked up, not the one sharing its stored position" and "puts the item at the end when it is dropped on the card rather than on an item" (finding 1's own named cost), and the new composed-level pin "does not move an item across sections when dropped on another item out of edit mode, and writes nothing" — see the finding 1 disposition above.
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js, +1817 -172 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. This second fix pass touched the file again, so this is the current whole-branch figure and it supersedes the +1811 -172 on the box immediately below for the same path; all three boxes stay open and all three are closed at review. This pass's own share is six lines: the comment and the `enterEditMode(element)` call added inline to "drops the item at the position it was dropped at, not at the end" (`:2666`), the one composed-level test that broke as a correct consequence of gating the item's own drop target — see the finding 1 disposition above for why that test's subject moved behind edit mode.
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js, +1811 -172 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. This slice's fix pass touched the file again, so this is the current whole-branch figure and it supersedes the +1798 -174 on the earlier box for the same path; both stay open and both are closed at review. The fix pass's own share is the restored self-drop pin, the item-axis announcement assertion, and the new drop-target pin.
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js, +1798 -174 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. Cumulative across slices 01, 03, 04 and 05 — twelve prior commits have rewritten this suite, and boxes on slices 03 and 04 carry the earlier figures for the same path. This slice's own share is the six composed-level drag and keyboard tests brought to the gated build, two net-zero-write rewrites, and `enterEditMode` baked into the `navigatorWithThree` and `navigatorWithSections` mount helpers.

- The trap 274 scan, run myself before writing any closing claim: a text-level scan of `salesforceNavigator.test.js` for a payload assertion (`savedIds`/`savedItems`/`lastSavedLayout`/`updateLayout.mock`/`createLayout.mock`) with no literal `enterEditMode` in the `it` body returned **18** candidates at the start of this slice, not the 4-6 the prior record implied. Read individually: 6 are the `renameFirstItem`/`burstItemRename` debounce-vehicle tests slice 04 already documented as deliberately outside edit mode (false positives — they pin the save chain, not a reachable rename route, since renaming is menu-gated as of slice 04); 7 are inside "renaming an item" and mount through that describe's own `navigatorOn` helper, which calls `enterEditMode` internally (false positives — the literal string just isn't in the `it` body); and the remaining 5 were the real, then-ungated drag/keyboard-reorder surfaces: item pointer drag (`:1574`), item keyboard reorder (`:1604` and `:2285`), section pointer drag (`:1766`), section keyboard reorder (`:1790`). That confirms the four-surface, two-per-component inventory slice 04 handed down — item and section, pointer and keyboard — with the item axis carrying two pinning tests for its keyboard route rather than one. Re-run at the end of this slice: 17 candidates, all of them the same 6 debounce-vehicle and 7 `navigatorOn`-mounted false positives plus 4 more that are now false positives for the identical helper-embedded reason (`navigatorWithThree`/`navigatorWithSections`, below, now call `enterEditMode` themselves). Zero real surfaces remain.
- `enterEditMode` was baked into the shared mount helpers `navigatorWithThree()` (item-axis "reordering" describe, 9 tests) and `navigatorWithSections()` (section-axis "the sections themselves" describe, 15 tests after this slice's addition) rather than added to each test individually, on the same reasoning as slice 04's `navigatorOn`: essentially every test in each describe drives the card or the anchor as a draggable or keyboard-grabbable thing, so the shared helper is the one place to say so, per `rstk-dry-enforcement.md`. The "moving an item into another section" describe's shared helpers (`navigatorWithTwoSections`, `navigatorWithAWithdrawnTab`) were deliberately left alone, exactly as slice 04 left them — they are also the mount helpers for that describe's pointer-drag tests, which must stay representative of the out-of-edit-mode state they exercise at the JS-handler level (see below) — and `enterEditMode` was added inline to the one keyboard test that needed it instead ("reorders the item the user picked up when an earlier one is out of reach").
- Every test moved behind edit mode that also checked a persisted payload had its `settleAutosave()` replaced with `saveEdits(element)`, for the same reason slice 04's fix pass gives: `scheduleSave`'s `isEditing` guard suppresses the debounce entirely while editing, so autosave is no longer a route to a write and Save is the only one left.
- Three tests whose net effect was a cancelled/abandoned gesture landing back on the edit session's own entry state needed more than a mechanical swap, because Save's `hasUnsavedCanvasChanges` guard means a byte-identical canvas now writes **nothing**, where the old debounce would have written the identical-content payload anyway: "puts the item back where it started when the drag is cancelled with Escape" (item axis) and "reorders the sections from the keyboard, and cancels on Escape" (section axis) each asserted `savedItemIds`/`savedSectionNames` equal to the original order; both now assert `saveEdits` calls neither `createLayout` nor `updateLayout`. "Moves no card the user never picked up after an abandoned section drag" and "writes nothing when a section card is dropped back on itself" already asserted no write, but only via `settleAutosave` — under edit mode that assertion is satisfied by `scheduleSave`'s own suppression regardless of whether the abandon/self-drop guard fired, which is the vacuous-retarget shape trap 273 names; both were changed to `saveEdits` + `not.toHaveBeenCalled()` on both Apex calls so the assertion is back to discriminating the guard it names.
- Pure pointer-drag tests (a hand-rolled `dragstart`/`dragover`/`drop` `CustomEvent` dispatched directly on the anchor or the card) are **not** gated at the JS-handler level, matching `## Design`'s explicit choice to rely on the browser refusing to start a drag on `draggable="false"` rather than adding a second, redundant guard in `handleDragStart`/`handleCardDragStart` etc. Per `lwc-jest-ceilings.md`, jsdom enforces no such thing, so these tests were already green before this slice's gating code existed and stayed green after it, whether or not they were wrapped in `enterEditMode` — wrapping them (via the shared helpers above) makes what they exercise consistent with the state the design says pointer dragging is now confined to, but the wrap itself proves nothing a jsdom test can prove; the `draggable` attribute-value assertions are what actually discriminates the pointer-drag gate, and those are new, targeted unit tests (see below), not the existing drag-forwarding tests.
- `navigatorSection.test.js`'s "reordering its items" and "reordering its items from the keyboard" describes dispatch `itemgrab`/`itemkeymove`/etc. as raw `CustomEvent`s straight onto the `c-navigator-item` host, bypassing the item's own `handleDragKeydown` entirely — they test `navigatorSection`'s handling of an event it has already received, not whether the item would produce that event out of edit mode. Left unchanged (all still pass with `editing` at its default `false`): the origin-side gating they don't exercise is pinned separately, in `navigatorItem.test.js`'s own keyboard-drag-source tests and in the two new composed-level negative tests below.
- Added two composed-level negative tests not named by the slice brief, to close the loop between the unit-level gate and the assembled component: "does not grab an item on Space out of edit mode, and writes nothing" (item axis) and "does not grab a section card on Space out of edit mode, and writes nothing" (section axis), both in `salesforceNavigator.test.js`, mounted without `enterEditMode`.
- `navigatorItem.js`'s `handleDragKeydown` returns `false` (not merely `undefined`/no-op) when not editing, matching its existing "returns whether the key was consumed" contract, so `handleKeydown`'s own fallback-Enter branch is undisturbed by the new guard.
- The grab cursor's *visual* effect — that it actually disappears from the pointer over a card or link out of edit mode, and actually appears in edit mode — is not asserted anywhere in this pass, and is not claimed to be. Per `lwc-jest-ceilings.md`, jsdom applies no stylesheet, so `getComputedStyle(...).cursor` would read empty regardless of what the CSS says, and a test built on it would pass or fail for reasons unrelated to the CSS — an invented check. What is asserted instead: that the modifier class (`rstk-nav-item_editing` / `rstk-nav-section_editing`) is present on the element only in edit mode (a DOM fact), and that the shipped CSS text carries `cursor: grab` on that class and not on the base rule (a source-text fact, the same technique this suite already uses for `_grabbed`'s appearance and the one `lwc-jest-assertions-that-can-fail.md` recommends for a CSS-only fact). Whether the two facts compose into the right on-screen cursor in an actual browser is a live-org, eye-only check this pass does not and cannot perform.
- Third fix pass, on `event.cancelBubble`: checked independently, outside the suite, before trusting it as a pin — in this jsdom, `cancelBubble` reads back `false` after `dispatchEvent()` returns even when a listener genuinely called `stopPropagation()` during dispatch (it reads `true` only if `stopPropagation()` is called directly on the event object outside a dispatch cycle, which is not the shape this fix needed to prove). So a `handleDrop` pin built on `event.cancelBubble` would have been an invented check per `lwc-jest-ceilings.md`'s own spirit — green or red for a reason unrelated to the code. The pin instead attaches a listener to `document.body` (an ancestor outside the item's own shadow tree) and asserts it is never reached, which is the same shape as the "the item's gate must hold at the composed level" instruction and was verified, by mutation, to redden exactly when the reorder this finding names is reverted.
- Third fix pass: the pre-existing critique boxes at lines 88-89 (trap 273 and O1) still say "the open finding above" and "the second open finding above," referring to what were open findings when that critique pass wrote them. Left as written rather than edited to point at the now-closed dispositions: each box is that reviewer's own record of their reasoning at the time, and the two dispositions immediately above name what closed them — rewriting a prior pass's own sentences to stay current would blur whose finding is whose across passes.

## Critique findings

- [x] fixed — restored `:1989`'s "writes nothing when a section card is dropped back on itself" to mount out of edit mode (`getLayouts.mockResolvedValue`, `createNavigator`, `getNavItems.emit`, `flush`, no `enterEditMode`) and assert via `settleAutosave()` in place of `saveEdits` + `not.toHaveBeenCalled()` — the vehicle the finding names as sufficient, since out of edit mode `scheduleSave` is not suppressed and carries no `hasUnsavedCanvasChanges`-style content check that could mask the short-circuit. Watched it go red first: with `if (from === to) { return; }` deleted from `handleSectionDrop` (`salesforceNavigator.js:1593-1595`), the rewritten test failed on `expect(updateLayout).not.toHaveBeenCalled()` (one call recorded, full `updateLayout` payload logged); restored the guard and the test is green again, with zero net diff left on `salesforceNavigator.js`. Also independently mutation-checked the three sibling rewrites the finding names, rather than taking the count on trust: `:1663` (item Escape) and `:1888` (section Escape) both redden when `handleEditSave`'s `hasUnsavedCanvasChanges` guard is bypassed (`const wroteChanges = true`) — each fails on `not.toHaveBeenCalled()` with one call recorded; `:1956` (abandoned section drag) reddens on the `sectionNames` order assertion, not the write assertion, when `handleSectionDragEnd`'s `this.clearDrag()` call is removed. All three mutations reverted afterward; all three genuinely discriminate and were left unchanged.
- [x] fixed — coerced both `editing` setters (`navigatorItem.js:115-123`, `navigatorSection.js:143-167`) to `value === true` before storing, so an uncoerced `undefined` crossing the `@api` boundary can no longer read back as "draggable". Pinned with a new test in each unit suite — `navigatorItem.test.js` and `navigatorSection.test.js` each gained "coerces an explicit undefined `editing` to draggable=\"false\" rather than failing open" — setting `editing: undefined` explicitly (direct property assignment, not `createSection()`'s destructured-default path, which cannot reach `undefined`) and asserting `hasAttribute`, `getAttribute` and the reflected `.draggable` property. Watched both pins go red against the unfixed setters: the item pin failed on `hasAttribute("draggable")` (`Expected: true, Received: false` — the attribute dropped entirely); the section pin failed the same assertion the same way. Both green after the fix.
- [x] fixed — added the missing item-axis announcement assertion to the composed test at `:1536` ("does not grab an item on Space out of edit mode, and writes nothing"), querying the item's own section's live region — `querySections(element)[0].shadowRoot.querySelector("[aria-live]")`, since the item-grab announcement lives in `navigatorSection.handleItemGrab`, not on `salesforceNavigator`'s own region — and asserting empty `textContent`, matching the section axis's existing `:1807-1809` shape. Watched it go red: deleting `if (!this.editing) { return false; }` from `navigatorItem.js`'s `handleDragKeydown` reddens the pre-existing `grabbed`-flags assertion as before; with that assertion temporarily disabled to isolate the new one, the announcement assertion alone also reddened on the same mutation (`Received: "Accounts grabbed. Position 1 of 3.​"`), which is what makes it a real, independently-discriminating pin rather than a duplicate of the flags check. Guard and test both restored; suite green.
- [x] fixed — gated `handleCardDragOver` (`navigatorSection.js:833-845`) behind `if (!this.editing) { return; }`, so out of edit mode the card no longer calls `preventDefault()` or sets `dropEffect = "move"` for a foreign drag — the drop-target half of O1's "no drag surface" that the drag-source half already covered via `draggable`. Left `handleCardDrop`, `handleCardDragEnter`/`handleCardDragLeave` and the drag-source handler (`handleCardDragStart`) untouched: the browser will not fire a native `drop` after an unprevented `dragover`, so a second guard on `handleCardDrop` would be redundant on the same reasoning the design already applies to trusting `draggable="false"` over a `handleDragStart` guard, and gating the drag source itself is the thing this pass was explicitly told not to do. Updated the one existing unit test that depended on the old unconditional behavior ("tells its parent when its own card is picked up and dropped on") to mount with `editing: true`, since its actual subject — dragstart/dragover/drop forwarding — is an in-edit-mode, criterion-4 concern. Added a new pin, "does not advertise itself as a drop target out of edit mode", asserting `over.defaultPrevented` is `false` on a plain `createSection()` (`editing` defaults to `false`). Watched it go red first — with the guard removed, `defaultPrevented` came back `true` — then restored the guard; the full suite (521 tests) is green.
- [x] verified — trap 274's scan, run independently rather than reconciled: a brace-matched pass over all 185 `it`/`it.each` blocks in `salesforceNavigator.test.js` finds 40 that assert a payload (`savedIds`/`savedItems`/`lastSavedLayout`/`updateLayout.mock`/`createLayout.mock`) and **17** of those with no literal `enterEditMode` in the body — the same count the builder reports. Every one classified by reading the helper rather than by trusting the dismissal: 6 in `describe("autosave")` drive `renameFirstItem`, which dispatches `itemrename` straight onto the item host and is a deliberate debounce vehicle documented at `:194-214`, not a route a user can reach now that the item menu is gated; 7 in `describe("renaming an item")` mount through `navigatorOn` (`:2795-2802`), which calls `enterEditMode`; 2 (`:1601`, `:1631`) mount through `navigatorWithThree` (`:1444-1450`), which calls `enterEditMode`; 2 (`:1830`, `:1854`) mount through `navigatorWithSections` (`:1771-1778`), which calls `enterEditMode`. Zero real ungated write routes remain by the scan's own criterion.
- [x] verified — the scan's blind spot checked independently from the production code rather than from the suite. All eleven `applyLayout` call sites in `salesforceNavigator.js` were walked back to a user gesture: `renameCurrentLayout`, `handleAddSection`, `handleSectionRename`, `handleSectionColumns`, `handleSectionDelete`, `handleItemRename`, `handleItemRemove` and `addChosenItem` all sit behind a control that is absent from the DOM out of edit mode (slices 01, 03, 04, plus the `!this.isEditing` re-check on the layout menu at `:662`); `moveItemWithin`, `moveSectionTo` and `moveItemBetween` are reachable only through the four drag surfaces this slice gates. A foreign drop reaches `handleSectionDrop` with no `dragKind` and returns without writing. Nothing else in the file writes.
- [x] verified — mount-helper census, all 23 tests. `navigatorWithThree` is used by 8 `it` blocks plus the 6-case `it.each(MOVES)` through `freshNavigator`; `navigatorWithSections` by 14, with the new out-of-edit-mode test at `:1792` mounting inline. Every one of the 23 drives a pointer drag, a keyboard grab, or focus restoration during one, so every one needs the mode it was moved into. Load-bearing discrimination re-checked by mutation on the four that assert an absence rather than a positive result; three redden and the fourth is the finding above. The rest assert a specific non-default order, payload or focus target and cannot be satisfied by a suppressed write.
- [x] verified — criterion 1 and trap 260. `draggable` renders as a value, not a presence, and the tests assert the value on both axes: `navigatorItem.test.js:304`/`:316`/`:708` and `navigatorSection.test.js:873`/`:888`, each checking `hasAttribute`, `getAttribute` and the reflected `.draggable` property. The one pre-existing `toContain("draggable")` at the old `:704` is gone. Mutating `draggable={editing}` back to `draggable="true"` reddens exactly one test in each component. The only surviving `[draggable]` presence check is `navigatorItem.test.js:1321`, and it is an absence claim about the whole node during `isRenaming` — the spec's own cited precedent — not a check on the gate.
- [x] verified — criterion 2 and criterion 3, by mutation. Deleting `if (!this.editing) { return false; }` from `handleDragKeydown` reddens the item unit test and the composed `:1536`. Deleting `if (!this.editing) { return; }` from `handleCardKeydown` reddens the section unit test and the composed `:1792`. Making `cardTabIndex` return `"0"` unconditionally reddens `navigatorSection.test.js:878`. The item anchor keeps its native link tab stop (`fallbackTabIndex` returns `undefined` whenever a real href resolved), so out of edit mode the panel's tab stops are links, the pencil and the layout switcher — which is what criterion 3 claims.
- [x] verified — criterion 6, both halves, and neither leaves a stale index. Section-level: `releaseGrab()` in the `editing` setter clears `grabbedItemIndex`, so `keepFocusOnGrabbedItem` early-returns; deleting it reddens the new `navigatorSection.test.js` test. Page-level: `leaveEditMode` clears `grabbedSectionIndex` via `releaseSectionGrab()` **and** `cardFocusIndex`, so `renderedCallback`'s `target` is `undefined` and the card-focus branch does not fight `focusEditTransition`'s hand-off to the pencil; deleting both lines reddens "ends any in-flight keyboard grab when edit mode ends". The two are genuinely distinct — different components, different fields — and both fire.
- [x] verified — criterion 5 and O7. `handleClick` is byte-identical to both `8ea706d` and `main`, compared by extracting the brace-matched method body from all three revisions. Its two `preventDefault()` cases stand and no drag suppression was added. The plain-click navigation test and the three modifier-click cases (`navigatorItem.test.js:104-144`) all mount with `editing` unset, so O7 is pinned in the state the Intent cares about rather than only in edit mode.
- [x] verified — criterion 7. The `navigatorItem.css` comment is replaced rather than deleted, per `rstk-preserve-documentation.md`, and the original reasoning survives inside it in substance ("HTML5 drag-and-drop separates a click from a drag ... a `click` only fires when no drag occurred"). It also says both things `## Design`'s "What this supersedes" requires: that the original reasoning was correct about clicking, and that the mode exists for a visual reason that reasoning never asked about.
- [x] verified — scope. `salesforceNavigator.js` carries no production change in `c4b8290` and nothing of slice 02's was touched; the diff is `navigatorItem` (js/html/css/test), `navigatorSection` (js/html/test) and `salesforceNavigator.test.js`. `npx eslint --max-warnings 0` and `prettier --check` are both clean on the changed components. Note the `touches:` front matter over-declares `salesforceNavigator.js`, which this slice did not modify.
- [x] verified — jsdom's ceiling, stated for the record. Of the four surfaces, the two keyboard ones are test-proven at unit and composed level; the two pointer ones rest on the attribute alone and nothing here can do better. Probe run to make the limit concrete rather than assumed: mounted out of edit mode, the existing synthetic `dragstart`/`dragover`/`drop` sequence on the item anchors still reorders the items and still writes a full `createLayout` through the debounce. That is the design's explicit choice — rely on the browser refusing to start a drag on `draggable="false"` rather than add a redundant `handleDragStart` guard — and `lwc-jest-ceilings.md` says jsdom enforces no such thing. So "nothing can reach the save path" is proven for the keyboard and asserted for the pointer, on one attribute-value test per component.
- [x] fixed — gated `navigatorItem.handleDragOver` (`navigatorItem.js:438-459`) behind `if (!this.editing) { return; }`, the identical three lines `handleCardDragOver` already carries, so out of edit mode the anchor no longer calls `preventDefault()` or sets `dropEffect = "move"` for a foreign drag. `handleDrop` (`:461-480`) is gated the same way, and deliberately, not merely for symmetry: `handleCardDrop` is safely left ungated because a real browser never fires `drop` at all once the preceding `dragover` went uncancelled — it fires `dragleave` instead — so an ungated handler there is dead code the browser itself never reaches. That reasoning does not extend to the item, because it cannot be *proven* here the way it can be reasoned about for the card: jsdom dispatches exactly the event a test hands it and enforces none of the browser's own dragover/drop sequencing, and the finding's own probe showed exactly that — a synthetic `drop` dispatched straight at the anchor reached the old ungated handler and dispatched `itemdrop` regardless of what `dragover` had done. Gating `handleDrop` directly closes that gap instead of resting on an inference this suite cannot check, and it is what makes a "drop no longer dispatches" pin real rather than decorative. Two new unit pins in `navigatorItem.test.js`, both watched red before green: "does not advertise itself as a drop target out of edit mode" (`dragover`'s `defaultPrevented === false`) and "does not dispatch a drop out of edit mode either" (`itemdrop` not dispatched, `drop`'s `defaultPrevented === false`) — deleting each guard in turn reddened exactly its own pin and nothing else, both restored after. The two pre-existing unit tests whose actual subject is in-edit-mode dragover/drop forwarding ("accepts a drop by cancelling dragover..." and "re-emits a drop as its own index...") were updated to mount with `editing: true`, following the card's own precedent; removing that override from each reddened it against the now-gated code, confirmed and reverted. One composed-level test broke as a direct and correct consequence of closing the gap: "drops the item at the position it was dropped at, not at the end" (`salesforceNavigator.test.js:2666`) drops onto another item's own anchor — exactly the path just gated — so `enterEditMode(element)` was added inline to it, the same technique already used elsewhere in that describe for a single test needing it. Its two "dropped on the card rather than on an item" siblings target the section's own `<article>` instead, which stays ungated by design, and neither was affected. Full suite green afterward, 527 of 527.
- [x] fixed — `navigatorSection.css:8-9`'s comment is replaced, not deleted, per `rstk-preserve-documentation.md`, with: "Draggable and focusable only in edit mode as of the edit-mode-gate spec — this comment used to say the card was permanently both. `draggable` is now bound to `editing` in the template (`navigatorSection.html`) rather than a static "true", and `tabindex` is a getter (`cardTabIndex` in navigatorSection.js) returning "0" in edit mode and "-1" out of it, so a card that cannot be grabbed is no longer a keyboard tab stop either. The grab cursor moves the same way: it used to sit unconditionally on `.rstk-nav-section` above, and now lives here instead, on the `editing`-conditional class `cardClass` adds — the same reason `navigatorItem.css` gates its own cursor, and for the same fix: a permanent affordance is visual clutter on a page whose primary purpose is navigation, not customisation." `cursor: grab` moved off both base rules (`.rstk-nav-section`, `.rstk-nav-item`) into two new modifier classes, `.rstk-nav-section_editing` and `.rstk-nav-item_editing`, each pushed by `cardClass`/`anchorClass` only when `this.editing` is true; source order keeps both ahead of `_grabbed`, so `cursor: grabbing` still wins while grabbed. The item's own comment (`navigatorItem.css`, now sitting directly above `.rstk-nav-item_editing`) is corrected to match: its last two sentences now read "...anything about clicking. The cursor moves with it: it used to sit unconditionally on `.rstk-nav-item` above, and now lives here instead, on the `editing`-conditional class `anchorClass` adds — so nothing about this element still advertises a drag out of edit mode, cursor included," replacing the self-contradicting "The cursor below is the only affordance saying so, same as before." Per `lwc-jest-ceilings.md`, jsdom applies no stylesheet, so the cursor's *visual* effect is not asserted by computed style anywhere — that remains an eye-only check in a real org, recorded under `## Deviations`. What is asserted, in both `navigatorItem.test.js` and `navigatorSection.test.js`: a DOM-level pin ("carries the grab-cursor class only in edit mode") that the modifier class is present only when `editing` is true, and a stylesheet-text pin ("keeps the grab cursor off the base rule...") reading the shipped CSS and asserting the base rule's text does not contain `cursor:` while the modifier rule's does — the same technique this suite already uses for `_grabbed`'s appearance, and the one `lwc-jest-assertions-that-can-fail.md` recommends for a fact that lives only in CSS. Both pins in both files watched red first: removing the `if (this.editing)` guard from each class getter reddened its own DOM-level pin; putting `cursor: grab` back on each base rule reddened its own stylesheet-text pin, and in the item's case confirmed the anchored `^...\{[^}]*\}` regex correctly skips the unrelated `.rstk-nav-item__row .rstk-nav-item { ... }` compound-selector rule rather than matching it by accident. All four mutations reverted; full suite green afterward, 527 of 527.
- [x] verified — finding 1's restored pin, re-mutated. Deleting `if (from === to) { return; }` from `handleSectionDrop` (`salesforceNavigator.js:1593-1595`) reddens `:1997` on `expect(updateLayout).not.toHaveBeenCalled()` with one call recorded, and the logged payload is byte-identical to the stored layout — which is the redundant write the guard exists to prevent, so the pin discriminates the named guard and not the mode. The `sectionNames` assertion above it passes under that mutation, so the write assertion is the load-bearing one, exactly as intended. The restoration does not reintroduce what the rewrite was solving: `hasUnsavedCanvasChanges` only exists on the Save path, and out of edit mode the write comes through `scheduleSave`'s debounce, which carries no content check — so `settleAutosave()` is the one vehicle on which "writes nothing" is a claim about the short-circuit. The vehicle is unreachable-in-production out of edit mode by this slice's own gate, which makes it a coverage vehicle of the same documented kind as the `renameFirstItem` debounce tests, not a claim about a reachable route.
- [x] verified — the three siblings, independently, and `:1964`'s write assertion is **not** half-inert. `:1671` (item Escape) and `:1896` (section Escape) each redden on `not.toHaveBeenCalled()` with one call recorded under `const wroteChanges = true` in `handleEditSave`; each also carries an order assertion that pins the Escape revert itself, so both assertions are load-bearing and neither test rests on the other. On `:1964` (abandoned section drag) the fix pass reported the `sectionNames` order assertion reddening "rather than" the write assertion — that is jest short-circuiting at the first failing `expect`, not inertness. Disabling line `:1986` and re-running the same mutation (`this.clearDrag()` removed from `handleSectionDragEnd`) reddens `:1993` alone, `Expected 0, received 1`. Both assertions discriminate the same guard through two different observables; the test is not the trap-273 shape wearing a different coat, and the fix pass under-reported its own test.
- [x] verified — finding 2's coercion is safe and both pins hold. Nothing legitimate crosses either `@api` boundary with a truthy non-`true`: `salesforceNavigator.html:217` binds `editing={isEditing}` and `isEditing` is a plain field assigned only `false` (`:261`, `:1260`) and `true` (`:1163`); `navigatorSection.html:118` binds `editing={editing}`, whose getter returns the already-coerced boolean; every test call site in all three suites passes a literal `true`, `false` or `undefined`; and no production code anywhere assigns `.editing` imperatively. So `=== true` narrows nothing that is in use and fails closed, which is the right direction for a gate. Both pins redden against the uncoerced setters on `hasAttribute("draggable")`. The item pin's strongest claim was checked separately by disabling its first two assertions: `anchor.draggable` comes back `true` under the mutation, so the item axis genuinely fails **open** and jsdom does model the `<a href>` native-draggable default. The section card has no such default and its pin is defensive only, which its own comment already says.
- [x] verified — finding 3's assertion is independently discriminating and the target is the only one available. Deleting `if (!this.editing) { return false; }` from `handleDragKeydown` with the neighbouring `grabbed`-flags assertion disabled reddens the announcement assertion alone: `Expected "", Received "Accounts grabbed. Position 1 of 3."`. Querying the section's region is sound rather than incidental — `navigatorItem.html` carries no `[aria-live]` at all, the only two regions in the family are `navigatorSection.html:150-155` and `salesforceNavigator.html:145`, and the item-grab sentence is written by `navigatorSection.announce`, so the item's own section's region is where the announcement can only be.
- [x] verified — finding 4's gate discriminates, and its browser claim is right for the card and incomplete for the family. Removing the guard reddens exactly one test, the new pin, and nothing else in the suite depended on the old unconditional behaviour. "The browser will not fire `drop` after an unprevented `dragover`" is the HTML drag-and-drop model as written: an uncancelled `dragover` sets the current drag operation to none, and at the end of the drag a `dragleave` is fired at the current target element instead of a `drop`. So leaving `handleCardDrop` ungated is safe *for the card* — a drop on one of its items is `stopPropagation`ed by the item and never reaches it, and a drop on the card's own area now has no cancelled `dragover` behind it. The new pin proves that `preventDefault` was not called, which is not a lesser fact: it is precisely the input the browser's own model consumes, unlike the `draggable` attribute where jsdom cannot show the browser refusing. The claim does not extend to the item anchor, whose `dragover` is still cancelled — see the open finding above.
- [x] verified — the seven criteria, by mutation, all seven independently. 1: `draggable={editing}` to `draggable="true"` reddens two tests in each component (the gate test and the new coercion pin). 2: item axis, deleting the `handleDragKeydown` guard reddens the item unit test and composed `:1560`; section axis, deleting the `handleCardKeydown` guard reddens `navigatorSection.test.js`'s Space test and composed `:1800`. 3: `cardTabIndex` returning `"0"` unconditionally reddens `navigatorSection.test.js:868`. 4: not mutation-shaped (a "still works" criterion) — held by the suite being green with the pointer and keyboard tests now mounted in edit mode, and `navigatorSection.test.js:958` pins the in-edit-mode `over.defaultPrevented === true` that the new out-of-mode pin is the mirror of. 5: `handleClick`'s brace-matched body is byte-identical across `main`, `8ea706d` and `9c2ee58` (same md5), and `navigatorItem.test.js:104-144` mounts with `editing` unset. 6: removing `releaseGrab()` from the section setter reddens the section unit test; removing `releaseSectionGrab()` and `cardFocusIndex = undefined` from `leaveEditMode` reddens the composed one. 7: read against `main` — the comment is rewritten, not deleted, and records both things `## Design` asks of it. Every mutation reverted; 521 green on a clean tree afterward.
- [x] verified — trap 274's scan, run again independently with a strictly wider token list, and the spec-level claim re-established from the production code rather than inherited. My brace-matched pass over all 185 `it`/`it.each` blocks adds `savedItemIds` and `savedSectionNames` to the trap's literal five tokens, which the trap omits and which this suite actually uses: 44 payload-asserting blocks, 22 with no literal `enterEditMode` (not 17 — the extra five are the wider tokens, not new routes). All 22 classified by reading the helper: 6 are the documented `renameFirstItem`/`burstItemRename` debounce vehicles, and 16 mount through `navigatorOn` (`:2810-2817`), `navigatorWithThree` (`:1444-1450`) or `navigatorWithSections` (`:1779-1786`), each of which calls `enterEditMode` on its last line. Zero real routes. Independently from the code: 11 `applyLayout` call sites, walked to their gestures — `handleAddSection`, `handleSectionRename`, `handleSectionColumns`, `handleSectionDelete`, `handleItemRename`, `handleItemRemove`, `addChosenItem` and `handleItemMoveTo` (the Move to... menu, a fifth route into `moveItemBetween` the earlier box's "only through the four drag surfaces" wording misses) all sit behind an `lwc:if={editing}`/`lwc:if={isEditing}` control; `renameCurrentLayout` and `createNewLayout` sit behind both a template gate and `handleLayoutMenuSelect`'s `!this.isEditing` return at `:658-665`; `handleEditSave` is the Save button. `moveItemWithin`, `moveSectionTo` and `moveItemBetween`'s drag routes each require `dragFromIndex`, `dragKind` or `grabbedSectionIndex`, and every writer of those three is downstream of a `dragstart` on a `draggable="false"` element or of a now-gated keydown. Nothing else in the file writes. The claim holds after this pass, and it holds despite the open finding above, which is a surface without a write behind it.
- [x] verified — scope. Neither `c4b8290` nor `9c2ee58` touches `salesforceNavigator.js` production or anything of slice 02's; the fix pass is six files, three of them tests. The `touches:` over-declaration of `salesforceNavigator.js` stands as the earlier box records it.
- [x] fixed — gated `navigatorSection.js`'s `handleCardDrop` (`:867`) behind the identical `if (!this.editing) { return; }` its own `handleCardDragOver` already carries, making it the fourth of the family's four `dragover`/`drop` handlers to hold the guard, and reordered `navigatorItem.js`'s `handleDrop` (`:473`) so `event.stopPropagation()` fires unconditionally, ahead of the `editing` check, rather than after it — the line the critique named as the actual gap, since a guard that returned before it left the event free to bubble to the card regardless of what the card decided. Both comments the finding quoted are corrected in place rather than deleted, per `rstk-preserve-documentation.md`: `handleDrop`'s comment block no longer argues the card is safely left ungated, and now records that a re-review ran the identical probe against the card and got the identical result; `handleCardDrop` gained its own comment (it had none before) explaining it is gated for consistency with the item, not because a distinct production gap was found on the card itself. Three pins added, each watched red before green: `navigatorSection.test.js`'s "does not accept a drop directly on the card out of edit mode either" (dispatches `drop` straight at the card's own `<article>`, asserts `defaultPrevented === false` and no `sectiondrop`) reddened on `Expected: false, Received: true` when the new guard was deleted; `navigatorItem.test.js`'s "does not dispatch a drop out of edit mode either, and keeps the event off whatever is outside it" was rewritten to check the ordering directly — `event.cancelBubble` does not survive past the end of `dispatchEvent` in this jsdom even when `stopPropagation()` was genuinely called (checked independently, outside the suite, before trusting it), so the pin instead adds a `document.body` listener for `drop` and asserts it is never reached; reverting the reorder (moving the guard back ahead of `stopPropagation()`) reddened it on `escaped` being called once. Pinned at the composed level as instructed, not only in isolation: `salesforceNavigator.test.js` gained "does not move an item across sections when dropped on another item out of edit mode, and writes nothing" — `dragstart` on one item's anchor, `dragover` and `drop` on another item's anchor in a different section, mounted with no `enterEditMode` — asserting both the order is unchanged and neither `updateLayout` nor `createLayout` was called. Mutation-checked precisely: reverting only the `stopPropagation()` reorder left this pin green (the card's own gate still catches the bubbled event, so the two protections overlap for this exact gesture); reverting only `handleCardDrop`'s guard also left it green (the item's own `stopPropagation()` now stops the event before it ever reaches the card); reverting **both together** — reproducing the exact regression the critique measured — reddened it on the section order (`Action Plans` still moved across). The three tests the finding named as the measured cost were each moved to the edit-mode treatment already applied to the item's two: `navigatorSection.test.js`'s "stops looking like a drop target once the drop has happened" now mounts `createSection(undefined, { editing: true })`; `salesforceNavigator.test.js`'s "drags the item the user picked up, not the one sharing its stored position" and "puts the item at the end when it is dropped on the card rather than on an item" each gained an inline `await enterEditMode(element);`. All four `dragover`/`drop` handlers in the family now carry the guard, closing trap 276's 3-of-4 to 4-of-4.
- [x] fixed — gated `navigatorItem.js`'s `hintId` (`:252`) on `editing` as well as `!grabbed`, and changed the resting hint's block in `navigatorItem.html` from `lwc:else` to `lwc:elseif={editing}` (`:133`), so out of edit mode neither the hint node nor its `aria-describedby` reference renders at all — matching the section card's own precedent, used as instructed rather than inventing a shape. New pin in `navigatorItem.test.js`, "does not tell a keyboard user to press a key that does nothing, out of edit mode": mounted on a plain `createNavigatorItem({ index: 0 })` (`editing` defaults to `false`), asserts the hint span is absent, the instructions span is absent, and the anchor carries no `aria-describedby` attribute at all. Watched red against two independent mutations, restored after each: reverting `hintId` alone (leaving the HTML gate in place) reddened the `aria-describedby` assertion specifically — `hasAttribute` came back `true` because `hintId` still resolved to a real, if now-dangling, id even though the span itself stayed out of the DOM via the template's own gate; reverting the HTML alone (leaving `hintId` fixed) reddened the span-presence assertion instead — the corrected `hintId` had it computed to `undefined`, but the un-gated `lwc:else` rendered the span anyway with no `id` attribute. Each assertion in the new pin is therefore load-bearing against a different half of the fix, not redundant with the other. Two pre-existing hint tests — "tells a keyboard user the move gesture exists before they have guessed it" and "swaps the hint for the full instructions while grabbed, never both at once" — needed the same `editing: true` mount the drop-target tests above did, since their own subject (the hint's grabbed/at-rest swap) is unaffected by the mode gate and was written assuming the hint is reachable at rest; "attaches the instruction text only while grabbed, never permanently" got the same mount for the same reason, since its own comment leans on the hint appearing at rest. The comment at `navigatorItem.css:57-60` the finding quoted as false is corrected, not deleted: it now reads "...so nothing about this element's on-screen appearance still advertises a drag out of edit mode, cursor included," followed by a new paragraph recording that the unscoped claim used to be false while the resting hint ignored `editing`, and that the claim is now scoped to what the stylesheet is actually responsible for. Swept both components for anything else — attribute, label, `aria-*`, title, class or announcement — still advertising a drag at rest out of edit mode: grepped every `.html` file in the family for drag/grab/move/press-key/reorder language and walked each hit. Found nothing else. The section's own instructions stay `lwc:if={grabbed}` with no resting counterpart, confirmed clean as the finding said; the item's `aria-label`, `title` and `fallbackRole`/`fallbackTabIndex` name or route the link, not the gesture; both `_editing` cursor classes were already gated by the prior pass; no other `slds-assistive-text` span exists in either template. The hint was the only survivor.
- [x] verified — the stylesheet-text pins are meaningful, not ceremonial, and the precedent is real. `readFileSync`-on-CSS is an established technique here, at 18 pre-existing call sites across four suites, and the specific `_grabbed` precedent the pass cites exists verbatim: `navigatorItem.test.js:682-698` and `navigatorSection.test.js:1107-1118`, both matching `\.rstk-nav-*_grabbed\s*\{[^}]*\}` and asserting on the rule body. The pairing is what makes the new pins carry weight rather than grep noise — the DOM pin proves the class is computed only when `editing`, the text pin proves the declaration lives on that class and nowhere else, and the two are joined by the same literal class name, so a rename in one file alone reddens one of them. Both mutations run: putting `cursor: grab` back on the two base rules reddens exactly the two "keeps the grab cursor off the base rule" tests and nothing else; deleting the `if (this.editing)` branch from `anchorClass` and `cardClass` reddens exactly the two "carries the grab-cursor class only in edit mode" tests and nothing else. The anchored-regex claim is also right, and it matters: `/^\.rstk-nav-item\s*\{[^}]*\}/m` skips the compound `.rstk-nav-item__row .rstk-nav-item {` at `navigatorItem.css:9` — whose body holds `flex` and `min-width` and no `cursor`, so an unanchored regex would have matched it and passed for the wrong reason forever. Walked every selector in the file: line 29 is the only line-anchored match. `lwc-jest-assertions-that-can-fail.md`'s "drive the pin off the imported constant" does not bite here, there being no constant — the coupling is the class-name literal, and the DOM pin is the other end of it.
- [x] verified — source order, both stylesheets, and the `grabbing` claim holds. `navigatorItem.css`: `.rstk-nav-item_editing` at `:61`, `.rstk-nav-item_grabbed` at `:77`. `navigatorSection.css`: `.rstk-nav-section_editing` at `:22`, `.rstk-nav-section_grabbed` at `:34`. Each pair is two single-class selectors of equal specificity (0,1,0), so the later declaration wins and `cursor: grabbing` beats `cursor: grab` in both files. The ordering is load-bearing rather than incidental: a grab is only reachable in edit mode, so both classes are always co-present while grabbed. Nothing else in either file declares `cursor` on these elements — `.rstk-nav-item:hover` at `:65` is higher specificity but sets `background-color` only, and `.rstk-nav-item__row .rstk-nav-item` at `:9` is (0,2,0) and sets no cursor.
- [x] verified — the composed test at `:2666` and its two siblings. "Drops the item at the position it was dropped at, not at the end" still proves its name under `enterEditMode`: it asserts `[["Clients"], ["Action Plans", "Contacts"]]`, the *positional* landing, against `:2688`'s `[["Action Plans"], ["Contacts", "Clients"]]`, the append. Mutation rather than reading — passing `undefined` in place of `event.detail.itemIndex` to `moveItemBetween` (`salesforceNavigator.js:1586`) reddens `:2666` and one other, so the positional half is what it discriminates. Both "on the card rather than on an item" siblings do genuinely target the section's own `<article>`: `:2693` and `:2712` each resolve `querySections(element)[n].shadowRoot.querySelector("article")`, not an item anchor, and both were rightly left alone as the code stands — they pass *because* `handleCardDrop` is ungated, which is the open finding above and not a fault of theirs.
- [x] verified — census of the two moved unit tests, and neither lost an assertion or went vacuous. "Accepts a drop by cancelling dragover" kept all three assertions across the move (`defaultPrevented === true`, `dropEffect === "move"`, `detail === {index:1}`); "re-emits a drop as its own index" kept both (`defaultPrevented === true`, `detail === {index:3}`). Diffed line by line against `5a0c8c4`: the only changes are the `editing: true` mount and an added comment. Vacuity checked by mutation rather than by inspection, since five criteria on this spec have been found false when ticked — replacing both item guards with an unconditional `return` reddens exactly those two tests, so each still discriminates its own in-edit-mode subject and neither is now passing merely because the mode was handed to it. `createNavigatorItem` reaches the `@api` setter via `Object.assign` before `appendChild`, so `editing: true` is a real property write and not a silently-ignored field.
- [x] verified — trap 273 on everything this pass touched, all nine. The four new cursor pins and the two new drop-target pins carry no "writes nothing" assertion at all and sit on components that cannot write, so neither 273 shape is available to them; each was independently mutated above and reddens alone. The two moved item tests assert positive results — a cancelled event and an event detail — which `scheduleSave`'s `isEditing` return cannot satisfy, and both still redden on their own guard under edit mode. `:2666` asserts a specific non-default order rather than an absence, and reddens under the `itemIndex` mutation. The one 273-adjacent cost is real but belongs to the finding above rather than to a box of its own: moving `:2666` behind edit mode retired its out-of-edit-mode coverage, and the route it used to cover is still live — the composed probe shows that gesture still moving an item and still calling `updateLayout` once out of edit mode, through the ungated card handler.
- [x] verified — O1, from the production code, and my own drop-target scan. The trap-276 grep run rather than inherited: `ondragover` and `ondrop` appear on exactly two elements in the family, `navigatorItem.html:38-39` (the anchor) and `navigatorSection.html:15-16` (the card); `salesforceNavigator.html:230`'s `onsectiondrop` is a `CustomEvent` listener with no native default to cancel, and the canvas itself binds no drag handler. Four native handlers, two per component, exactly the inventory the trap predicts. Three carry the `editing` gate; `handleCardDrop` does not — see the open finding. Beyond the four: `dragDepth` still counts a foreign `dragenter`/`dragleave` out of edit mode, but the `_droptarget` highlight it feeds also requires `itemDragActive`, which the parent derives from `dragKind === "item"`, reachable only from an item `dragstart` the browser will not fire on `draggable="false"` — measured, the card renders bare `rstk-nav-section` after a foreign `dragenter` and only gains `_droptarget` when `itemDragActive` is forced true by hand. So on the four surfaces the slice names, O1 is met in a real browser: the two drag sources by `draggable="false"`, the two keyboard routes by the `handleDragKeydown` / `handleCardKeydown` gates, and the two drop targets by the two `dragover` gates. What is *not* clean is the fifth thing, which no surface inventory on this spec has counted — the item's permanent "Press Space to move." hint, second open finding above. O1 says "no drag surface"; an affordance that only a screen reader can perceive is still an affordance, and it is the one channel this slice left advertising a gesture that no longer works.
- [x] verified — scope, this pass. `9319337` touches eight files: `navigatorItem` js/css/test, `navigatorSection` js/css/test, `salesforceNavigator.test.js` (+6 lines, test only) and this slice document. No production change to `salesforceNavigator.js` and nothing of slice 02's — the confirm-before-discarding prompt machinery is untouched. `navigatorSection.css` is again outside the declared `touches:`, already recorded on the first `- [ ] excess` box. `npx jest` is 527 of 527 on a clean tree, `npx eslint force-app/main/default/lwc --max-warnings 0` and `prettier --check` both clean; every mutation above reverted and the tree verified clean afterwards.
- [x] verified — scope and result, this third pass. Six files touched, all named in `touches:` except `salesforceNavigator.test.js` (recorded below, as every prior pass's own share was): `navigatorItem` js/css/html/test, `navigatorSection.js`, `navigatorSection.test.js`, `salesforceNavigator.test.js`. No line of `salesforceNavigator.js` production code changed, and nothing of slice 02's touched — `git diff --stat` against `109097a` confirms both. All four `- [ ] excess` boxes above stand exactly as this pass found them. Deploy: `sf project deploy start`, no `--target-org` (the org this resolves to is `sfnav-t2`, confirmed via `sf config get target-org`); the bare run deployed both changed bundles (`navigatorItem`, `navigatorSection`) rather than reporting "No changes to deploy," so no explicit `--source-dir` fallback was needed — `Status: Succeeded`, 2/2 components. Full suite: `npx jest` is **530 of 530** on a clean tree (527 plus this pass's three new pins), `npx eslint force-app/main/default/lwc --max-warnings 0` and `prettier --check` on every file this pass touched both clean; every mutation run above (six of them: `handleCardDrop`'s guard, `handleDrop`'s `stopPropagation()` reorder alone, `handleCardDrop`'s guard alone, both together, `hintId` alone, the HTML `lwc:elseif` alone) was reverted and the tree confirmed clean afterward.
- [x] fixed — added the finding's own probe verbatim as a new test in `salesforceNavigator.test.js`, right after the existing "does not move an item across sections when dropped on another item out of edit mode, and writes nothing": "does not move an item across sections when dropped on the destination section's own card out of edit mode, and writes nothing" — `navigatorWithTwoSections()`, no `enterEditMode`, `dragstart` on an item anchor then `drop` on `querySections(element)[1].shadowRoot.querySelector("article")`, asserting the section order is unchanged and neither `updateLayout` nor `createLayout` fires. Re-ran both single mutations myself rather than trusting the finding's own account: reverting `handleCardDrop`'s `editing` guard alone reddens the new pin (`Action Plans` crosses into the second section) and leaves the pre-existing drop-on-item pin green; reverting only `handleDrop`'s `stopPropagation()` reorder in `navigatorItem.js` leaves the new pin green (the card's own guard still catches it) and leaves the pre-existing pin green too, matching the finding's account that this half of the mutation shows up in `navigatorItem.test.js`'s own unit-level assertion instead. Both mutations reverted after. The out-of-edit-mode drop-on-card route now has a write-side assertion again. Full jest suite green at 564/564 (563 carried in, plus this one new test); deployed clean to `sfnav-t2`. Original finding: The composed pin is under-specified for `handleCardDrop`, and the route it cannot see is the one this pass retired the composed coverage of. Both single mutations run independently rather than taken on report, and the report's account of them is exactly right: reverting only the `stopPropagation()` reorder leaves "does not move an item across sections when dropped on another item out of edit mode, and writes nothing" green and reddens `navigatorItem.test.js`'s `escaped` assertion instead (`Received number of calls: 1`); removing only `handleCardDrop`'s guard leaves the composed pin green and reddens `navigatorSection.test.js:1044` instead (`Expected: false, Received: true`); both together redden the composed pin on the section order, `Action Plans` still crossing. For *that* gesture the overlap is genuine and no assertion could separate them — a drop on another item's anchor is prevented by either protection alone and the observable is byte-identical either way, so the pin is not badly written for the gesture it picked. **A different gesture does separate them, and I measured it rather than reasoned it.** A composed drop landing on the destination section's own `<article>` rather than on one of its items never passes through the item at all, so the card's guard is the sole protection: I added that probe (`navigatorWithTwoSections`, no `enterEditMode`, `dragstart` on an item anchor then `dragover` and `drop` on `querySections(element)[1].shadowRoot.querySelector("article")`, asserting the order is unchanged and neither `updateLayout` nor `createLayout` fires), watched it pass on the shipped code, watched it fail on `handleCardDrop`'s guard removed alone (`Action Plans` crosses into the second section), and watched it stay green on the `stopPropagation()` reorder reverted alone. So the card's guard *is* isolable at the composed level; the shipped pin picks the one gesture where it is not. **Not a production defect** — the probe is green as shipped, and the guard is separately pinned at unit level in both components. What is missing is the *write*. `navigatorSection.test.js`'s new card pin asserts `defaultPrevented` and the absence of `sectiondrop`, which are component facts; whether that route reaches `updateLayout` or `createLayout` is a composed fact, and trap 274 is explicit that that inventory lives in `salesforceNavigator.test.js` rather than in the component you have just gated. This pass moved both composed tests that used to drive the drop-on-card route out of edit mode — `"puts the item at the end when it is dropped on the card rather than on an item"` and `"drags the item the user picked up, not the one sharing its stored position"` — behind `enterEditMode`, which is trap 273's retired-vehicle shape exactly, and the replacement pin it added drives a different gesture. The out-of-edit-mode drop-on-card route therefore now has no write-side assertion anywhere in the suite. Fifteen lines and one gesture close it, and it reddens on a mutation nothing else at the composed level can see.
