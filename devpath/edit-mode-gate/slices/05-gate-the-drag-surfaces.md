---
depends_on:
  - devpath/edit-mode-gate/slices/03-gate-the-section-header-controls.md
  - devpath/edit-mode-gate/slices/04-gate-the-item-menu.md
done: true
fix_cycles: 0
touches:
  - force-app/main/default/lwc/salesforceNavigator/salesforceNavigator.js
  - force-app/main/default/lwc/navigatorSection/navigatorSection.js
  - force-app/main/default/lwc/navigatorSection/navigatorSection.html
  - force-app/main/default/lwc/navigatorSection/__tests__/navigatorSection.test.js
  - force-app/main/default/lwc/navigatorItem/navigatorItem.js
  - force-app/main/default/lwc/navigatorItem/navigatorItem.html
  - force-app/main/default/lwc/navigatorItem/navigatorItem.css
  - force-app/main/default/lwc/navigatorItem/__tests__/navigatorItem.test.js
---

# Out of edit mode nothing can be dragged or grabbed

> Watch a new test go red before you make it green.
> _Why: a test written before the code cannot be written to hit a coverage number — there is nothing to
> cover yet. Retires when Apex gets mutation testing._

## What to build

Out of edit mode a user cannot rearrange the Navigator by any route — sections and items are not
draggable by pointer and cannot be grabbed from the keyboard — while clicking a link goes on working
exactly as it does today.

## Acceptance criteria

- [x] met Out of edit mode a section card and an item anchor both report `draggable="false"`.
- [x] met Out of edit mode pressing Space on a section or an item does not grab it, and no announcement is made.
- [x] met Out of edit mode a section card is not a tab stop, so a keyboard user moving through the panel stops only on links.
- [x] met In edit mode pointer dragging and the full keyboard grab-move-drop-cancel path both work exactly as they do today, announcements included.
- [x] met Clicking a link out of edit mode navigates, and a Ctrl-, Cmd- or Shift-click still reaches the browser instead of being intercepted.
- [x] met Leaving edit mode clears any keyboard grab that was in flight, so focus restoration cannot chase a card that is no longer grabbable.
- [x] met The comment in `navigatorItem.css` asserting that there is no edit mode is replaced with one recording what superseded it, not deleted.

## Deviations
- [ ] excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js, +1811 -172 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. This slice's fix pass touched the file again, so this is the current whole-branch figure and it supersedes the +1798 -174 on the earlier box for the same path; both stay open and both are closed at review. The fix pass's own share is the restored self-drop pin, the item-axis announcement assertion, and the new drop-target pin.
- [ ] excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js, +1798 -174 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. Cumulative across slices 01, 03, 04 and 05 — twelve prior commits have rewritten this suite, and boxes on slices 03 and 04 carry the earlier figures for the same path. This slice's own share is the six composed-level drag and keyboard tests brought to the gated build, two net-zero-write rewrites, and `enterEditMode` baked into the `navigatorWithThree` and `navigatorWithSections` mount helpers.

- The trap 274 scan, run myself before writing any closing claim: a text-level scan of `salesforceNavigator.test.js` for a payload assertion (`savedIds`/`savedItems`/`lastSavedLayout`/`updateLayout.mock`/`createLayout.mock`) with no literal `enterEditMode` in the `it` body returned **18** candidates at the start of this slice, not the 4-6 the prior record implied. Read individually: 6 are the `renameFirstItem`/`burstItemRename` debounce-vehicle tests slice 04 already documented as deliberately outside edit mode (false positives — they pin the save chain, not a reachable rename route, since renaming is menu-gated as of slice 04); 7 are inside "renaming an item" and mount through that describe's own `navigatorOn` helper, which calls `enterEditMode` internally (false positives — the literal string just isn't in the `it` body); and the remaining 5 were the real, then-ungated drag/keyboard-reorder surfaces: item pointer drag (`:1574`), item keyboard reorder (`:1604` and `:2285`), section pointer drag (`:1766`), section keyboard reorder (`:1790`). That confirms the four-surface, two-per-component inventory slice 04 handed down — item and section, pointer and keyboard — with the item axis carrying two pinning tests for its keyboard route rather than one. Re-run at the end of this slice: 17 candidates, all of them the same 6 debounce-vehicle and 7 `navigatorOn`-mounted false positives plus 4 more that are now false positives for the identical helper-embedded reason (`navigatorWithThree`/`navigatorWithSections`, below, now call `enterEditMode` themselves). Zero real surfaces remain.
- `enterEditMode` was baked into the shared mount helpers `navigatorWithThree()` (item-axis "reordering" describe, 9 tests) and `navigatorWithSections()` (section-axis "the sections themselves" describe, 15 tests after this slice's addition) rather than added to each test individually, on the same reasoning as slice 04's `navigatorOn`: essentially every test in each describe drives the card or the anchor as a draggable or keyboard-grabbable thing, so the shared helper is the one place to say so, per `rstk-dry-enforcement.md`. The "moving an item into another section" describe's shared helpers (`navigatorWithTwoSections`, `navigatorWithAWithdrawnTab`) were deliberately left alone, exactly as slice 04 left them — they are also the mount helpers for that describe's pointer-drag tests, which must stay representative of the out-of-edit-mode state they exercise at the JS-handler level (see below) — and `enterEditMode` was added inline to the one keyboard test that needed it instead ("reorders the item the user picked up when an earlier one is out of reach").
- Every test moved behind edit mode that also checked a persisted payload had its `settleAutosave()` replaced with `saveEdits(element)`, for the same reason slice 04's fix pass gives: `scheduleSave`'s `isEditing` guard suppresses the debounce entirely while editing, so autosave is no longer a route to a write and Save is the only one left.
- Three tests whose net effect was a cancelled/abandoned gesture landing back on the edit session's own entry state needed more than a mechanical swap, because Save's `hasUnsavedCanvasChanges` guard means a byte-identical canvas now writes **nothing**, where the old debounce would have written the identical-content payload anyway: "puts the item back where it started when the drag is cancelled with Escape" (item axis) and "reorders the sections from the keyboard, and cancels on Escape" (section axis) each asserted `savedItemIds`/`savedSectionNames` equal to the original order; both now assert `saveEdits` calls neither `createLayout` nor `updateLayout`. "Moves no card the user never picked up after an abandoned section drag" and "writes nothing when a section card is dropped back on itself" already asserted no write, but only via `settleAutosave` — under edit mode that assertion is satisfied by `scheduleSave`'s own suppression regardless of whether the abandon/self-drop guard fired, which is the vacuous-retarget shape trap 273 names; both were changed to `saveEdits` + `not.toHaveBeenCalled()` on both Apex calls so the assertion is back to discriminating the guard it names.
- Pure pointer-drag tests (a hand-rolled `dragstart`/`dragover`/`drop` `CustomEvent` dispatched directly on the anchor or the card) are **not** gated at the JS-handler level, matching `## Design`'s explicit choice to rely on the browser refusing to start a drag on `draggable="false"` rather than adding a second, redundant guard in `handleDragStart`/`handleCardDragStart` etc. Per `lwc-jest-ceilings.md`, jsdom enforces no such thing, so these tests were already green before this slice's gating code existed and stayed green after it, whether or not they were wrapped in `enterEditMode` — wrapping them (via the shared helpers above) makes what they exercise consistent with the state the design says pointer dragging is now confined to, but the wrap itself proves nothing a jsdom test can prove; the `draggable` attribute-value assertions are what actually discriminates the pointer-drag gate, and those are new, targeted unit tests (see below), not the existing drag-forwarding tests.
- `navigatorSection.test.js`'s "reordering its items" and "reordering its items from the keyboard" describes dispatch `itemgrab`/`itemkeymove`/etc. as raw `CustomEvent`s straight onto the `c-navigator-item` host, bypassing the item's own `handleDragKeydown` entirely — they test `navigatorSection`'s handling of an event it has already received, not whether the item would produce that event out of edit mode. Left unchanged (all still pass with `editing` at its default `false`): the origin-side gating they don't exercise is pinned separately, in `navigatorItem.test.js`'s own keyboard-drag-source tests and in the two new composed-level negative tests below.
- Added two composed-level negative tests not named by the slice brief, to close the loop between the unit-level gate and the assembled component: "does not grab an item on Space out of edit mode, and writes nothing" (item axis) and "does not grab a section card on Space out of edit mode, and writes nothing" (section axis), both in `salesforceNavigator.test.js`, mounted without `enterEditMode`.
- `navigatorItem.js`'s `handleDragKeydown` returns `false` (not merely `undefined`/no-op) when not editing, matching its existing "returns whether the key was consumed" contract, so `handleKeydown`'s own fallback-Enter branch is undisturbed by the new guard.

## Critique findings

- [x] fixed — restored `:1989`'s "writes nothing when a section card is dropped back on itself" to mount out of edit mode (`getLayouts.mockResolvedValue`, `createNavigator`, `getNavItems.emit`, `flush`, no `enterEditMode`) and assert via `settleAutosave()` in place of `saveEdits` + `not.toHaveBeenCalled()` — the vehicle the finding names as sufficient, since out of edit mode `scheduleSave` is not suppressed and carries no `hasUnsavedCanvasChanges`-style content check that could mask the short-circuit. Watched it go red first: with `if (from === to) { return; }` deleted from `handleSectionDrop` (`salesforceNavigator.js:1593-1595`), the rewritten test failed on `expect(updateLayout).not.toHaveBeenCalled()` (one call recorded, full `updateLayout` payload logged); restored the guard and the test is green again, with zero net diff left on `salesforceNavigator.js`. Also independently mutation-checked the three sibling rewrites the finding names, rather than taking the count on trust: `:1663` (item Escape) and `:1888` (section Escape) both redden when `handleEditSave`'s `hasUnsavedCanvasChanges` guard is bypassed (`const wroteChanges = true`) — each fails on `not.toHaveBeenCalled()` with one call recorded; `:1956` (abandoned section drag) reddens on the `sectionNames` order assertion, not the write assertion, when `handleSectionDragEnd`'s `this.clearDrag()` call is removed. All three mutations reverted afterward; all three genuinely discriminate and were left unchanged.
- [x] fixed — coerced both `editing` setters (`navigatorItem.js:115-123`, `navigatorSection.js:143-167`) to `value === true` before storing, so an uncoerced `undefined` crossing the `@api` boundary can no longer read back as "draggable". Pinned with a new test in each unit suite — `navigatorItem.test.js` and `navigatorSection.test.js` each gained "coerces an explicit undefined `editing` to draggable=\"false\" rather than failing open" — setting `editing: undefined` explicitly (direct property assignment, not `createSection()`'s destructured-default path, which cannot reach `undefined`) and asserting `hasAttribute`, `getAttribute` and the reflected `.draggable` property. Watched both pins go red against the unfixed setters: the item pin failed on `hasAttribute("draggable")` (`Expected: true, Received: false` — the attribute dropped entirely); the section pin failed the same assertion the same way. Both green after the fix.
- [x] fixed — added the missing item-axis announcement assertion to the composed test at `:1536` ("does not grab an item on Space out of edit mode, and writes nothing"), querying the item's own section's live region — `querySections(element)[0].shadowRoot.querySelector("[aria-live]")`, since the item-grab announcement lives in `navigatorSection.handleItemGrab`, not on `salesforceNavigator`'s own region — and asserting empty `textContent`, matching the section axis's existing `:1807-1809` shape. Watched it go red: deleting `if (!this.editing) { return false; }` from `navigatorItem.js`'s `handleDragKeydown` reddens the pre-existing `grabbed`-flags assertion as before; with that assertion temporarily disabled to isolate the new one, the announcement assertion alone also reddened on the same mutation (`Received: "Accounts grabbed. Position 1 of 3.​"`), which is what makes it a real, independently-discriminating pin rather than a duplicate of the flags check. Guard and test both restored; suite green.
- [x] fixed — gated `handleCardDragOver` (`navigatorSection.js:833-845`) behind `if (!this.editing) { return; }`, so out of edit mode the card no longer calls `preventDefault()` or sets `dropEffect = "move"` for a foreign drag — the drop-target half of O1's "no drag surface" that the drag-source half already covered via `draggable`. Left `handleCardDrop`, `handleCardDragEnter`/`handleCardDragLeave` and the drag-source handler (`handleCardDragStart`) untouched: the browser will not fire a native `drop` after an unprevented `dragover`, so a second guard on `handleCardDrop` would be redundant on the same reasoning the design already applies to trusting `draggable="false"` over a `handleDragStart` guard, and gating the drag source itself is the thing this pass was explicitly told not to do. Updated the one existing unit test that depended on the old unconditional behavior ("tells its parent when its own card is picked up and dropped on") to mount with `editing: true`, since its actual subject — dragstart/dragover/drop forwarding — is an in-edit-mode, criterion-4 concern. Added a new pin, "does not advertise itself as a drop target out of edit mode", asserting `over.defaultPrevented` is `false` on a plain `createSection()` (`editing` defaults to `false`). Watched it go red first — with the guard removed, `defaultPrevented` came back `true` — then restored the guard; the full suite (521 tests) is green.
- [x] verified — trap 274's scan, run independently rather than reconciled: a brace-matched pass over all 185 `it`/`it.each` blocks in `salesforceNavigator.test.js` finds 40 that assert a payload (`savedIds`/`savedItems`/`lastSavedLayout`/`updateLayout.mock`/`createLayout.mock`) and **17** of those with no literal `enterEditMode` in the body — the same count the builder reports. Every one classified by reading the helper rather than by trusting the dismissal: 6 in `describe("autosave")` drive `renameFirstItem`, which dispatches `itemrename` straight onto the item host and is a deliberate debounce vehicle documented at `:194-214`, not a route a user can reach now that the item menu is gated; 7 in `describe("renaming an item")` mount through `navigatorOn` (`:2795-2802`), which calls `enterEditMode`; 2 (`:1601`, `:1631`) mount through `navigatorWithThree` (`:1444-1450`), which calls `enterEditMode`; 2 (`:1830`, `:1854`) mount through `navigatorWithSections` (`:1771-1778`), which calls `enterEditMode`. Zero real ungated write routes remain by the scan's own criterion.
- [x] verified — the scan's blind spot checked independently from the production code rather than from the suite. All eleven `applyLayout` call sites in `salesforceNavigator.js` were walked back to a user gesture: `renameCurrentLayout`, `handleAddSection`, `handleSectionRename`, `handleSectionColumns`, `handleSectionDelete`, `handleItemRename`, `handleItemRemove` and `addChosenItem` all sit behind a control that is absent from the DOM out of edit mode (slices 01, 03, 04, plus the `!this.isEditing` re-check on the layout menu at `:662`); `moveItemWithin`, `moveSectionTo` and `moveItemBetween` are reachable only through the four drag surfaces this slice gates. A foreign drop reaches `handleSectionDrop` with no `dragKind` and returns without writing. Nothing else in the file writes.
- [x] verified — mount-helper census, all 23 tests. `navigatorWithThree` is used by 8 `it` blocks plus the 6-case `it.each(MOVES)` through `freshNavigator`; `navigatorWithSections` by 14, with the new out-of-edit-mode test at `:1792` mounting inline. Every one of the 23 drives a pointer drag, a keyboard grab, or focus restoration during one, so every one needs the mode it was moved into. Load-bearing discrimination re-checked by mutation on the four that assert an absence rather than a positive result; three redden and the fourth is the finding above. The rest assert a specific non-default order, payload or focus target and cannot be satisfied by a suppressed write.
- [x] verified — criterion 1 and trap 260. `draggable` renders as a value, not a presence, and the tests assert the value on both axes: `navigatorItem.test.js:304`/`:316`/`:708` and `navigatorSection.test.js:873`/`:888`, each checking `hasAttribute`, `getAttribute` and the reflected `.draggable` property. The one pre-existing `toContain("draggable")` at the old `:704` is gone. Mutating `draggable={editing}` back to `draggable="true"` reddens exactly one test in each component. The only surviving `[draggable]` presence check is `navigatorItem.test.js:1321`, and it is an absence claim about the whole node during `isRenaming` — the spec's own cited precedent — not a check on the gate.
- [x] verified — criterion 2 and criterion 3, by mutation. Deleting `if (!this.editing) { return false; }` from `handleDragKeydown` reddens the item unit test and the composed `:1536`. Deleting `if (!this.editing) { return; }` from `handleCardKeydown` reddens the section unit test and the composed `:1792`. Making `cardTabIndex` return `"0"` unconditionally reddens `navigatorSection.test.js:878`. The item anchor keeps its native link tab stop (`fallbackTabIndex` returns `undefined` whenever a real href resolved), so out of edit mode the panel's tab stops are links, the pencil and the layout switcher — which is what criterion 3 claims.
- [x] verified — criterion 6, both halves, and neither leaves a stale index. Section-level: `releaseGrab()` in the `editing` setter clears `grabbedItemIndex`, so `keepFocusOnGrabbedItem` early-returns; deleting it reddens the new `navigatorSection.test.js` test. Page-level: `leaveEditMode` clears `grabbedSectionIndex` via `releaseSectionGrab()` **and** `cardFocusIndex`, so `renderedCallback`'s `target` is `undefined` and the card-focus branch does not fight `focusEditTransition`'s hand-off to the pencil; deleting both lines reddens "ends any in-flight keyboard grab when edit mode ends". The two are genuinely distinct — different components, different fields — and both fire.
- [x] verified — criterion 5 and O7. `handleClick` is byte-identical to both `8ea706d` and `main`, compared by extracting the brace-matched method body from all three revisions. Its two `preventDefault()` cases stand and no drag suppression was added. The plain-click navigation test and the three modifier-click cases (`navigatorItem.test.js:104-144`) all mount with `editing` unset, so O7 is pinned in the state the Intent cares about rather than only in edit mode.
- [x] verified — criterion 7. The `navigatorItem.css` comment is replaced rather than deleted, per `rstk-preserve-documentation.md`, and the original reasoning survives inside it in substance ("HTML5 drag-and-drop separates a click from a drag ... a `click` only fires when no drag occurred"). It also says both things `## Design`'s "What this supersedes" requires: that the original reasoning was correct about clicking, and that the mode exists for a visual reason that reasoning never asked about.
- [x] verified — scope. `salesforceNavigator.js` carries no production change in `c4b8290` and nothing of slice 02's was touched; the diff is `navigatorItem` (js/html/css/test), `navigatorSection` (js/html/test) and `salesforceNavigator.test.js`. `npx eslint --max-warnings 0` and `prettier --check` are both clean on the changed components. Note the `touches:` front matter over-declares `salesforceNavigator.js`, which this slice did not modify.
- [x] verified — jsdom's ceiling, stated for the record. Of the four surfaces, the two keyboard ones are test-proven at unit and composed level; the two pointer ones rest on the attribute alone and nothing here can do better. Probe run to make the limit concrete rather than assumed: mounted out of edit mode, the existing synthetic `dragstart`/`dragover`/`drop` sequence on the item anchors still reorders the items and still writes a full `createLayout` through the debounce. That is the design's explicit choice — rely on the browser refusing to start a drag on `draggable="false"` rather than add a redundant `handleDragStart` guard — and `lwc-jest-ceilings.md` says jsdom enforces no such thing. So "nothing can reach the save path" is proven for the keyboard and asserted for the pointer, on one attribute-value test per component.
