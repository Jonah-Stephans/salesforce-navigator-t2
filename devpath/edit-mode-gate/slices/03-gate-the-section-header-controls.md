---
depends_on:
  - devpath/edit-mode-gate/slices/01-enter-and-leave-edit-mode.md
done: true
fix_cycles: 1
touches:
  - force-app/main/default/lwc/salesforceNavigator/salesforceNavigator.html
  - force-app/main/default/lwc/navigatorSection/navigatorSection.js
  - force-app/main/default/lwc/navigatorSection/navigatorSection.html
  - force-app/main/default/lwc/navigatorSection/__tests__/navigatorSection.test.js
---

# Out of edit mode a section is a heading and its items

> Watch a new test go red before you make it green.
> _Why: a test written before the code cannot be written to hit a coverage number — there is nothing to
> cover yet. Retires when Apex gets mutation testing._

## What to build

Out of edit mode a section shows its name and its links and nothing else; the "Add items" button and the
section's overflow menu — rename, column count, delete — appear only once the user enters edit mode.

## Acceptance criteria

- [x] met Out of edit mode a section renders no "Add items" button.
- [x] met Out of edit mode a section renders no overflow menu, so renaming it, changing its column count and deleting it are all unreachable.
- [x] met In edit mode all of those are present and behave exactly as they do today, including the 1-6 column-count entries.
- [x] met The controls are absent from the DOM rather than hidden by CSS, so neither the tab order nor a screen reader can reach them out of edit mode.
- [x] met A section takes the mode as an `@api editing` property set by the Navigator, and renders correctly for both values of it when mounted on its own.

## Deviations
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js, +1623 -178 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. This slice's fix pass touched the file again, so this is the current whole-branch figure and it supersedes the +1565 -180 on the earlier box for the same path; both stay open and both are closed at review. The fix pass's own share is finding 1's two restored mutations, finding 2's unfolding of `enterEditMode` from the `navigatorOn` helper with explicit calls added to the ~22 tests that need it, and one new end-to-end test for the empty-section message.
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.test.js, +1565 -180 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. Most of that figure is slices 01 and 03 together — this branch has rewritten this suite across seven prior commits — and this slice's own share is the ~48 tests the section gate broke, retargeted rather than deleted.
- [x] false positive — the file was legitimately worked by this slice and the slice's `touches` was written before anyone knew it would be; nothing out of scope was committed. Verified: `salesforceNavigator.test.js` is the parent suite for the component each of these slices gates, and four slices rewrote it because gating a child broke tests that had used the child's controls as an incidental vehicle; `navigatorSection.js`, `navigatorSection.css` and `salesforceNavigator.smallFormFactor.test.js` were each touched for a change the slice's own criteria required. Closed by Jonah, 2026-09-01. Original audit note: excess — force-app/main/default/lwc/salesforceNavigator/__tests__/salesforceNavigator.smallFormFactor.test.js, +11 -0 against `main` as this branch found it; committed by `git add -A` and outside this slice's `touches`. A local `enterEditMode` helper this suite did not have, added because gating the section header broke it too.

- The section's transient `isRenaming` state is not itself named by any acceptance criterion, but it is only ever entered through the now-gated overflow menu. Its setter for `editing` resets `isRenaming` to `false` whenever `editing` turns off, so a rename left open when edit mode ends (e.g. via the parent's Save/Cancel) cannot leave a `lightning-input` on screen with no menu left to close it. Covered by a new test ("cancels an in-progress rename when edit mode ends…") in `navigatorSection.test.js`. This is a `how` decision within the same header-control gate this slice builds, not a new control or a new criterion.
- This slice's own production code and `navigatorSection.test.js` changes were built directly, single-writer. Fixing the collateral fallout in `salesforceNavigator.test.js` and `salesforceNavigator.smallFormFactor.test.js` (48 tests broken by gating the section, most having nothing to do with this slice's five criteria — they exercised the section's overflow menu or "Add items" button as an incidental vehicle for testing unrelated save-chain/debounce/layout-switch behaviour) was delegated to a spawned fork running in parallel with this session's own direct edits to the same file. The two writers collided on `salesforceNavigator.test.js`: the fork detected a concurrent edit, stopped, and returned; both sets of edits were already interleaved on disk. Recovered by treating the file as it stood as the shared starting point, re-reading fresh before every subsequent edit, checking for and finding no duplicated helpers, and re-verifying every retargeted assertion end to end — full jest run (504/504) and lint (0 problems) after reconciliation. No further subagents were spawned after the collision; the remaining ~15 test fixes were done serially by this session alone.
- Out of edit mode a section is still not the whole of "no canvas-edit path from a section can reach the save path" — that claim was over-broad and this fix pass does not close it. `navigatorSection.html:6-19` still carries `draggable="true"`, `tabindex="0"` and an ungated `onkeydown` on the section card itself, and `salesforceNavigator.test.js`'s "reorders the sections when a section card is dragged onto another" mounts out of edit mode, drags a card, and asserts a written `updateLayout` — a Tier 1 canvas edit, dispatched from a section, with no Save pressed. That surface belongs to slice 05, not this one, and this slice is right to leave it untouched; recorded here so slice 03 is not read as having closed the class of bug it only closed for the section header's own controls.

## Critique findings

- [x] fixed — restored a real mutation beside the control-absence assertions in both tests, rather than only the control absence. Added `await renameFirstItem(element, 0, "Renamed");` immediately before `await settleAutosave();` in both "tells the user when their stored layout could not be read, and saves nothing until it can" and "says so, names what an administrator needs, and saves nothing" (`salesforceNavigator.test.js`). Watched red first: deleting `scheduleSave`'s `if (this.hasLayoutLoadError) { return; }` (`salesforceNavigator.js:1929-1931`) reddened both tests — `createLayout` was called once in each, per the failure output (`Received number of calls: 1`) — then restored the guard and both went green again, and the two tests still assert the stronger control-absence checks the critic said should stay.
- [x] fixed — removed `await enterEditMode(element);` from the `navigatorOn` shared mount helper (`salesforceNavigator.test.js`, in the "removing an item and adding it back" describe), so it mounts only, as its name always implied. "writes nothing when a removal names no item on screen" now runs out of edit mode by default and exercises `handleItemRemove`'s payload-equality guard directly. Added an explicit `await enterEditMode(element);` to every other test in that describe (~22 of them) that reaches for the Add-items button, the section's own overflow menu, or Save — the three tests using only an item's own (still-ungated) menu with no write assertion needed no change. Watched red first: deleting `handleItemRemove`'s guard (`salesforceNavigator.js:1472-1474`) reddened the test with `createLayout` called once; restored, green, and the full 26-test describe passes. Looked at the other four tests the critic named as having an unfailable write half: the "merely opened" and "cancelled" tests have no removable guard of their own to begin with — opening never calls `addChosenItem`, and a cancel's `undefined` tabId is already screened out by `addItemToSection`'s own accessible-set check independent of `addChosenItem`'s `if (!tabId)`; the falsy-blank-value and Escape tests are the ones where `addChosenItem`'s guard is actually load-bearing (an empty id *is* in the accessible set), and both already carry an `itemLabelsBySection` equality assertion that pins it — confirmed by inspection, not just by the critic's claim. All four needed only to gain `enterEditMode`, not a new assertion.
- [x] fixed — `emptyMessage` (`navigatorSection.js`) now branches on `this.editing`: unchanged in edit mode ("This section has no items. Use Add items to put tabs you can reach into it."), and out of edit mode "This section has no items." with no button reference, since the button that sentence used to name is exactly what this slice gates out of the DOM at that state. Updated the doc comments on `emptyMessage`, the `@api editing` property, and the HTML comment above `navigatorSection.html`'s `lwc:if={isEmpty}` block to record the branch (`rstk-preserve-documentation.md`). Added "says only that the section is empty out of edit mode, since there is no button to point at" to `navigatorSection.test.js` and "says only that the section is empty once Save leaves it that way out of edit mode" to `salesforceNavigator.test.js`; both watched red against the prior unconditional message (`Expected substring: not "Add items"` / `Received string: "...Use Add items..."`) before the fix and green after.
- [x] fixed the record, not the code — this is a documentation correction; see the new `## Deviations` bullet below stating plainly that the section drag and keyboard-reorder path remains a live out-of-edit-mode write route until slice 05 gates it. No production change: `navigatorSection.html:6-19`'s `draggable`, `tabindex` and `onkeydown` are slice 05's and were left untouched, and "reorders the sections when a section card is dragged onto another" (`salesforceNavigator.test.js`, around `:1711`) is unchanged and still green.
- [x] false positive — the predicted dead-end did not occur, verified on current code: `renameFirstItem` (`salesforceNavigator.test.js:215-224`) dispatches the `itemrename` `CustomEvent` directly on the `c-navigator-item` element and never opens its menu, so it works in every mode and no gate retires it. Slice 04 chose that vehicle and slice 05 did not disturb it; the tests still run out of edit mode, which is the state `scheduleSave` arms in. The concern was live when written and the code has since made its prediction false. Closed by Jonah, 2026-09-01. Original finding: The re-swap due at slice 04 is contained, but the problem is only deferred. All twelve swaps route through two helpers (`renameFirstItem` / `burstItemRename`, `salesforceNavigator.test.js:202-221`), so gating the item menu is a two-function edit — except that every one of those tests has to run *out* of edit mode, since that is the only state in which `scheduleSave` arms at all, and after slice 04 the sole remaining ungated Tier 1 act is the section card's own drag and keyboard reorder, which slice 05 then gates as well. At that point nothing can arm the debounce in any state and the twelve have no vehicle left. That is a decision — a test seam, or retiring the debounce's tests along with the debounce the design already calls neutered — and it should be taken deliberately at 04 or 05 rather than discovered mid-build.
- [x] false positive — the vehicle swap holds. Each of the twelve retargeted tests was checked by deleting the guard its name claims and confirming it still reddens: the debounce delay (`:1248` and `:1258`), leading-edge capture of `pendingSave` (`:1258`), `rememberSaved`'s adoption of the created id (`:1280`), `persist`'s update/create branch in both directions (`:1302` and `:1326`), `disconnectedCallback`'s `flushPendingSave` (`:1358`), the error path leaving `storedLayout` alone (`:1371`), `handleEditStart`'s pre-edit flush (`:5466`), and for the four switch-race tests (`:4190`, `:4220`, `:4256`, `:4292`) the pre-switch flush and reading `layoutId` at write time rather than at change time. That last set reddens on exactly the same mutations, and only those, as it did at `6e6974b`. Nothing was proved by `columns-N` that the item rename does not prove.
- [x] false positive — the `isRenaming` reset is in scope, correct and pinned. It closes the one transient state the gated menu is the only route into, which is the same gate rather than a new control; deleting `if (!value) { this.isRenaming = false; }` reddens the new test and nothing else. LWC calls the setter only when the bound value changes, so it fires on a real transition rather than on every parent re-render, and `draftName` surviving is harmless because `startRename` overwrites it.
- [x] false positive — scope, the fourteen section-suite preconditions and the deploy all check out. This commit changes nothing in `navigatorItem` (slice 04), nothing in `salesforceNavigator.js` (slice 01) and neither `draggable` nor `tabindex` (slice 05); `navigatorSection.test.js:830` still asserts `card.draggable === true` and `tabindex === "0"`, is untouched by the diff and still passes. Every one of the fourteen tests given `createSection(section, { editing: true })` kept its original assertions, and every one of them fails when the precondition is removed, so none of them is decoration. The deploy did land despite the bare command's "No changes to deploy": the target org's tracking file (`.sf/orgs/00DO800000XdtJPMAZ/maxRevision.json`, written 12:08, two minutes before the commit) holds revisions 482-491 as the `navigatorSection` and `salesforceNavigator` bundles with `lastRetrievedFromServer` equal to `serverRevisionCounter` — exactly the two bundles this slice touched. `eslint --max-warnings 0` and `prettier --check` are both clean, and the suite is 504/504.
- [x] fixed — the name, not the code: made the name true rather than trying to make the test check a guard, because mutation shows there is no guard reachable from this gesture for it to check. Went further than the finding's own repro to be sure: with *both* `addChosenItem`'s `if (!tabId) { return; }` *and* `addItemToSection`'s accessible-set check (`navigatorLayoutModel.js:661`) removed at the same time, the Escape test still stays green — nothing downstream of `addChosenItem` ever renders an item whose id matches nothing in the resolved tab list, and the test runs in edit mode, where `scheduleSave` writes nothing regardless of what changed. So it is not merely that a different, downstream guard happens to catch this input instead of `addChosenItem`'s own (the finding's framing) — no reachable mutation on this path reddens either assertion at all, which is a stronger reason the "confirmed by inspection" claim was wrong. Renamed to "writes nothing when Escape closes the picker, though no guard here is what stops it" and rewrote its header comment to say so plainly, including the double-guard-removal check. "Writes nothing when the picker closes with a falsy value that is not undefined" (the blank-value test, just above it) remains the sole test discriminating `addChosenItem`'s `if (!tabId)` guard — re-verified: deleting it reddens only that one test. Full jest suite green at 564/564; deployed clean to `sfnav-t2`. Original finding: The Escape test does not pin `addChosenItem`'s `if (!tabId)`, and "confirmed by inspection" is wrong on exactly the half the last round's own trap says to mutate. Deleting `if (!tabId) { return; }` (`salesforceNavigator.js:1521-1523`) reddens exactly one test in the whole 506 — "writes nothing when the picker closes with a falsy value that is not undefined" — and deleting `addChosenItem`'s payload-equality return alongside it still reddens only that one, so "adds nothing and writes nothing when Escape closes the picker" survives both of that method's guards being gone. The reason is which value each test supplies, not which assertion it carries: Escape resolves `undefined`, and `addItemToSection` rejects `undefined` on its own accessible-set check (`navigatorLayoutModel.js:661`), so the Escape test's `itemLabelsBySection` equality is satisfied two modules downstream and pins nothing in this component; only the blank-string id the other test drives is in the accessible set and therefore discriminating, which is what that test's own comment already says. No coverage was dropped — the guard is pinned, once, by the blank-value test — but the Escape test asserts less than its name claims in both halves and still does, and the disposition should have been reached by mutation rather than by reading, per the trap this slice's last round produced.
- [x] false positive — the predicted dead-end did not occur, verified on current code: `renameFirstItem` (`salesforceNavigator.test.js:215-224`) dispatches the `itemrename` `CustomEvent` directly on the `c-navigator-item` element and never opens its menu, so it works in every mode and no gate retires it. Slice 04 chose that vehicle and slice 05 did not disturb it; the tests still run out of edit mode, which is the state `scheduleSave` arms in. The concern was live when written and the code has since made its prediction false. Closed by Jonah, 2026-09-01. Original finding: Finding 5's "twelve" is now fourteen, and the two this pass added are the two that cannot be re-vehicled the way the other twelve can. `renameFirstItem` / `burstItemRename` now carry 14 distinct tests (`salesforceNavigator.test.js:611`, `:737`, `:1256`, `:1266`, `:1288`, `:1310`, `:1334`, `:1366`, `:1379`, `:4250`, `:4280`, `:4316`, `:4352`, `:5526`), up from 12 at `cdf61ea`, because finding 1's fix put the vehicle into "tells the user when their stored layout could not be read, and saves nothing until it can" and "says so, names what an administrator needs, and saves nothing". Those two run on `hasLayoutLoadError` fixtures and each asserts the edit affordance is absent, so `canEdit` is false and edit mode is unreachable in them by construction: when slice 04 gates the item menu they lose their only mutation with no in-edit-mode fallback at all, unlike the other twelve, which can at least be driven through Save. What slice 04 inherits is therefore fourteen call sites in two helpers plus one guard — `scheduleSave`'s `hasLayoutLoadError` early return — that becomes unreachable from any gesture in either mode, and the decision finding 5 asks for has to cover it. Recorded rather than fixed: keeping the mutation was the right call for this slice and the alternative was leaving the guard uncovered now to make slice 04 tidier.
- [x] false positive — the rest of that inspection claim holds, checked by mutation rather than by reading. With both of `addChosenItem`'s guards deleted, "writes nothing when the picker is merely opened" and "writes nothing when the picker is cancelled" stay green, and there is no third guard on either path to delete: opening reaches no `applyLayout` at all, and a cancel's `undefined` is rejected by `addItemToSection`'s accessible-set check exactly as the pass says. Their write halves are unfailable, but nothing was dropped in reaching that state — both were already inside edit mode at `2391b2e`, and it is `scheduleSave`'s `isEditing` return that makes the assertion redundant rather than anything the unfold did. Separately verified as intact by the same method: `scheduleSave`'s `hasLayoutLoadError` return now reddens both `canEdit`-false tests; `handleItemRemove`'s payload-equality guard now reddens "writes nothing when a removal names no item on screen"; `addChosenItem`'s `isAttached` guard reddens the post-disconnect test; and `emptyMessage`'s branch reddens in both directions — dropping the display-mode arm reddens the two new tests, and dropping the edit-mode arm reddens the two that assert the full sentence. `addChosenItem`'s own payload-equality return is pinned by nothing, but that is pre-existing and unreachable by design, since the picker never offers an already-placed tab.
