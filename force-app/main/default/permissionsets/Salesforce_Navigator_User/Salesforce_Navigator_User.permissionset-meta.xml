<?xml version="1.0" encoding="UTF-8" ?>
<PermissionSet xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>Salesforce Navigator User</label>
    <description
  >Grants visibility of the Salesforce Navigator tab, and the access to Navigator_Layout__c the component needs to keep a user's own layouts. Assignment is a documented admin step — PermissionSetAssignment is data, not metadata.</description>
    <hasActivationRequired>false</hasActivationRequired>
    <classAccesses>
        <apexClass>NavigatorLayoutController</apexClass>
        <enabled>true</enabled>
    </classAccesses>
</PermissionSet>
